---
phase: 11-wave6-expansion-implementations
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/build/system-reminder-injection/src/index.ts
  - packages/build/system-reminder-injection/src/system-reminder-injection.test.ts
  - packages/build/output-style-system/src/index.ts
  - packages/build/output-style-system/src/output-style-system.test.ts
  - packages/build/dialogue-history-manager/src/index.ts
  - packages/build/dialogue-history-manager/src/dialogue-history-manager.test.ts
autonomous: true
requirements: [W6-06, W6-07, W6-11, NFR-01, NFR-03]

must_haves:
  truths:
    - "injectReminder appends wrapped reminder content to message string"
    - "wrapInReminderTags wraps content in XML tags with configurable tag name"
    - "extractReminders parses system-reminder tags from message string"
    - "shouldInjectReminder returns boolean based on source type rules"
    - "loadOutputStyles returns merged styles array with project taking precedence"
    - "isPlainText returns true when first N chars contain no markdown markers"
    - "createMarkdownCache returns LRU Map with eviction at max entries"
    - "DialogueHistoryManager.addMessage appends to in-memory array"
    - "getEffectiveMessages returns messages after last compact boundary"
    - "insertCompactBoundary adds boundary message splitting effective from archived"
  artifacts:
    - path: "packages/build/system-reminder-injection/src/index.ts"
      provides: "System reminder tag injection"
      contains: "function injectReminder"
    - path: "packages/build/output-style-system/src/index.ts"
      provides: "Output style loading and LRU cache"
      contains: "function createMarkdownCache"
    - path: "packages/build/dialogue-history-manager/src/index.ts"
      provides: "JSONL dialogue history management"
      contains: "class DialogueHistoryManager"
  key_links: []
---

<objective>
Implement 3 moderate build-tier TS packages: system-reminder-injection, output-style-system, dialogue-history-manager. All standalone.

Purpose: Convert type stubs to working implementations for reminder injection, output styling, and dialogue persistence.
Output: 3 working packages with TDD test suites, zero TODO throws.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-wave1-quick-wins/06-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement system-reminder-injection and output-style-system</name>
  <files>
    packages/build/system-reminder-injection/src/index.ts,
    packages/build/system-reminder-injection/src/system-reminder-injection.test.ts,
    packages/build/output-style-system/src/index.ts,
    packages/build/output-style-system/src/output-style-system.test.ts
  </files>
  <read_first>
    packages/build/system-reminder-injection/src/index.ts,
    packages/build/output-style-system/src/index.ts,
    packages/extract/cost-tracker/src/index.ts,
    packages/extract/cost-tracker/src/cost-tracker.test.ts
  </read_first>
  <behavior>
    system-reminder-injection:
    - wrapInReminderTags("hello") returns "<system-reminder>hello</system-reminder>"
    - wrapInReminderTags("hello", "custom-tag") returns "<custom-tag>hello</custom-tag>"
    - injectReminder("msg", reminder) returns "msg\n<system-reminder>reminder.content</system-reminder>"
    - extractReminders("<system-reminder>content</system-reminder>") returns array with one SystemReminder
    - extractReminders("no tags here") returns empty array
    - shouldInjectReminder("user_context", 1) returns true (always inject user_context)
    - shouldInjectReminder("mcp_status", 3) returns true only on periodic turns (e.g., every 5 turns)

    output-style-system:
    - loadOutputStyles(config) returns simulated style array (in-memory, no real filesystem)
    - applyOutputStyle("# heading", style) transforms content based on style flags
    - applyOutputStyle(content, style) with keepCodingInstructions=true preserves code blocks
    - isPlainText("hello world") returns true
    - isPlainText("# heading\n**bold**") returns false
    - isPlainText("plain", 5) uses sampleSize=5 for check
    - createMarkdownCache() returns MarkdownCache with get/set/has/clear/size
    - createMarkdownCache({maxEntries: 2}).set 3 items, oldest evicted (LRU)
  </behavior>
  <action>
    Follow TDD workflow established in Phase 6.

    **system-reminder-injection:**
    1. Write system-reminder-injection.test.ts with 7+ tests.
    2. Implement in index.ts:
       - wrapInReminderTags: `` return `<${tagName ?? "system-reminder"}>${content}</${tagName ?? "system-reminder"}>` ``
       - injectReminder: append wrapped content to message. If config.wrapInTags (default true), use wrapInReminderTags. Return `message + "\n" + wrappedContent`.
       - extractReminders: use regex `/<system-reminder>([\s\S]*?)<\/system-reminder>/g` to find all tags. For each match, create SystemReminder with source "user_context", content from capture group, targetMessageIndex 0, timestamp Date.now().
       - shouldInjectReminder: switch on source — "user_context"/"tool_result"/"attachment" always true. "mcp_status"/"deferred_tools" true every 5 turns (`turnCount % 5 === 0`). "todo_write" always true.
    3. Remove all underscore prefixes. Preserve all type/interface exports.

    **output-style-system:**
    1. Write output-style-system.test.ts with 8+ tests.
    2. Implement in index.ts:
       - loadOutputStyles: return simulated array. Create 2 default styles in-memory: "default" (keepCodingInstructions=true, forceForPlugin=false) and "concise" (keepCodingInstructions=false, forceForPlugin=false). Merge: project styles override user styles by name.
       - applyOutputStyle: if style.forceForPlugin, wrap content in plugin markers. If style.keepCodingInstructions, preserve code blocks as-is. Otherwise, strip code blocks. Append style.content as suffix instruction.
       - isPlainText: take first `sampleSize` chars (default 500). Check for markdown markers: `#`, `**`, `__`, `` ` ``, `- `, `> `, `|`, `[`. Return true if none found.
       - createMarkdownCache: return object backed by `new Map()`. On set: if map.size >= maxEntries (default 500), delete the first key (oldest insertion = LRU since Map preserves insertion order). Then set. On get: delete and re-set to move to end (LRU refresh). Return object implementing MarkdownCache interface.
    3. Remove all underscore prefixes. Preserve all type/interface exports.

    Verify: `cd packages/build/system-reminder-injection && bun test` and `cd packages/build/output-style-system && bun test`
  </action>
  <verify>
    <automated>cd packages/build/system-reminder-injection && bun test && cd ../../build/output-style-system && bun test</automated>
  </verify>
  <acceptance_criteria>
    - packages/build/system-reminder-injection/src/index.ts contains "system-reminder" and does NOT contain "throw new Error"
    - packages/build/system-reminder-injection/src/system-reminder-injection.test.ts contains at least 6 "test("
    - packages/build/output-style-system/src/index.ts contains "new Map" and "localeCompare" or "maxEntries" and does NOT contain "throw new Error"
    - packages/build/output-style-system/src/output-style-system.test.ts contains at least 7 "test("
    - Both test suites exit 0
  </acceptance_criteria>
  <done>system-reminder-injection and output-style-system have working implementations with passing TDD tests, zero TODO throws</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement dialogue-history-manager</name>
  <files>
    packages/build/dialogue-history-manager/src/index.ts,
    packages/build/dialogue-history-manager/src/dialogue-history-manager.test.ts
  </files>
  <read_first>
    packages/build/dialogue-history-manager/src/index.ts,
    packages/build/system-reminder-injection/src/system-reminder-injection.test.ts
  </read_first>
  <behavior>
    - constructor(config) stores config and initializes empty messages array
    - addMessage(msg) appends message to internal array
    - getEffectiveMessages() returns messages after last compact_boundary, or all if no boundary
    - persist() serializes messages to JSONL format string (in-memory, simulated — stores serialized string)
    - loadFromDisk(path) parses JSONL string back to DialogueMessage[] (simulated — uses internal serialized state)
    - insertCompactBoundary("summary text") inserts CompactBoundaryMessage with type "compact_boundary"
    - getEffectiveMessages after insertCompactBoundary returns only messages added after the boundary
    - multiple compact boundaries: getEffectiveMessages returns messages after the LAST boundary
  </behavior>
  <action>
    Follow TDD workflow.

    1. Write dialogue-history-manager.test.ts with 8+ tests.
    2. Implement in index.ts:
       - Private fields: `private messages: DialogueMessage[] = []`, `private config: HistoryConfig`, `private serialized: string = ""`
       - constructor: store config, init empty messages and serialized
       - addMessage: push msg to messages array. If messages.length > config.maxRecords, shift oldest.
       - getEffectiveMessages: find last index where type === "compact_boundary". If found, return slice from index+1. Otherwise return all messages (spread copy).
       - persist: `this.serialized = this.messages.map(m => JSON.stringify(m)).join("\n");` (simulated JSONL — no real filesystem)
       - loadFromDisk: parse `this.serialized.split("\n").filter(Boolean).map(line => JSON.parse(line) as DialogueMessage)`. Return parsed array and also set this.messages to it.
       - insertCompactBoundary: create CompactBoundaryMessage with type "compact_boundary", content summary, timestamp Date.now(), metadata {}, summary, originalMessageCount = current messages.length. Push to messages.
    3. Remove all underscore prefixes. Preserve all type/interface/class exports.

    Verify: `cd packages/build/dialogue-history-manager && bun test`
  </action>
  <verify>
    <automated>cd packages/build/dialogue-history-manager && bun test</automated>
  </verify>
  <acceptance_criteria>
    - packages/build/dialogue-history-manager/src/index.ts contains "class DialogueHistoryManager" and "compact_boundary"
    - packages/build/dialogue-history-manager/src/index.ts does NOT contain "throw new Error"
    - packages/build/dialogue-history-manager/src/dialogue-history-manager.test.ts contains at least 7 "test("
    - Test suite exits 0
  </acceptance_criteria>
  <done>dialogue-history-manager has working implementation with JSONL persistence simulation, compact boundary support, and passing TDD tests</done>
</task>

</tasks>

<verification>
All 3 packages compile and tests pass:
```bash
cd packages/build/system-reminder-injection && bun test
cd packages/build/output-style-system && bun test
cd packages/build/dialogue-history-manager && bun test
```
</verification>

<success_criteria>
- 3 packages implemented with zero TODO throws
- All test suites pass
- LRU cache evicts oldest entries correctly
- Compact boundary splits effective messages correctly
</success_criteria>

<output>
After completion, create `.planning/phases/11-wave6-expansion-implementations/11-02-SUMMARY.md`
</output>
