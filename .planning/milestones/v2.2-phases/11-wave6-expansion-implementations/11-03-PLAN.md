---
phase: 11-wave6-expansion-implementations
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/build/plugin-lifecycle-manager/src/index.ts
  - packages/build/plugin-lifecycle-manager/src/plugin-lifecycle-manager.test.ts
  - packages/build/sdk-bridge/src/index.ts
  - packages/build/sdk-bridge/src/sdk-bridge.test.ts
  - packages/build/onboarding-flow-engine/src/index.ts
  - packages/build/onboarding-flow-engine/src/onboarding-flow-engine.test.ts
autonomous: true
requirements: [W6-08, W6-09, W6-12, NFR-01, NFR-03]

must_haves:
  truths:
    - "PluginLifecycleManager.discover returns simulated manifests from seed dirs"
    - "PluginLifecycleManager.activate transitions plugin state to active"
    - "PluginLifecycleManager.cleanupOrphaned removes stale entries and returns count"
    - "SDKBridge.connect stores config and marks connected"
    - "SDKBridge.send serializes message as NDJSON (JSON + newline)"
    - "SDKBridge.onMessage registers handler, handler called on simulated receive"
    - "SDKBridge.handleControlRequest returns true for requiresApproval=false"
    - "OnboardingFlowEngine.assembleSteps filters skipped steps and validates deps"
    - "OnboardingFlowEngine.run executes steps in order, records state"
    - "OnboardingFlowEngine.skipTo marks intermediate steps as skipped"
  artifacts:
    - path: "packages/build/plugin-lifecycle-manager/src/index.ts"
      provides: "Four-phase plugin lifecycle"
      contains: "class PluginLifecycleManager"
    - path: "packages/build/sdk-bridge/src/index.ts"
      provides: "SDK WebSocket bridge simulation"
      contains: "class SDKBridge"
    - path: "packages/build/onboarding-flow-engine/src/index.ts"
      provides: "Dependency-ordered onboarding flow"
      contains: "class OnboardingFlowEngine"
  key_links: []
---

<objective>
Implement 3 moderate build-tier TS packages: plugin-lifecycle-manager, sdk-bridge, onboarding-flow-engine. All standalone.

Purpose: Convert type stubs to working implementations for plugin lifecycle, SDK bridge, and onboarding flow engine.
Output: 3 working packages with TDD test suites, zero TODO throws.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-wave1-quick-wins/06-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement plugin-lifecycle-manager and sdk-bridge</name>
  <files>
    packages/build/plugin-lifecycle-manager/src/index.ts,
    packages/build/plugin-lifecycle-manager/src/plugin-lifecycle-manager.test.ts,
    packages/build/sdk-bridge/src/index.ts,
    packages/build/sdk-bridge/src/sdk-bridge.test.ts
  </files>
  <read_first>
    packages/build/plugin-lifecycle-manager/src/index.ts,
    packages/build/sdk-bridge/src/index.ts,
    packages/extract/cost-tracker/src/index.ts,
    packages/extract/cost-tracker/src/cost-tracker.test.ts
  </read_first>
  <behavior>
    plugin-lifecycle-manager:
    - constructor(config) stores config, initializes plugins Map<string, {manifest, state}>
    - discover() returns simulated manifests array (e.g., 2 hardcoded test manifests). Sets state to "discovered" for each.
    - loadFromCache() returns simulated cached manifests. Sets state to "cached".
    - cleanupOrphaned() removes entries with state "orphaned" from internal map, returns count removed. MUST be async (return Promise<number>).
    - logSessionPlugins(active) stores active list for telemetry (no-op beyond storage).
    - getState(pluginName) returns PluginState from internal map, or "error" if not found.
    - activate(pluginName) sets state to "active" for the named plugin. Throws if not found.

    sdk-bridge:
    - connect(config) stores config, sets connected=true. Returns resolved Promise.
    - send(message) serializes to NDJSON: `JSON.stringify(message) + "\n"`. Stores in outbound buffer array.
    - onMessage(handler) registers handler in handlers array.
    - handleControlRequest(req) returns true if requiresApproval=false, simulates approval prompt returning true for requiresApproval=true. Returns Promise<boolean>.
    - disconnect() sets connected=false, clears handlers and buffer.
  </behavior>
  <action>
    Follow TDD workflow.

    **plugin-lifecycle-manager:**
    1. Write plugin-lifecycle-manager.test.ts with 8+ tests.
    2. Implement in index.ts:
       - Private fields: `private config: PluginLifecycleConfig`, `private plugins = new Map<string, { manifest: PluginManifest; state: PluginState }>()`
       - constructor: store config, init empty plugins Map
       - discover: create 2 simulated manifests (name: "test-plugin-1", "test-plugin-2"). For each, set in plugins Map with state "discovered". Return manifests array.
       - loadFromCache: create 1 simulated cached manifest (name: "cached-plugin"). Set state "cached". Return array.
       - cleanupOrphaned: `async` — iterate plugins, collect entries with state "orphaned", delete them, return count.
       - logSessionPlugins: store active array in private field `private activePlugins: PluginManifest[] = []`
       - getState: return plugins.get(name)?.state ?? "error"
       - activate: get entry from map. If not found, throw Error. Set state to "active".
    3. Remove underscore prefixes. Preserve all type/interface exports exactly.

    **sdk-bridge:**
    1. Write sdk-bridge.test.ts with 7+ tests.
    2. Implement in index.ts:
       - Private fields: `private connected = false`, `private config: SessionConfig | null = null`, `private handlers: Array<(msg: SDKMessage) => void> = []`, `private outbound: string[] = []`
       - connect: store config, set connected=true, return Promise.resolve()
       - send: if not connected, throw. Push `JSON.stringify(message) + "\n"` to outbound.
       - onMessage: push handler to handlers array
       - handleControlRequest: if !requiresApproval return true. Otherwise simulate approval: return Promise.resolve(true).
       - disconnect: set connected=false, clear handlers array (length=0), clear outbound (length=0), set config=null.
       - Export `getOutbound(): string[]` for test access to outbound buffer.
    3. Remove underscore prefixes. Preserve all type/interface exports.

    Verify: `cd packages/build/plugin-lifecycle-manager && bun test` and `cd packages/build/sdk-bridge && bun test`
  </action>
  <verify>
    <automated>cd packages/build/plugin-lifecycle-manager && bun test && cd ../../build/sdk-bridge && bun test</automated>
  </verify>
  <acceptance_criteria>
    - packages/build/plugin-lifecycle-manager/src/index.ts contains "new Map" and "async cleanupOrphaned" and does NOT contain "throw new Error(\"TODO"
    - packages/build/plugin-lifecycle-manager/src/plugin-lifecycle-manager.test.ts contains at least 7 "test("
    - packages/build/sdk-bridge/src/index.ts contains "JSON.stringify" and "connected" and does NOT contain "throw new Error(\"TODO"
    - packages/build/sdk-bridge/src/sdk-bridge.test.ts contains at least 6 "test("
    - Both test suites exit 0
  </acceptance_criteria>
  <done>plugin-lifecycle-manager and sdk-bridge have working implementations with passing TDD tests, zero TODO throws</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement onboarding-flow-engine</name>
  <files>
    packages/build/onboarding-flow-engine/src/index.ts,
    packages/build/onboarding-flow-engine/src/onboarding-flow-engine.test.ts
  </files>
  <read_first>
    packages/build/onboarding-flow-engine/src/index.ts,
    packages/build/plugin-lifecycle-manager/src/plugin-lifecycle-manager.test.ts
  </read_first>
  <behavior>
    - constructor(config) stores config, initializes OnboardingState with currentStep="", completedSteps=[], skippedSteps=[], errors=[]
    - assembleSteps(config) filters out steps where shouldSkip() returns true, validates that all dependsOn step IDs exist in remaining steps (throws if missing dep)
    - runStep(step) calls step.execute(), on success records in completedSteps, on error records in errors array. Returns StepResult.
    - run() assembles steps, executes each in order. For each: if shouldSkip(), add to skippedSteps. Otherwise call runStep(). Updates currentStep. Returns final OnboardingState.
    - getState() returns current OnboardingState snapshot (copy)
    - skipTo(stepId) finds step index, marks all steps before it as skipped in skippedSteps, sets currentStep to stepId
  </behavior>
  <action>
    Follow TDD workflow.

    1. Write onboarding-flow-engine.test.ts with 8+ tests. Create mock OnboardingStep objects with:
       - id, label, execute: () => Promise.resolve({success: true, nextStep: null, error: null}), shouldSkip: () => false, dependsOn: []
       - At least one step with shouldSkip: () => true
       - At least one step with dependsOn referencing another step
       - At least one step whose execute returns error

    2. Implement in index.ts:
       - Private fields: `private config: OnboardingConfig`, `private state: OnboardingState`, `private assembledSteps: OnboardingStep[] = []`
       - constructor: store config, init state = { currentStep: "", completedSteps: [], skippedSteps: [], errors: [] }
       - assembleSteps: filter config.steps by !step.shouldSkip(). Then validate deps: for each remaining step, check all dependsOn IDs exist in the filtered list. If missing, throw Error(`Missing dependency: ${depId} for step ${step.id}`). Return filtered+validated array.
       - runStep: try { const result = await step.execute(); if (result.success) { state.completedSteps.push(step.id); } else { state.errors.push({stepId: step.id, error: result.error ?? "unknown"}); } state.currentStep = step.id; return result; } catch(e) { state.errors.push({stepId: step.id, error: String(e)}); return {success: false, nextStep: null, error: String(e)}; }
       - run: assembledSteps = this.assembleSteps(this.config). For each step in order: if step.shouldSkip(), add to skippedSteps. Else await runStep(step). Return {...this.state} (copy).
       - getState: return { ...this.state, completedSteps: [...this.state.completedSteps], skippedSteps: [...this.state.skippedSteps], errors: [...this.state.errors] }
       - skipTo: find index of stepId in assembledSteps (or config.steps). Mark all steps before that index as skipped. Set currentStep = stepId.
    3. Remove underscore prefixes. Preserve all type/interface exports.

    Verify: `cd packages/build/onboarding-flow-engine && bun test`
  </action>
  <verify>
    <automated>cd packages/build/onboarding-flow-engine && bun test</automated>
  </verify>
  <acceptance_criteria>
    - packages/build/onboarding-flow-engine/src/index.ts contains "class OnboardingFlowEngine" and "assembleSteps" and "completedSteps"
    - packages/build/onboarding-flow-engine/src/index.ts does NOT contain "throw new Error(\"TODO"
    - packages/build/onboarding-flow-engine/src/onboarding-flow-engine.test.ts contains at least 7 "test("
    - Test suite exits 0
  </acceptance_criteria>
  <done>onboarding-flow-engine has working implementation with dependency ordering, skip support, and passing TDD tests</done>
</task>

</tasks>

<verification>
All 3 packages compile and tests pass:
```bash
cd packages/build/plugin-lifecycle-manager && bun test
cd packages/build/sdk-bridge && bun test
cd packages/build/onboarding-flow-engine && bun test
```
</verification>

<success_criteria>
- 3 packages implemented with zero TODO throws
- All test suites pass
- cleanupOrphaned is async and returns Promise<number>
- OnboardingFlowEngine validates dependency order
</success_criteria>

<output>
After completion, create `.planning/phases/11-wave6-expansion-implementations/11-03-SUMMARY.md`
</output>
