---
phase: 11-wave6-expansion-implementations
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/build/tool-schema-cache/src/index.ts
  - packages/build/tool-schema-cache/src/tool-schema-cache.test.ts
  - packages/build/tool-registry/src/index.ts
  - packages/build/tool-registry/src/tool-registry.test.ts
  - packages/build/voice-input-gating/src/index.ts
  - packages/build/voice-input-gating/src/voice-input-gating.test.ts
autonomous: true
requirements: [W6-04, W6-05, W6-10, NFR-01, NFR-03]

must_haves:
  truths:
    - "ToolSchemaCache.get returns cached schema by name, undefined for unknown"
    - "ToolSchemaCache.refresh bulk-loads tools with cachedAt timestamps"
    - "ToolSchemaCache.getStableSchemaList returns schemas sorted by name"
    - "assembleToolPool merges built-in and external tools then filters by deny rules"
    - "filterToolsByDenyRules removes tools whose name matches any deny rule string"
    - "sortForCacheStability sorts tools alphabetically by name"
    - "registerTool adds to module-level registry, getAllTools retrieves all"
    - "checkVoiceGating runs 3 layers in order and fails fast on first denial"
    - "compositeGateCheck evaluates ordered gate layers with short-circuit"
  artifacts:
    - path: "packages/build/tool-schema-cache/src/index.ts"
      provides: "Map-based tool schema cache"
      contains: "class ToolSchemaCache"
    - path: "packages/build/tool-registry/src/index.ts"
      provides: "Three-layer tool assembly"
      contains: "function assembleToolPool"
    - path: "packages/build/voice-input-gating/src/index.ts"
      provides: "Three-layer voice input gating"
      contains: "function checkVoiceGating"
  key_links: []
---

<objective>
Implement 3 simple build-tier TS packages: tool-schema-cache, tool-registry, voice-input-gating. All standalone with no cross-package dependencies.

Purpose: Convert type stubs to working implementations for tool caching, tool assembly, and voice feature gating.
Output: 3 working packages with TDD test suites, zero TODO throws.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-wave1-quick-wins/06-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement tool-schema-cache and tool-registry</name>
  <files>
    packages/build/tool-schema-cache/src/index.ts,
    packages/build/tool-schema-cache/src/tool-schema-cache.test.ts,
    packages/build/tool-registry/src/index.ts,
    packages/build/tool-registry/src/tool-registry.test.ts
  </files>
  <read_first>
    packages/build/tool-schema-cache/src/index.ts,
    packages/build/tool-registry/src/index.ts,
    packages/extract/denial-tracking/src/index.ts,
    packages/extract/denial-tracking/src/denial-tracking.test.ts
  </read_first>
  <behavior>
    tool-schema-cache:
    - get("unknown") returns undefined
    - set("toolA", schema) then get("toolA") returns schema
    - refresh([tool1, tool2]) populates cache; get returns both with cachedAt set to Date.now()
    - invalidate() clears all; get returns undefined after
    - getStableSchemaList() returns schemas sorted alphabetically by name

    tool-registry:
    - registerTool(tool) adds to module-level array; getAllTools() returns it
    - filterToolsByDenyRules([toolA, toolB], ["toolA"]) returns only toolB
    - sortForCacheStability([toolB, toolA]) returns [toolA, toolB] (sorted by name)
    - assembleToolPool(builtIn, external, denyRules) merges both arrays, filters by deny, returns ToolPool with getByName and filter
    - getAllTools after fresh import returns empty array (module-level state starts empty)
  </behavior>
  <action>
    Follow TDD workflow established in Phase 6.

    **tool-schema-cache:**
    1. Write tool-schema-cache.test.ts with 5+ tests covering get/set/refresh/invalidate/getStableSchemaList.
    2. Implement in index.ts:
       - Add private field: `private cache = new Map<string, CachedToolSchema>();`
       - get: `return this.cache.get(toolName);`
       - set: `this.cache.set(toolName, schema);`
       - refresh: clear cache, iterate tools, set each with `{ ...tool, cachedAt: Date.now() }`
       - invalidate: `this.cache.clear();`
       - getStableSchemaList: `return [...this.cache.values()].sort((a, b) => a.name.localeCompare(b.name));`
    3. Remove all underscore prefixes from parameter names. Preserve all type/interface exports exactly.

    **tool-registry:**
    1. Write tool-registry.test.ts with 6+ tests. Use beforeEach to reset module-level state.
    2. Implement in index.ts:
       - Add module-level: `const registry: Tool[] = [];`
       - Export `function resetRegistry(): void { registry.length = 0; }` for test isolation
       - registerTool: `registry.push(tool);`
       - getAllTools: `return [...registry];`
       - filterToolsByDenyRules: `return tools.filter(t => !denyRules.includes(t.name));`
       - sortForCacheStability: `return [...tools].sort((a, b) => a.name.localeCompare(b.name));`
       - assembleToolPool: merge builtIn+external, filter by deny rules, sort for stability, return ToolPool object with tools array, getByName using find(), filter using Array.filter()
    3. Remove all underscore prefixes. Preserve all type/interface exports exactly.

    Verify: `cd packages/build/tool-schema-cache && bun test` and `cd packages/build/tool-registry && bun test`
  </action>
  <verify>
    <automated>cd packages/build/tool-schema-cache && bun test && cd ../../build/tool-registry && bun test</automated>
  </verify>
  <acceptance_criteria>
    - packages/build/tool-schema-cache/src/index.ts contains "this.cache.get(toolName)" and "this.cache.set(toolName"
    - packages/build/tool-schema-cache/src/index.ts does NOT contain "throw new Error"
    - packages/build/tool-schema-cache/src/tool-schema-cache.test.ts contains "describe(" and at least 5 "test("
    - packages/build/tool-registry/src/index.ts contains "registry.push(tool)" and "denyRules.includes"
    - packages/build/tool-registry/src/index.ts does NOT contain "throw new Error"
    - packages/build/tool-registry/src/tool-registry.test.ts contains "describe(" and at least 5 "test("
    - Both test suites exit 0
  </acceptance_criteria>
  <done>tool-schema-cache and tool-registry have working implementations with passing TDD tests, zero TODO throws</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement voice-input-gating</name>
  <files>
    packages/build/voice-input-gating/src/index.ts,
    packages/build/voice-input-gating/src/voice-input-gating.test.ts
  </files>
  <read_first>
    packages/build/voice-input-gating/src/index.ts,
    packages/build/tool-schema-cache/src/tool-schema-cache.test.ts
  </read_first>
  <behavior>
    - checkRemoteFlag("enabled_flag") returns true (simulated: always true)
    - checkRemoteFlag("disabled_flag") returns false when flag not in simulated set
    - checkAuthentication("api_key") returns true (simulated)
    - checkRuntimeSupport("darwin") returns true for supported platforms
    - checkRuntimeSupport("unsupported") returns false
    - checkVoiceGating(config) returns {allowed: true, deniedBy: null, reason: "all gates passed"} when all 3 layers pass
    - checkVoiceGating with unsupported platform returns {allowed: false, deniedBy: "runtime", reason: ...}
    - compositeGateCheck(["remote_flag", "authentication", "runtime"]) evaluates in order, fails fast
  </behavior>
  <action>
    Follow TDD workflow.

    1. Write voice-input-gating.test.ts with 7+ tests covering all 5 exported functions.
    2. Implement in index.ts:
       - checkRemoteFlag: simulated lookup — use a module-level Set of enabled flags. Default: contains the config's featureFlagKey. Return `enabledFlags.has(flagKey)`.
       - checkAuthentication: simulated — return true (always authenticated in reference implementation)
       - checkRuntimeSupport: check `platform` against a hardcoded supported list `["darwin", "linux", "win32"]`
       - compositeGateCheck: iterate gates array, for each gate call the corresponding check function. On first false, return `{ allowed: false, deniedBy: gate, reason: "..." }`. If all pass, return `{ allowed: true, deniedBy: null, reason: "all gates passed" }`.
       - checkVoiceGating: call compositeGateCheck with all 3 layers in order: remote_flag (using config.featureFlagKey), authentication (using config.requiredAuthType), runtime (checking config.supportedPlatforms includes current simulated platform).
       - Export `resetGatingState()` for test isolation if needed.
    3. Remove all underscore prefixes. Preserve all type/interface exports exactly.

    Verify: `cd packages/build/voice-input-gating && bun test`
  </action>
  <verify>
    <automated>cd packages/build/voice-input-gating && bun test</automated>
  </verify>
  <acceptance_criteria>
    - packages/build/voice-input-gating/src/index.ts contains "function checkVoiceGating" and "function compositeGateCheck"
    - packages/build/voice-input-gating/src/index.ts does NOT contain "throw new Error"
    - packages/build/voice-input-gating/src/voice-input-gating.test.ts contains "describe(" and at least 6 "test("
    - Test suite exits 0
  </acceptance_criteria>
  <done>voice-input-gating has working implementation with passing TDD tests, zero TODO throws</done>
</task>

</tasks>

<verification>
All 3 packages compile and tests pass:
```bash
cd packages/build/tool-schema-cache && bun test
cd packages/build/tool-registry && bun test
cd packages/build/voice-input-gating && bun test
```
</verification>

<success_criteria>
- 3 packages implemented with zero TODO throws
- All test suites pass
- All type/interface exports preserved exactly from stubs
</success_criteria>

<output>
After completion, create `.planning/phases/11-wave6-expansion-implementations/11-01-SUMMARY.md`
</output>
