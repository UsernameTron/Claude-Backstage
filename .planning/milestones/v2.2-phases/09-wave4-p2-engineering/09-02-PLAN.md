---
phase: 09-wave4-p2-engineering
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/build/mcp-integration/src/index.ts
  - packages/build/mcp-integration/src/mcp-integration.test.ts
  - packages/build/cli-startup-optimization/src/index.ts
  - packages/build/cli-startup-optimization/src/cli-startup-optimization.test.ts
autonomous: true
requirements: [W4-07, W4-08, NFR-01, NFR-03]

must_haves:
  truths:
    - "connectToServer() returns McpConnection with discovered tools and resources"
    - "getMcpToolsCommandsAndResources() aggregates tools/resources from multiple connections"
    - "main() runs ordered startup phases and enters ready state"
    - "registerLazyModule() defers non-critical modules based on priority"
  artifacts:
    - path: "packages/build/mcp-integration/src/index.ts"
      provides: "MCP server connection and tool proxying"
      contains: "function connectToServer"
    - path: "packages/build/cli-startup-optimization/src/index.ts"
      provides: "CLI startup phases with lazy loading"
      contains: "function main"
  key_links: []
---

<objective>
Implement 2 build-tier packages: mcp-integration and cli-startup-optimization. These are standalone packages with no cross-package dependencies.

Purpose: Provide MCP server management and CLI startup optimization patterns.
Output: 2 working build packages with TDD test suites, zero TODO throws.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-wave1-quick-wins/06-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement mcp-integration</name>
  <files>
    packages/build/mcp-integration/src/index.ts,
    packages/build/mcp-integration/src/mcp-integration.test.ts
  </files>
  <read_first>
    packages/build/mcp-integration/src/index.ts,
    packages/build/multi-agent-coordinator/src/index.ts,
    packages/build/multi-agent-coordinator/src/multi-agent-coordinator.test.ts
  </read_first>
  <behavior>
    - connectToServer({scope:"local",serverName:"test",config:{type:"stdio",command:"node",args:[]}}) returns McpConnection with status "connected"
    - connectToServer with invalid config returns McpConnection with status "error"
    - getMcpToolsCommandsAndResources([conn1, conn2]) merges tools and resources arrays
    - getMcpToolsCommandsAndResources([]) returns empty tools and resources
    - disconnectServer(conn) sets status to "disconnected"
    - proxyToolCall(conn, "toolName", {arg:1}) returns tool result
    - proxyToolCall on disconnected connection throws
    - All type interfaces (McpTransport, StdioMcpServerConfig, HttpMcpServerConfig, etc.) remain unchanged
  </behavior>
  <action>
    Follow TDD workflow.

    1. Write mcp-integration.test.ts testing connectToServer (stdio, http, sse configs), getMcpToolsCommandsAndResources aggregation, disconnectServer state change, proxyToolCall success and failure.
    2. Implement connectToServer: validate config has required fields. For pattern lib, simulate connection (no real I/O). Return McpConnection with serverName, empty tools/resources arrays (populated by discovery), status "connected". For invalid configs (missing command for stdio, missing url for http), return status "error".
    3. Implement getMcpToolsCommandsAndResources: flatMap connections' tools and resources arrays into single aggregate. Filter out connections with status !== "connected".
    4. Implement disconnectServer: set connection status to "disconnected", clear tools/resources arrays.
    5. Implement proxyToolCall: check connection.status === "connected", throw if not. Find tool by name in connection.tools, throw if not found. Return simulated result object (pattern lib — no real MCP protocol).

    Verify: bun test + tsc.
  </action>
  <verify>
    <automated>$HOME/.bun/bin/bun test packages/build/mcp-integration && npx tsc --noEmit -p packages/build/mcp-integration/tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "TODO" packages/build/mcp-integration/src/index.ts returns 0
    - mcp-integration.test.ts exists with at least 8 tests
    - bun test passes
    - tsc --noEmit passes
  </acceptance_criteria>
  <done>mcp-integration has zero TODO throws, all tests pass, tsc clean</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement cli-startup-optimization</name>
  <files>
    packages/build/cli-startup-optimization/src/index.ts,
    packages/build/cli-startup-optimization/src/cli-startup-optimization.test.ts
  </files>
  <read_first>
    packages/build/cli-startup-optimization/src/index.ts,
    packages/build/agent-dialogue-loop/src/index.ts,
    packages/build/agent-dialogue-loop/src/agent-dialogue-loop.test.ts
  </read_first>
  <behavior>
    - main() runs through all 6 startup phases in order (pre_init -> config_load -> auth_check -> mcp_connect -> prompt_assemble -> ready)
    - setup() runs config_load, auth_check, mcp_connect phases
    - setup({bareMode:true}) skips mcp_connect phase
    - registerLazyModule({name,load,priority:"critical"}) registers for immediate load
    - registerLazyModule({name,load,priority:"deferred"}) registers for post-ready load
    - registerLazyModule({name,load,priority:"idle"}) registers for idle-time load
    - getStartupMetrics() returns timing data with totalMs and per-phase breakdown
    - getStartupMetrics() returns lazyModulesDeferred count
  </behavior>
  <action>
    Follow TDD workflow.

    1. Write cli-startup-optimization.test.ts testing main lifecycle, setup with various configs, registerLazyModule priority handling, getStartupMetrics timing data.
    2. Use module-level state: registeredModules Map<string, LazyModule>, startupMetrics object, currentPhase tracker.
    3. Implement main: record start time. Run phases in order: pre_init (parse args), config_load (load config), auth_check (verify credentials), mcp_connect (connect MCP servers), prompt_assemble (build system prompt), ready. Record timing per phase. Load critical modules during pre_init, schedule deferred modules after ready. In pattern lib, each phase is simulated (no real I/O) — just record phase entry/exit times.
    4. Implement setup: run config_load, auth_check. If !config.bareMode, run mcp_connect. Skip pre_init and prompt_assemble (those are main-only). Record timing.
    5. Implement registerLazyModule: store in registeredModules map. If priority is "critical", call load() immediately. Otherwise store for later scheduling.
    6. Implement getStartupMetrics: return { totalMs, phases (per-phase timing), lazyModulesDeferred: count of non-critical registered modules }.

    Verify: bun test + tsc.
  </action>
  <verify>
    <automated>$HOME/.bun/bin/bun test packages/build/cli-startup-optimization && npx tsc --noEmit -p packages/build/cli-startup-optimization/tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "TODO" packages/build/cli-startup-optimization/src/index.ts returns 0
    - cli-startup-optimization.test.ts exists with at least 8 tests
    - bun test passes
    - tsc --noEmit passes
  </acceptance_criteria>
  <done>cli-startup-optimization has zero TODO throws, all tests pass, tsc clean</done>
</task>

</tasks>

<verification>
- Both packages compile: npx tsc --noEmit for each
- All tests pass: $HOME/.bun/bin/bun test for each
- Zero TODO throws: grep -r "TODO" across both src/index.ts files returns nothing
</verification>

<success_criteria>
- 2 build packages implemented with working logic
- All type signatures preserved from stubs
- TDD test suites for both packages
- No downstream consumers — these are leaf packages
</success_criteria>

<output>
After completion, create `.planning/phases/09-wave4-p2-engineering/09-02-SUMMARY.md`
</output>
