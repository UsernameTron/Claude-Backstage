---
phase: 08-wave3-p1-portfolio
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/extract/token-estimation/src/index.ts
  - packages/extract/token-estimation/src/token-estimation.test.ts
  - packages/extract/streaming-tool-executor/src/index.ts
  - packages/extract/streaming-tool-executor/src/streaming-tool-executor.test.ts
  - packages/extract/state-store/src/index.ts
  - packages/extract/state-store/src/state-store.test.ts
  - packages/extract/claudemd-memory/src/index.ts
  - packages/extract/claudemd-memory/src/claudemd-memory.test.ts
autonomous: true
requirements: [W3-01, W3-02, W3-03, W3-06, NFR-01, NFR-03]

must_haves:
  truths:
    - "token-estimation roughTokenCountEstimation returns ~text.length/4"
    - "streaming-tool-executor addTool tracks concurrent promises and getCompletedResults returns resolved"
    - "state-store createStore provides getState/setState/subscribe with Object.is equality check"
    - "claudemd-memory getMemoryFiles returns 4-tier hierarchy (managed, user, project, local)"
  artifacts:
    - path: "packages/extract/token-estimation/src/index.ts"
      provides: "5 token estimation functions"
      contains: "roughTokenCountEstimation"
    - path: "packages/extract/streaming-tool-executor/src/index.ts"
      provides: "StreamingToolExecutor class"
      contains: "class StreamingToolExecutor"
    - path: "packages/extract/state-store/src/index.ts"
      provides: "createStore factory with reactive subscriptions"
      contains: "function createStore"
    - path: "packages/extract/claudemd-memory/src/index.ts"
      provides: "4-tier CLAUDE.md memory loading"
      contains: "function getMemoryFiles"
  key_links:
    - from: "packages/extract/auto-compact/src/index.ts"
      to: "packages/extract/token-estimation/src/index.ts"
      via: "workspace:* dependency"
      pattern: "import.*token-estimation"
    - from: "packages/build/agent-dialogue-loop/src/index.ts"
      to: "packages/extract/state-store/src/index.ts"
      via: "workspace:* dependency"
      pattern: "import.*state-store"
---

<objective>
Implement 4 standalone extract-tier packages: token-estimation, streaming-tool-executor, state-store, and claudemd-memory. These are Wave 1 foundations that Wave 2 packages depend on.

Purpose: Provide the core extract-tier implementations that auto-compact, agent-dialogue-loop, and skills-system will import in Plan 08-04.
Output: 4 working extract packages with TDD test suites, zero TODO throws.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-wave1-quick-wins/06-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement token-estimation and streaming-tool-executor</name>
  <files>
    packages/extract/token-estimation/src/index.ts,
    packages/extract/token-estimation/src/token-estimation.test.ts,
    packages/extract/streaming-tool-executor/src/index.ts,
    packages/extract/streaming-tool-executor/src/streaming-tool-executor.test.ts
  </files>
  <read_first>
    packages/extract/token-estimation/src/index.ts,
    packages/extract/streaming-tool-executor/src/index.ts,
    packages/extract/cost-tracker/src/cost-tracker.test.ts,
    packages/extract/cost-tracker/src/index.ts
  </read_first>
  <behavior>
    token-estimation:
    - roughTokenCountEstimation("hello world") returns Math.ceil(11/4) = 3
    - roughTokenCountEstimationForMessages([{role:"user",content:"test"}]) counts content tokens
    - bytesPerTokenForFileType(".ts") returns a number > 0
    - countTokensWithAPI returns rough estimate (no real API)
    - tokenCountWithEstimation delegates to rough estimation

    streaming-tool-executor:
    - new StreamingToolExecutor() creates empty executor
    - addTool(definition, promise) tracks a pending tool
    - getCompletedResults() returns results from resolved promises
    - getRemainingResults() awaits all pending and returns results
    - getUpdatedContext() returns accumulated context from tool results
  </behavior>
  <action>
    Follow TDD workflow from Phase 6 (see 06-01-SUMMARY.md pattern).

    token-estimation:
    1. Write token-estimation.test.ts with tests for all 5 exported functions.
    2. Implement roughTokenCountEstimation: return Math.ceil(text.length / 4). This is the ~4 chars/token heuristic from the KB.
    3. Implement roughTokenCountEstimationForMessages: sum roughTokenCountEstimation across message contents plus overhead per message (~4 tokens for role/structure).
    4. Implement bytesPerTokenForFileType: return lookup map of common extensions (.ts=3.5, .py=3.8, .json=4.0, .md=4.5) with default 4.0.
    5. Implement countTokensWithAPI: delegate to roughTokenCountEstimation (no actual API in pattern lib). Format messages to string, estimate.
    6. Implement tokenCountWithEstimation: call roughTokenCountEstimationForMessages.

    streaming-tool-executor:
    1. Write streaming-tool-executor.test.ts testing addTool, getCompletedResults, getRemainingResults, getUpdatedContext.
    2. Implement StreamingToolExecutor class with private pendingTools: Map<string, Promise<ToolResult>> and completedResults: ToolResult[].
    3. addTool: store promise in pendingTools map, attach .then() to move to completedResults on resolve.
    4. getCompletedResults: return shallow copy of completedResults array.
    5. getRemainingResults: await Promise.all remaining pending, return all results.
    6. getUpdatedContext: concatenate result outputs into context string.

    Verify each package: $HOME/.bun/bin/bun test <pkg-path> and npx tsc --noEmit -p <pkg-path>/tsconfig.json
  </action>
  <verify>
    <automated>$HOME/.bun/bin/bun test packages/extract/token-estimation && $HOME/.bun/bin/bun test packages/extract/streaming-tool-executor && npx tsc --noEmit -p packages/extract/token-estimation/tsconfig.json && npx tsc --noEmit -p packages/extract/streaming-tool-executor/tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "TODO" packages/extract/token-estimation/src/index.ts returns 0
    - grep -c "TODO" packages/extract/streaming-tool-executor/src/index.ts returns 0
    - token-estimation.test.ts exists with at least 8 tests
    - streaming-tool-executor.test.ts exists with at least 6 tests
    - bun test passes for both packages
    - tsc --noEmit passes for both packages
  </acceptance_criteria>
  <done>Both packages have zero TODO throws, all tests pass, tsc clean</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement state-store and claudemd-memory</name>
  <files>
    packages/extract/state-store/src/index.ts,
    packages/extract/state-store/src/state-store.test.ts,
    packages/extract/claudemd-memory/src/index.ts,
    packages/extract/claudemd-memory/src/claudemd-memory.test.ts
  </files>
  <read_first>
    packages/extract/state-store/src/index.ts,
    packages/extract/claudemd-memory/src/index.ts,
    packages/extract/denial-tracking/src/index.ts
  </read_first>
  <behavior>
    state-store:
    - createStore({count:0}) returns Store with getState/setState/subscribe
    - getState() returns shallow copy (Object.is fails against internal)
    - setState({count:1}) updates state, notifies listeners
    - subscribe(fn) returns unsubscribe function
    - Listener only fires when Object.is(oldState, newState) is false

    claudemd-memory:
    - MAX_MEMORY_CHARACTER_COUNT equals 40000
    - getMemoryFiles("/some/dir") returns MemoryFile[] sorted by tier priority
    - getClaudeMds("/dir") returns array of found CLAUDE.md file paths
    - processMemoryFile("/path/CLAUDE.md") returns MemoryFile with content and tier
    - resolveIncludes handles @include directives in memory file content
  </behavior>
  <action>
    Follow TDD workflow.

    state-store:
    1. Write state-store.test.ts with tests for createStore, getState, setState, subscribe, unsubscribe, Object.is equality.
    2. Implement createStore<T>(initialState: T): Store<T>.
       - Internal state variable. listeners Set<Listener>.
       - getState: return { ...state } (shallow copy for flat objects, per Phase 6 pattern).
       - setState: if Object.is(state, newState) return early. Otherwise update state, notify all listeners.
       - subscribe: add listener to Set, return () => listeners.delete(listener).

    claudemd-memory:
    1. Write claudemd-memory.test.ts with tests for all 4 functions + MAX_MEMORY_CHARACTER_COUNT constant.
    2. getMemoryFiles: simulate 4-tier hierarchy lookup (managed: /Library/.../CLAUDE.md, user: ~/.claude/CLAUDE.md, project: ./CLAUDE.md, local: ./.claude/CLAUDE.md). Return MemoryFile[] with tier, path, content. In pattern lib, use simulated file reads (no real filesystem).
    3. getClaudeMds: walk up from startDir looking for CLAUDE.md files. Pattern implementation: return array of paths that would be found.
    4. processMemoryFile: read file path, extract frontmatter if present, return MemoryFile.
    5. resolveIncludes: scan content for @include directives, recursively resolve. Track visited paths to prevent cycles.

    Verify: $HOME/.bun/bin/bun test <pkg-path> and tsc for both.
  </action>
  <verify>
    <automated>$HOME/.bun/bin/bun test packages/extract/state-store && $HOME/.bun/bin/bun test packages/extract/claudemd-memory && npx tsc --noEmit -p packages/extract/state-store/tsconfig.json && npx tsc --noEmit -p packages/extract/claudemd-memory/tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "TODO" packages/extract/state-store/src/index.ts returns 0
    - grep -c "TODO" packages/extract/claudemd-memory/src/index.ts returns 0
    - state-store.test.ts exists with at least 6 tests
    - claudemd-memory.test.ts exists with at least 6 tests
    - bun test passes for both packages
    - tsc --noEmit passes for both packages
  </acceptance_criteria>
  <done>Both packages have zero TODO throws, all tests pass, tsc clean</done>
</task>

</tasks>

<verification>
- All 4 packages compile: npx tsc --noEmit for each
- All tests pass: $HOME/.bun/bin/bun test for each
- Zero TODO throws: grep -r "TODO" across all 4 src/index.ts files returns nothing
</verification>

<success_criteria>
- 4 extract packages implemented with working logic
- All type signatures preserved from stubs
- TDD test suites for all 4 packages
- Ready for Wave 2 consumers (auto-compact, agent-dialogue-loop, skills-system)
</success_criteria>

<output>
After completion, create `.planning/phases/08-wave3-p1-portfolio/08-01-SUMMARY.md`
</output>
