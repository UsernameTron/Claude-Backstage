---
phase: 08-wave3-p1-portfolio
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/build/multi-agent-coordinator/src/index.ts
  - packages/build/multi-agent-coordinator/src/multi-agent-coordinator.test.ts
  - packages/translate/agent-skill-routing/src/agent_skill_routing/__init__.py
  - packages/translate/agent-skill-routing/src/agent_skill_routing/test_routing.py
autonomous: true
requirements: [W3-07, W3-10, NFR-01, NFR-02, NFR-03]

must_haves:
  truths:
    - "multi-agent-coordinator isCoordinatorMode returns boolean based on config"
    - "multi-agent-coordinator dispatchTask returns resolved promise with status"
    - "agent-skill-routing evaluate_rules applies deny>ask>allow cascade"
    - "agent-skill-routing load_rules returns list of RoutingRule dataclasses"
  artifacts:
    - path: "packages/build/multi-agent-coordinator/src/index.ts"
      provides: "4 coordinator functions with mcp-integration type imports"
      contains: "function dispatchTask"
    - path: "packages/translate/agent-skill-routing/src/agent_skill_routing/__init__.py"
      provides: "Python routing with deny>ask>allow cascade"
      contains: "def evaluate_rules"
  key_links:
    - from: "packages/build/multi-agent-coordinator/src/index.ts"
      to: "packages/build/mcp-integration/src/index.ts"
      via: "workspace:* type-only import"
      pattern: "import.*mcp-integration"
---

<objective>
Implement multi-agent-coordinator (build, type-only dep on mcp-integration stub) and agent-skill-routing (translate/Python, standalone).

Purpose: Complete the build-tier coordinator and Python translate package. Multi-agent-coordinator uses types from mcp-integration (Phase 9 stub) which already resolve.
Output: 1 TS build package + 1 Python translate package, both with test suites.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-wave1-quick-wins/06-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement multi-agent-coordinator with mcp-integration type imports</name>
  <files>
    packages/build/multi-agent-coordinator/src/index.ts,
    packages/build/multi-agent-coordinator/src/multi-agent-coordinator.test.ts
  </files>
  <read_first>
    packages/build/multi-agent-coordinator/src/index.ts,
    packages/build/multi-agent-coordinator/package.json,
    packages/build/mcp-integration/src/index.ts
  </read_first>
  <behavior>
    - isCoordinatorMode() returns true when coordinator config is active
    - getCoordinatorSystemPrompt(config) returns string with coordinator instructions
    - getCoordinatorUserContext(config) returns string with agent context
    - dispatchTask(task) returns Promise<CoordinatorNotification> with completed status
    - Type imports from mcp-integration resolve (stub types sufficient)
  </behavior>
  <action>
    1. Read multi-agent-coordinator stub for full type signatures (CoordinatorConfig, AgentTask, CoordinatorNotification).
    2. Read mcp-integration stub to understand imported types (likely McpServerConfig or similar).
    3. Write multi-agent-coordinator.test.ts:
       - Test isCoordinatorMode returns boolean
       - Test getCoordinatorSystemPrompt returns non-empty string containing "coordinator"
       - Test getCoordinatorUserContext returns context string
       - Test dispatchTask resolves with CoordinatorNotification having status field
       - Test dispatchTask with different task types
    4. Implement isCoordinatorMode: check for module-level coordinatorConfig state. Return true if config is set with active: true. Default false.
    5. Implement getCoordinatorSystemPrompt: assemble prompt string from config.agentNames and config.taskDescription. Return template with coordinator instructions.
    6. Implement getCoordinatorUserContext: format agent list and capabilities from config into context string.
    7. Implement dispatchTask: async function that simulates task dispatch. Return { taskId: task.id, status: "completed", agentName: task.targetAgent, result: "Task dispatched successfully" }. This is a pattern implementation — real MCP dispatch would happen in production.

    Do NOT attempt to import runtime implementations from mcp-integration. Only type imports.
    Preserve all type signatures. Only replace TODO throw bodies.
  </action>
  <verify>
    <automated>$HOME/.bun/bin/bun test packages/build/multi-agent-coordinator && npx tsc --noEmit -p packages/build/multi-agent-coordinator/tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "TODO" packages/build/multi-agent-coordinator/src/index.ts returns 0
    - multi-agent-coordinator.test.ts exists with at least 5 tests
    - grep "mcp-integration" packages/build/multi-agent-coordinator/src/index.ts finds import
    - bun test passes
    - tsc --noEmit passes
  </acceptance_criteria>
  <done>multi-agent-coordinator implemented, mcp-integration type imports resolve, all tests pass</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement agent-skill-routing Python package</name>
  <files>
    packages/translate/agent-skill-routing/src/agent_skill_routing/__init__.py,
    packages/translate/agent-skill-routing/src/agent_skill_routing/test_routing.py
  </files>
  <read_first>
    packages/translate/agent-skill-routing/src/agent_skill_routing/__init__.py,
    packages/translate/consecutive-breach-tracker/src/consecutive_breach_tracker/__init__.py,
    packages/translate/consecutive-breach-tracker/src/consecutive_breach_tracker/test_tracker.py
  </read_first>
  <behavior>
    - evaluate_rules(rules, agent) applies deny>ask>allow cascade, first deny match blocks
    - load_rules(config) returns list of RoutingRule dataclasses
    - check_compliance(agent, requirements) verifies agent meets skill requirements
    - RoutingRule dataclass has action (deny/ask/allow), pattern, and priority fields
    - deny rules evaluated first, then ask, then allow (3-factor cascade from permission system)
  </behavior>
  <action>
    1. Read agent-skill-routing __init__.py for full type signatures (RoutingRule, RoutingAction, etc.).
    2. Read consecutive-breach-tracker test and impl for Python translate-tier patterns.
    3. Write test_routing.py following Phase 6 Python test pattern:
       - Test evaluate_rules with deny rule returns "deny"
       - Test evaluate_rules with no deny, ask rule returns "ask"
       - Test evaluate_rules with no deny or ask returns "allow"
       - Test evaluate_rules with empty rules returns "allow" (default)
       - Test load_rules returns list of RoutingRule
       - Test check_compliance with matching skills returns True
       - Test check_compliance with missing skill returns False
    4. Implement evaluate_rules: sort rules by priority. Iterate deny rules first — if any match, return "deny". Then ask rules — if any match, return "ask". Then allow rules — if any match, return "allow". Default "allow" if no rules match.
    5. Implement load_rules: parse config dict into list of RoutingRule dataclasses.
    6. Implement check_compliance: check agent skills dict against requirements list. Return True if all requirements met with sufficient proficiency.

    Use pytest conventions. Follow Phase 6 Python patterns exactly.
  </action>
  <verify>
    <automated>cd "/Users/cpconnor/projects/Inside Claude Code" && python -m pytest packages/translate/agent-skill-routing -v && ruff check packages/translate/agent-skill-routing</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "NotImplementedError\|TODO" packages/translate/agent-skill-routing/src/agent_skill_routing/__init__.py returns 0
    - test_routing.py exists with at least 7 tests
    - pytest passes all tests
    - ruff check passes with no errors
  </acceptance_criteria>
  <done>agent-skill-routing Python package implemented with deny>ask>allow cascade, all tests pass, ruff clean</done>
</task>

</tasks>

<verification>
- TS package compiles: npx tsc --noEmit
- Python package lints: ruff check
- All tests pass for both packages
- Zero TODO/NotImplementedError throws
</verification>

<success_criteria>
- multi-agent-coordinator uses mcp-integration types without runtime dependency
- agent-skill-routing implements the permission cascade pattern in Python
- Both packages have TDD test suites
</success_criteria>

<output>
After completion, create `.planning/phases/08-wave3-p1-portfolio/08-03-SUMMARY.md`
</output>
