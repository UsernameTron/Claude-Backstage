---
phase: 08-wave3-p1-portfolio
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/extract/yolo-classifier/src/index.ts
  - packages/extract/yolo-classifier/src/yolo-classifier.test.ts
  - packages/translate/ivr-call-flow-validator/src/index.ts
  - packages/translate/ivr-call-flow-validator/src/ivr-call-flow-validator.test.ts
autonomous: true
requirements: [W3-04, W3-09, NFR-01, NFR-03, NFR-04]

must_haves:
  truths:
    - "yolo-classifier classifierDecision returns a YoloClassifierResult based on permission mode"
    - "yolo-classifier classifierToPermission maps classifier results to PermissionResult"
    - "ivr-call-flow-validator validate detects unreachable nodes and missing transitions"
    - "ivr-call-flow-validator getUnreachableNodes uses BFS from entryNode"
  artifacts:
    - path: "packages/extract/yolo-classifier/src/index.ts"
      provides: "3 classifier functions importing from permission-system"
      contains: "function classifierDecision"
    - path: "packages/translate/ivr-call-flow-validator/src/index.ts"
      provides: "FSM validation with graph reachability"
      contains: "function validate"
  key_links:
    - from: "packages/extract/yolo-classifier/src/index.ts"
      to: "packages/extract/permission-system/src/index.ts"
      via: "workspace:* import"
      pattern: "import.*permission-system"
---

<objective>
Implement yolo-classifier (extract, depends on Phase 7 permission-system) and ivr-call-flow-validator (translate/TS, standalone FSM validation).

Purpose: Complete the extract-tier packages that depend on Phase 7 outputs and the IVR translate package.
Output: 2 working packages with TDD test suites, cross-package imports verified.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-wave2-core-architecture/07-03-SUMMARY.md
</context>

<interfaces>
<!-- From permission-system (Phase 7, already implemented) -->
From packages/extract/permission-system/src/index.ts:
- export type PermissionMode = "default" | "plan" | "acceptEdits" | "bypassPermissions"
- export interface PermissionResult { decision: "allow" | "deny" | "ask"; reason?: string }
- export function evaluatePermission(...): PermissionResult
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement yolo-classifier with permission-system imports</name>
  <files>
    packages/extract/yolo-classifier/src/index.ts,
    packages/extract/yolo-classifier/src/yolo-classifier.test.ts
  </files>
  <read_first>
    packages/extract/yolo-classifier/src/index.ts,
    packages/extract/yolo-classifier/package.json,
    packages/extract/permission-system/src/index.ts
  </read_first>
  <behavior>
    - classifierDecision(template, context) returns YoloClassifierResult with allow/deny/ask decision
    - classifierShared(context) returns ClassifierSharedContext with permission mode and tool info
    - classifierToPermission(result) maps YoloClassifierResult to PermissionResult (from permission-system types)
    - Cross-package import from @claude-patterns/permission-system resolves at runtime
  </behavior>
  <action>
    1. Read the yolo-classifier stub to understand all type signatures and the permission-system imports.
    2. Read permission-system src/index.ts to understand the types being imported (PermissionMode, PermissionResult, etc.).
    3. Write yolo-classifier.test.ts:
       - Test classifierDecision returns valid YoloClassifierResult for "permissions_external" template
       - Test classifierDecision returns valid result for "permissions_anthropic" template
       - Test classifierShared extracts context correctly
       - Test classifierToPermission maps allow -> {decision:"allow"}, deny -> {decision:"deny"}, ask -> {decision:"ask"}
    4. Implement classifierDecision: async function that evaluates the template type and context. For pattern lib, return a simulated decision based on the template (permissions_external defaults to "ask", permissions_anthropic defaults to "allow"). This is a pattern implementation, not a real API call.
    5. Implement classifierShared: extract permission mode and tool metadata from the shared context parameter.
    6. Implement classifierToPermission: direct mapping from YoloClassifierResult.decision to PermissionResult.

    Preserve all existing type signatures exactly. Only replace TODO throw bodies.
  </action>
  <verify>
    <automated>$HOME/.bun/bin/bun test packages/extract/yolo-classifier && npx tsc --noEmit -p packages/extract/yolo-classifier/tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "TODO" packages/extract/yolo-classifier/src/index.ts returns 0
    - yolo-classifier.test.ts exists with at least 6 tests
    - grep "permission-system" packages/extract/yolo-classifier/src/index.ts finds import statement
    - bun test passes
    - tsc --noEmit passes (verifies cross-package import resolution)
  </acceptance_criteria>
  <done>yolo-classifier has zero TODO throws, imports from permission-system resolve, all tests pass</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement ivr-call-flow-validator with graph reachability</name>
  <files>
    packages/translate/ivr-call-flow-validator/src/index.ts,
    packages/translate/ivr-call-flow-validator/src/ivr-call-flow-validator.test.ts
  </files>
  <read_first>
    packages/translate/ivr-call-flow-validator/src/index.ts,
    packages/translate/prompt-cache-optimizer/src/prompt-cache-optimizer.test.ts
  </read_first>
  <behavior>
    - validate(flow) returns ValidationResult with issues array and isValid boolean
    - validate detects: unreachable nodes, missing DTMF coverage, invalid transitions
    - getUnreachableNodes(flow) uses BFS from entryNode, returns node IDs not visited
    - getTransitionCoverage(node) returns ratio of defined transitions to possible DTMF inputs (0-9,*,#)
    - Empty flow with just entry node validates clean
    - Flow with disconnected node reports unreachable warning
  </behavior>
  <action>
    1. Read ivr-call-flow-validator stub for all type signatures (IVRCallFlow, IVRNode, IVRTransition, DTMFInput, etc.).
    2. Read prompt-cache-optimizer test file for translate-tier test pattern.
    3. Write ivr-call-flow-validator.test.ts:
       - Test validate on simple valid flow (entry -> menu -> exit)
       - Test validate detects unreachable node
       - Test validate detects missing DTMF transitions
       - Test getUnreachableNodes returns disconnected node IDs
       - Test getTransitionCoverage returns correct ratio
       - Test validate with empty single-node flow
    4. Implement getUnreachableNodes: BFS from flow.entryNode. Use Set<string> for visited, queue array. Traverse each node's transitions array and defaultTransition. Return Object.keys(flow.nodes).filter(id => !visited.has(id)).
    5. Implement getTransitionCoverage: count node.transitions.length / 12 (DTMF digits 0-9 plus * and #). Clamp to [0, 1].
    6. Implement validate: run getUnreachableNodes (add "warning" issues for each), check each node's transition coverage (add "warning" if < 0.5), check for nodes with no transitions and no defaultTransition that aren't terminal (add "error"). Return { isValid: errors.length === 0, issues }.

    Preserve all type signatures. Only replace TODO throw bodies.
  </action>
  <verify>
    <automated>$HOME/.bun/bin/bun test packages/translate/ivr-call-flow-validator && npx tsc --noEmit -p packages/translate/ivr-call-flow-validator/tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "TODO" packages/translate/ivr-call-flow-validator/src/index.ts returns 0
    - ivr-call-flow-validator.test.ts exists with at least 6 tests
    - bun test passes
    - tsc --noEmit passes
  </acceptance_criteria>
  <done>ivr-call-flow-validator has zero TODO throws, graph validation works, all tests pass</done>
</task>

</tasks>

<verification>
- Both packages compile: npx tsc --noEmit for each
- All tests pass: $HOME/.bun/bin/bun test for each
- Cross-package import: yolo-classifier successfully imports from permission-system
- Zero TODO throws in both packages
</verification>

<success_criteria>
- yolo-classifier imports and uses permission-system types at compile time
- ivr-call-flow-validator BFS reachability works correctly
- Both packages have TDD test suites
</success_criteria>

<output>
After completion, create `.planning/phases/08-wave3-p1-portfolio/08-02-SUMMARY.md`
</output>
