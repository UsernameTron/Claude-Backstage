---
phase: 06-wave1-quick-wins
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/translate/consecutive-breach-tracker/src/consecutive_breach_tracker/__init__.py
  - packages/translate/cost-per-interaction/src/cost_per_interaction/__init__.py
  - packages/translate/consecutive-breach-tracker/src/consecutive_breach_tracker/test_tracker.py
  - packages/translate/cost-per-interaction/src/cost_per_interaction/test_aggregator.py
autonomous: true
requirements: [W1-03, W1-04, NFR-02, NFR-03, NFR-05, NFR-06]

must_haves:
  truths:
    - "ConsecutiveBreachTracker.record_breach() returns WIDEN_RINGS after 3 consecutive breaches"
    - "ConsecutiveBreachTracker.record_breach() returns FORCE_STAFFING after 20 total breaches"
    - "ConsecutiveBreachTracker.record_recovery() resets consecutive to 0 but preserves total"
    - "ChannelCostAggregator.add_interaction() stores costs per channel"
    - "ChannelCostAggregator.get_cost_per_contact() returns total_cost / total_interactions"
    - "ChannelCostAggregator.format_total_cost() returns '$X.XX' string"
  artifacts:
    - path: "packages/translate/consecutive-breach-tracker/src/consecutive_breach_tracker/__init__.py"
      provides: "ConsecutiveBreachTracker with working implementations"
      contains: "record_breach"
    - path: "packages/translate/cost-per-interaction/src/cost_per_interaction/__init__.py"
      provides: "ChannelCostAggregator with working implementations"
      contains: "add_interaction"
    - path: "packages/translate/consecutive-breach-tracker/src/consecutive_breach_tracker/test_tracker.py"
      provides: "Tests for ConsecutiveBreachTracker"
      min_lines: 20
    - path: "packages/translate/cost-per-interaction/src/cost_per_interaction/test_aggregator.py"
      provides: "Tests for ChannelCostAggregator"
      min_lines: 20
  key_links:
    - from: "packages/translate/consecutive-breach-tracker/src/consecutive_breach_tracker/__init__.py"
      to: "BREACH_THRESHOLDS"
      via: "get_action uses BREACH_THRESHOLDS for escalation decisions"
      pattern: "BREACH_THRESHOLDS"
    - from: "packages/translate/cost-per-interaction/src/cost_per_interaction/__init__.py"
      to: "Channel enum"
      via: "add_interaction and get_summary key by Channel"
      pattern: "Channel\\."
---

<objective>
Implement the two Python translate-tier packages: consecutive-breach-tracker (~80 LOC) and cost-per-interaction (~100 LOC). Replace all NotImplementedError stubs with working code while preserving existing type signatures exactly.

Purpose: These translate Claude Code patterns (denial-tracking and cost-tracker) into contact center domain equivalents, proving the Python implementation workflow.
Output: Two working Python packages with tests, zero NotImplementedError stubs, ruff lint clean, pip install -e works.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@IMPLEMENTATION-PLAYBOOK.md
@packages/translate/consecutive-breach-tracker/src/consecutive_breach_tracker/__init__.py
@packages/translate/cost-per-interaction/src/cost_per_interaction/__init__.py
@packages/translate/consecutive-breach-tracker/README.md
@packages/translate/cost-per-interaction/README.md
@packages/extract/denial-tracking/src/index.ts
@packages/extract/cost-tracker/src/index.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement consecutive-breach-tracker</name>
  <files>packages/translate/consecutive-breach-tracker/src/consecutive_breach_tracker/__init__.py, packages/translate/consecutive-breach-tracker/src/consecutive_breach_tracker/test_tracker.py</files>
  <behavior>
    - __init__(queue_id): stores queue_id, initializes BreachState(consecutive=0, total=0, queue_id=queue_id)
    - record_breach(): increments consecutive and total; returns FORCE_STAFFING if total >= 20, WIDEN_RINGS if consecutive >= 3, else NONE
    - record_recovery(): resets consecutive to 0, total unchanged
    - get_action(): checks thresholds without modifying state; same priority as record_breach (FORCE_STAFFING > WIDEN_RINGS > NONE)
    - reset(): sets consecutive and total to 0
    - state property: returns BreachState copy of current counters
    - Edge: 3 breaches then recovery then 3 more breaches returns WIDEN_RINGS (consecutive resets, total does not)
    - Edge: exactly 20 total breaches returns FORCE_STAFFING
  </behavior>
  <action>
    Read the stub at packages/translate/consecutive-breach-tracker/src/consecutive_breach_tracker/__init__.py (per W1-03). The interface contract is defined: ConsecutiveBreachTracker class, BreachState dataclass, BreachAction enum, BREACH_THRESHOLDS dict.

    This is a Template 3 (Translate) package. The source pattern is denial-tracking (DenialTracker). The domain translation: permission denials -> SLA breaches, fallback_to_interactive -> WIDEN_RINGS/FORCE_STAFFING, approval -> recovery.

    Implementation:
    - __init__(queue_id): self._state = BreachState(consecutive=0, total=0, queue_id=queue_id)
    - record_breach(): self._state.consecutive += 1; self._state.total += 1; return self.get_action()
    - record_recovery(): self._state.consecutive = 0
    - get_action(): if self._state.total >= BREACH_THRESHOLDS["max_total"]: return FORCE_STAFFING; if self._state.consecutive >= BREACH_THRESHOLDS["max_consecutive"]: return WIDEN_RINGS; return NONE
    - reset(): self._state.consecutive = 0; self._state.total = 0
    - state property: return BreachState(consecutive=self._state.consecutive, total=self._state.total, queue_id=self._state.queue_id)

    Do NOT change any class/method signatures, dataclass fields, enum values, or the BREACH_THRESHOLDS dict. Only replace NotImplementedError bodies.

    Then create test file at packages/translate/consecutive-breach-tracker/src/consecutive_breach_tracker/test_tracker.py using pytest. Test all behaviors listed above.
  </action>
  <verify>
    <automated>cd /Users/cpconnor/projects/Inside\ Claude\ Code/packages/translate/consecutive-breach-tracker && pip install -e . --break-system-packages -q && python -m pytest src/consecutive_breach_tracker/test_tracker.py -v</automated>
  </verify>
  <done>ConsecutiveBreachTracker has 6 working methods/properties, zero NotImplementedError, all tests pass, pip install -e works</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement cost-per-interaction</name>
  <files>packages/translate/cost-per-interaction/src/cost_per_interaction/__init__.py, packages/translate/cost-per-interaction/src/cost_per_interaction/test_aggregator.py</files>
  <behavior>
    - __init__(): initializes empty dict[Channel, list[InteractionCost]]
    - add_interaction(cost): appends cost to the list for cost.channel
    - get_cost_per_contact(channel): returns total_cost_usd / len(interactions) for channel, 0.0 if no interactions
    - get_summary(channel=None): returns list of ChannelSummary for each channel (or filtered to one); cost_per_contact = total_cost / count; avg_duration from interactions that have duration_seconds
    - get_summary() with no interactions for a channel: not included in output
    - format_total_cost(): returns "$X.XX" with sum across all channels
    - Edge: format_total_cost() with no interactions returns "$0.00"
    - Edge: get_cost_per_contact for channel with no interactions returns 0.0
    - Edge: avg_duration_seconds only considers interactions where duration_seconds is not None
  </behavior>
  <action>
    Read the stub at packages/translate/cost-per-interaction/src/cost_per_interaction/__init__.py (per W1-04). The interface contract is defined: ChannelCostAggregator class, InteractionCost dataclass, ChannelSummary dataclass, Channel enum.

    This is a Template 3 (Translate) package. The source pattern is cost-tracker (per-model token aggregation). The domain translation: models -> channels, tokens -> interaction costs, session -> shift/period.

    Implementation:
    - __init__(): self._interactions: dict[Channel, list[InteractionCost]] = {}
    - add_interaction(cost): self._interactions.setdefault(cost.channel, []).append(cost)
    - get_cost_per_contact(channel): interactions = self._interactions.get(channel, []); return sum(i.cost_usd for i in interactions) / len(interactions) if interactions else 0.0
    - get_summary(channel=None): for each channel (or just the specified one), build ChannelSummary with total_interactions=len, total_cost_usd=sum, cost_per_contact=total/count, avg_duration_seconds=mean of non-None durations (0.0 if none). Only include channels that have interactions.
    - format_total_cost(): total = sum of all interaction cost_usd across all channels; return f"${total:.2f}"

    Do NOT change any class/method signatures, dataclass fields, or enum values. Only replace NotImplementedError bodies.

    Then create test file at packages/translate/cost-per-interaction/src/cost_per_interaction/test_aggregator.py using pytest. Test all behaviors listed above.
  </action>
  <verify>
    <automated>cd /Users/cpconnor/projects/Inside\ Claude\ Code/packages/translate/cost-per-interaction && pip install -e . --break-system-packages -q && python -m pytest src/cost_per_interaction/test_aggregator.py -v</automated>
  </verify>
  <done>ChannelCostAggregator has 5 working methods, zero NotImplementedError, all tests pass, pip install -e works</done>
</task>

<task type="auto">
  <name>Task 3: Lint and monorepo verification</name>
  <files></files>
  <action>
    Run ruff lint on both Python packages to confirm clean code. Run make lint for the full monorepo. Grep both implemented files for "NotImplementedError" and "TODO" to confirm zero stubs remain.

    If ruff reports issues, fix them in the relevant __init__.py files without changing method signatures.
  </action>
  <verify>
    <automated>cd /Users/cpconnor/projects/Inside\ Claude\ Code && ruff check packages/translate/consecutive-breach-tracker/ packages/translate/cost-per-interaction/ && make lint</automated>
  </verify>
  <done>ruff lint clean for both Python packages, make lint passes monorepo-wide, no NotImplementedError or TODO stubs remain</done>
</task>

</tasks>

<verification>
- `pip install -e packages/translate/consecutive-breach-tracker --break-system-packages` succeeds
- `pip install -e packages/translate/cost-per-interaction --break-system-packages` succeeds
- `python -m pytest packages/translate/consecutive-breach-tracker/ -v` passes
- `python -m pytest packages/translate/cost-per-interaction/ -v` passes
- `grep -r "NotImplementedError" packages/translate/consecutive-breach-tracker/src/` returns empty
- `grep -r "NotImplementedError" packages/translate/cost-per-interaction/src/` returns empty (excluding test files)
- `ruff check packages/translate/` passes
- `make lint` passes for entire monorepo
</verification>

<success_criteria>
1. consecutive-breach-tracker ConsecutiveBreachTracker has 6 working methods with correct threshold escalation
2. cost-per-interaction ChannelCostAggregator has 5 working methods with per-channel aggregation and $X.XX formatting
3. Zero NotImplementedError stubs remain in either package
4. Both packages install cleanly with pip install -e
5. Tests exist and pass for both packages
6. ruff lint clean, make lint passes monorepo-wide
</success_criteria>

<output>
After completion, create `.planning/phases/06-wave1-quick-wins/06-02-SUMMARY.md`
</output>
