---
phase: 02-p0-package-stubs
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/translate/consecutive-breach-tracker/pyproject.toml
  - packages/translate/consecutive-breach-tracker/src/consecutive_breach_tracker/__init__.py
  - packages/translate/consecutive-breach-tracker/README.md
  - packages/translate/cost-per-interaction/pyproject.toml
  - packages/translate/cost-per-interaction/src/cost_per_interaction/__init__.py
  - packages/translate/cost-per-interaction/README.md
  - packages/translate/prompt-cache-optimizer/tsconfig.json
  - packages/translate/prompt-cache-optimizer/src/index.ts
  - packages/translate/prompt-cache-optimizer/README.md
  - packages/translate/prompt-cache-optimizer/package.json
autonomous: true
requirements: [FR-4.1, FR-4.4, FR-4.5, NFR-2, NFR-3]

must_haves:
  truths:
    - "pip install -e succeeds for consecutive-breach-tracker"
    - "pip install -e succeeds for cost-per-interaction"
    - "prompt-cache-optimizer compiles with tsc --noEmit"
    - "All 3 packages have README.md with source/pattern references"
    - "make scaffold-check reports OK for all 3 translate P0 packages"
  artifacts:
    - path: "packages/translate/consecutive-breach-tracker/src/consecutive_breach_tracker/__init__.py"
      provides: "Queue breach tracking type stubs"
      exports: ["ConsecutiveBreachTracker", "record_breach", "get_action", "reset"]
    - path: "packages/translate/cost-per-interaction/src/cost_per_interaction/__init__.py"
      provides: "Channel cost aggregation type stubs"
      exports: ["InteractionCost", "ChannelCostAggregator", "add_interaction", "get_cost_per_contact", "get_summary"]
    - path: "packages/translate/prompt-cache-optimizer/src/index.ts"
      provides: "Cache optimization type stubs"
      exports: ["optimizeCacheOrder", "CacheScope", "CACHE_BOUNDARY_MARKER"]
  key_links:
    - from: "packages/translate/prompt-cache-optimizer/tsconfig.json"
      to: "tsconfig.base.json"
      via: "extends ../../../tsconfig.base.json"
      pattern: "extends.*tsconfig\\.base"
---

<objective>
Scaffold the 3 translate-tier P0 packages: consecutive-breach-tracker (Python), cost-per-interaction (Python), and prompt-cache-optimizer (TypeScript). Python packages get pyproject.toml, src/{name}/__init__.py, README.md. The TS package gets package.json, tsconfig.json, src/index.ts, README.md.

Purpose: These translate Claude Code patterns to operational domains. consecutive-breach-tracker applies denial tracking to queue overflow. cost-per-interaction applies cost tracking to channel economics. prompt-cache-optimizer applies cache-stable ordering to API cost reduction.

Output: 2 pip-installable Python packages + 1 compilable TS package in packages/translate/.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

Critical references for the executor:
1. Knowledge Base: /Users/cpconnor/projects/Inside Claude Code/claude-code-technical-knowledge-base-v2.1.md (Pattern 4: line 1402, Pattern 7: line 1420, Section 43: lines 1662-1698)
2. Build Inventory: /Users/cpconnor/projects/Inside Claude Code/KB-v2.1-Build-Inventory.md
3. bun binary: $HOME/.bun/bin/bun (not in default PATH)
4. Package scope: @claude-patterns/{name} (TS only)
5. Python package convention: src/{snake_case_name}/__init__.py
6. All implementations are TODO stubs only -- raise NotImplementedError (Python), throw Error (TS)
7. translate TS packages use tsconfig extends path ../../../tsconfig.base.json (one level deeper than extract/build)

<interfaces>
<!-- Existing translate TS package.json pattern from Phase 1 -->
prompt-cache-optimizer already has a minimal package.json:
```json
{"name":"@claude-patterns/prompt-cache-optimizer","version":"0.0.0","private":true}
```
This needs to be expanded with main/types fields.

Translate TS tsconfig.json pattern (one level deeper):
```json
{
  "extends": "../../../tsconfig.base.json",
  "compilerOptions": { "rootDir": "src", "outDir": "dist" },
  "include": ["src"]
}
```

Python pyproject.toml pattern:
```toml
[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.backends._legacy:_Backend"

[project]
name = "claude-patterns-{kebab-name}"
version = "0.0.0"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["src"]
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold consecutive-breach-tracker and cost-per-interaction Python packages (FR-4.1, FR-4.4)</name>
  <files>
    packages/translate/consecutive-breach-tracker/pyproject.toml,
    packages/translate/consecutive-breach-tracker/src/consecutive_breach_tracker/__init__.py,
    packages/translate/consecutive-breach-tracker/README.md,
    packages/translate/cost-per-interaction/pyproject.toml,
    packages/translate/cost-per-interaction/src/cost_per_interaction/__init__.py,
    packages/translate/cost-per-interaction/README.md
  </files>
  <read_first>
    /Users/cpconnor/projects/Inside Claude Code/claude-code-technical-knowledge-base-v2.1.md (Pattern 7: lines 1420-1425, Section 43: lines 1675-1678)
  </read_first>
  <action>
Create directory structures and files for both Python packages.

**consecutive-breach-tracker** (per FR-4.1, KB Pattern 7 + Section 43):

pyproject.toml:
```toml
[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.backends._legacy:_Backend"

[project]
name = "claude-patterns-consecutive-breach-tracker"
version = "0.0.0"
description = "Queue breach detection with adaptive routing escalation"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["src"]
```

src/consecutive_breach_tracker/__init__.py:
```python
"""Queue overflow handling translated from Claude Code denial tracking (Pattern 7).

Tracks SLA breaches per queue. Thresholds:
- 3 consecutive breaches -> widen bullseye rings (activate overflow routing)
- 20 total breaches in shift -> force staffing action (escalate to supervisor)

Source pattern: utils/permissions/denialTracking.ts (45 LOC)
KB reference: Pattern 7 — Adaptive Denial Tracking, Section 43
"""

from __future__ import annotations

from dataclasses import dataclass, field
from enum import Enum
from typing import Optional


class BreachAction(Enum):
    """Action to take after recording a breach."""
    NONE = "none"
    WIDEN_RINGS = "widen_rings"
    FORCE_STAFFING = "force_staffing"


@dataclass
class BreachState:
    """Current breach tracking state for a queue."""
    consecutive: int = 0
    total: int = 0
    queue_id: str = ""


BREACH_THRESHOLDS = {
    "max_consecutive": 3,
    "max_total": 20,
}


class ConsecutiveBreachTracker:
    """Track SLA breaches per queue with adaptive escalation.

    Translates Claude Code's denial tracking pattern to contact center
    queue overflow handling.
    """

    def __init__(self, queue_id: str) -> None:
        # TODO: implement breach tracking logic
        raise NotImplementedError("TODO: translate from denial tracking pattern")

    def record_breach(self) -> BreachAction:
        """Record an SLA breach and return the recommended action.

        Returns:
            BreachAction.NONE — below thresholds
            BreachAction.WIDEN_RINGS — 3+ consecutive breaches
            BreachAction.FORCE_STAFFING — 20+ total breaches
        """
        # TODO: implement breach tracking logic
        raise NotImplementedError("TODO: translate from denial tracking pattern")

    def record_recovery(self) -> None:
        """Record a successful service level interval (resets consecutive counter)."""
        # TODO: implement recovery logic
        raise NotImplementedError("TODO: translate from denial tracking pattern")

    def get_action(self) -> BreachAction:
        """Check current state and return recommended action without recording."""
        # TODO: implement threshold check
        raise NotImplementedError("TODO: translate from denial tracking pattern")

    def reset(self) -> None:
        """Reset all counters (e.g., at shift boundary)."""
        # TODO: implement reset
        raise NotImplementedError("TODO: translate from denial tracking pattern")

    @property
    def state(self) -> BreachState:
        """Current breach state (read-only)."""
        # TODO: return current state
        raise NotImplementedError("TODO: translate from denial tracking pattern")
```

README.md: Description of contact center queue overflow handling. Source pattern: denial tracking (Pattern 7). KB sections 8.4, 43. Translate tier P0. Notes: 3 consecutive -> widen rings, 20 total -> force staffing. Key insight from Claude Code: threshold triggers strategy change, not just alert.

**cost-per-interaction** (per FR-4.4, KB Section 43):

pyproject.toml:
```toml
[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.backends._legacy:_Backend"

[project]
name = "claude-patterns-cost-per-interaction"
version = "0.0.0"
description = "Per-channel cost aggregation for contact center economics"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["src"]
```

src/cost_per_interaction/__init__.py:
```python
"""Per-channel cost aggregation translated from Claude Code cost tracking.

Mirrors per-model token cost tracking from cost-tracker.ts but applied
to contact center channel economics (voice, chat, email, SMS).

Source pattern: cost-tracker.ts (323 LOC)
KB reference: Section 29 — Cost Tracking, Section 43
"""

from __future__ import annotations

from dataclasses import dataclass, field
from datetime import datetime
from enum import Enum
from typing import Optional


class Channel(Enum):
    """Contact center interaction channels."""
    VOICE = "voice"
    CHAT = "chat"
    EMAIL = "email"
    SMS = "sms"
    SOCIAL = "social"
    CALLBACK = "callback"


@dataclass
class InteractionCost:
    """Cost record for a single interaction."""
    channel: Channel
    cost_usd: float
    duration_seconds: Optional[float] = None
    agent_id: Optional[str] = None
    queue_id: Optional[str] = None
    timestamp: Optional[datetime] = None


@dataclass
class ChannelSummary:
    """Aggregated cost summary for a channel."""
    channel: Channel
    total_interactions: int = 0
    total_cost_usd: float = 0.0
    cost_per_contact: float = 0.0
    avg_duration_seconds: float = 0.0


class ChannelCostAggregator:
    """Aggregate interaction costs across channels.

    Translates Claude Code's per-model cost aggregation to
    per-channel cost-per-interaction tracking.
    """

    def __init__(self) -> None:
        # TODO: implement cost aggregation
        raise NotImplementedError("TODO: translate from cost-tracker.ts pattern")

    def add_interaction(self, cost: InteractionCost) -> None:
        """Record an interaction cost."""
        # TODO: implement cost recording
        raise NotImplementedError("TODO: translate from cost-tracker.ts pattern")

    def get_cost_per_contact(self, channel: Channel) -> float:
        """Get average cost per contact for a channel."""
        # TODO: implement cost calculation
        raise NotImplementedError("TODO: translate from cost-tracker.ts pattern")

    def get_summary(self, channel: Optional[Channel] = None) -> list[ChannelSummary]:
        """Get cost summary, optionally filtered by channel."""
        # TODO: implement summary generation
        raise NotImplementedError("TODO: translate from cost-tracker.ts pattern")

    def format_total_cost(self) -> str:
        """Format total cost across all channels as string."""
        # TODO: implement formatting
        raise NotImplementedError("TODO: translate from cost-tracker.ts pattern")
```

README.md: Description of per-channel cost tracking. Source pattern: cost-tracker.ts. KB sections 29, 43. Translate tier P0. Notes: Mirrors per-model token tracking applied to voice/chat/email/SMS channels.

After creating both packages, verify `pip install -e` succeeds for each (use `pip install -e . --dry-run` or actual install in a way that confirms the package structure is valid).
  </action>
  <verify>
    <automated>cd "/Users/cpconnor/projects/Inside Claude Code/packages/translate/consecutive-breach-tracker" && pip install -e . --quiet 2>&1 && cd "/Users/cpconnor/projects/Inside Claude Code/packages/translate/cost-per-interaction" && pip install -e . --quiet 2>&1 && echo "BOTH INSTALL OK"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "ConsecutiveBreachTracker" packages/translate/consecutive-breach-tracker/src/consecutive_breach_tracker/__init__.py` matches
    - `grep "record_breach" packages/translate/consecutive-breach-tracker/src/consecutive_breach_tracker/__init__.py` matches
    - `grep "get_action" packages/translate/consecutive-breach-tracker/src/consecutive_breach_tracker/__init__.py` matches
    - `grep "WIDEN_RINGS" packages/translate/consecutive-breach-tracker/src/consecutive_breach_tracker/__init__.py` matches
    - `grep "FORCE_STAFFING" packages/translate/consecutive-breach-tracker/src/consecutive_breach_tracker/__init__.py` matches
    - `grep "max_consecutive.*3" packages/translate/consecutive-breach-tracker/src/consecutive_breach_tracker/__init__.py` matches
    - `grep "max_total.*20" packages/translate/consecutive-breach-tracker/src/consecutive_breach_tracker/__init__.py` matches
    - `grep "Pattern 7\|denial tracking" packages/translate/consecutive-breach-tracker/README.md` matches
    - `grep "ChannelCostAggregator" packages/translate/cost-per-interaction/src/cost_per_interaction/__init__.py` matches
    - `grep "InteractionCost" packages/translate/cost-per-interaction/src/cost_per_interaction/__init__.py` matches
    - `grep "get_cost_per_contact" packages/translate/cost-per-interaction/src/cost_per_interaction/__init__.py` matches
    - `grep "get_summary" packages/translate/cost-per-interaction/src/cost_per_interaction/__init__.py` matches
    - `grep "cost-tracker.ts\|Section 29" packages/translate/cost-per-interaction/README.md` matches
    - `pip install -e packages/translate/consecutive-breach-tracker` exits 0
    - `pip install -e packages/translate/cost-per-interaction` exits 0
  </acceptance_criteria>
  <done>Both Python packages exist with type stubs, pip install -e succeeds for both, READMEs reference source patterns</done>
</task>

<task type="auto">
  <name>Task 2: Scaffold prompt-cache-optimizer TS package (FR-4.5)</name>
  <files>
    packages/translate/prompt-cache-optimizer/package.json,
    packages/translate/prompt-cache-optimizer/tsconfig.json,
    packages/translate/prompt-cache-optimizer/src/index.ts,
    packages/translate/prompt-cache-optimizer/README.md
  </files>
  <read_first>
    tsconfig.base.json,
    packages/translate/prompt-cache-optimizer/package.json,
    /Users/cpconnor/projects/Inside Claude Code/claude-code-technical-knowledge-base-v2.1.md (Pattern 4: lines 1402-1406, Section 15.2: lines 900-917)
  </read_first>
  <action>
Expand the existing minimal package.json and create the remaining 3 files for prompt-cache-optimizer (per FR-4.5):

**package.json** (update existing): Add main and types fields:
```json
{
  "name": "@claude-patterns/prompt-cache-optimizer",
  "version": "0.0.0",
  "private": true,
  "main": "src/index.ts",
  "types": "src/index.ts"
}
```

**tsconfig.json**: Note translate tier is one level deeper than extract/build:
```json
{
  "extends": "../../../tsconfig.base.json",
  "compilerOptions": { "rootDir": "src", "outDir": "dist" },
  "include": ["src"]
}
```

**src/index.ts**: Type stubs based on KB Pattern 4 + Section 15.2:
```typescript
/**
 * Prompt Cache Optimizer — reduces API costs by 40-70% via cache-stable ordering.
 *
 * Translates Claude Code's cache-stable ordering pattern (Pattern 4) to
 * a reusable utility for any LLM application.
 *
 * Key insight: Tool definitions sorted with built-in tools as contiguous prefix,
 * MCP/dynamic tools appended after boundary marker. Inserting dynamic content
 * between static content invalidates all downstream cache entries.
 *
 * Source pattern: constants/prompts.ts (cache ordering), Pattern 4
 * KB reference: Pattern 4 — Cache-Stable Ordering, Section 15
 */

/**
 * Cache scope determines the sharing level for cached prompt content.
 * - global: shared across all users/orgs (static content)
 * - org: shared within an organization (org-specific config)
 * - null: not cached (per-turn dynamic content)
 */
export enum CacheScope {
  Global = "global",
  Org = "org",
  None = "none",
}

export interface CacheSegment {
  content: string;
  scope: CacheScope;
  stable: boolean;
}

/**
 * Boundary marker between static (cacheable) and dynamic (per-turn) content.
 * Content before this marker is cache-stable; content after may change per request.
 */
export const CACHE_BOUNDARY_MARKER = "--- CACHE BOUNDARY ---";

export interface CacheOptimizationResult {
  segments: CacheSegment[];
  estimatedCacheHitRate: number;
  boundaryPosition: number;
}

/**
 * Reorder prompt segments for optimal cache utilization.
 *
 * Places stable/global content first, org-scoped content second,
 * dynamic content last. Inserts boundary marker between stable and
 * dynamic regions.
 *
 * @param segments - Unordered prompt segments with scope annotations
 * @returns Optimized ordering with boundary marker inserted
 */
export function optimizeCacheOrder(
  segments: CacheSegment[]
): CacheOptimizationResult {
  // TODO: translate from cache-stable ordering pattern
  throw new Error("TODO: translate from cache-stable ordering pattern (Pattern 4)");
}

/**
 * Check if a segment should be placed before the cache boundary.
 */
export function isStableSegment(segment: CacheSegment): boolean {
  // TODO: translate from cache-stable ordering pattern
  throw new Error("TODO: translate from cache-stable ordering pattern (Pattern 4)");
}

/**
 * Calculate estimated cache savings for a given segment ordering.
 *
 * @param totalTokens - Total tokens in all segments
 * @param stableTokens - Tokens in stable (pre-boundary) segments
 * @returns Estimated cost reduction ratio (0.0 to 1.0)
 */
export function estimateCacheSavings(
  totalTokens: number,
  stableTokens: number
): number {
  // TODO: translate from cache-stable ordering pattern
  throw new Error("TODO: translate from cache-stable ordering pattern (Pattern 4)");
}
```

**README.md**: Description of prompt cache optimization for API cost reduction. Source pattern: Pattern 4 (Cache-Stable Ordering). KB sections 15, Pattern 4. Translate tier P0. Notes: 40-70% cost reduction by maintaining cache-stable prefix ordering. Three-tier scoping: global > org > null.

After creating, run `bun install` from root to update workspace resolution, then verify compilation.
  </action>
  <verify>
    <automated>cd "/Users/cpconnor/projects/Inside Claude Code" && npx tsc --noEmit -p packages/translate/prompt-cache-optimizer/tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - `grep "optimizeCacheOrder" packages/translate/prompt-cache-optimizer/src/index.ts` matches
    - `grep "CacheScope" packages/translate/prompt-cache-optimizer/src/index.ts` matches
    - `grep "CACHE_BOUNDARY_MARKER" packages/translate/prompt-cache-optimizer/src/index.ts` matches
    - `grep "CacheSegment" packages/translate/prompt-cache-optimizer/src/index.ts` matches
    - `grep "extends.*tsconfig.base" packages/translate/prompt-cache-optimizer/tsconfig.json` matches
    - `grep "\.\./\.\./\.\./tsconfig" packages/translate/prompt-cache-optimizer/tsconfig.json` matches
    - `grep "@claude-patterns/prompt-cache-optimizer" packages/translate/prompt-cache-optimizer/package.json` matches
    - `grep "Pattern 4\|Cache-Stable\|40-70%" packages/translate/prompt-cache-optimizer/README.md` matches
    - `npx tsc --noEmit -p packages/translate/prompt-cache-optimizer/tsconfig.json` exits 0
  </acceptance_criteria>
  <done>prompt-cache-optimizer package exists with cache optimization type stubs, compiles with tsc --noEmit, README references Pattern 4 and cache-stable ordering</done>
</task>

</tasks>

<verification>
All 3 translate P0 packages work and scaffold-check recognizes them:
```bash
cd "/Users/cpconnor/projects/Inside Claude Code"
pip install -e packages/translate/consecutive-breach-tracker --quiet
pip install -e packages/translate/cost-per-interaction --quiet
npx tsc --noEmit -p packages/translate/prompt-cache-optimizer/tsconfig.json
make scaffold-check 2>&1 | grep -E "consecutive-breach|cost-per-interaction|prompt-cache-optimizer"
```
All 3 should show "OK" in scaffold-check output.
</verification>

<success_criteria>
- 2 Python packages + 1 TS package created with all required files
- `pip install -e` succeeds for both Python packages
- prompt-cache-optimizer passes `tsc --noEmit`
- Python stubs have proper dataclass/enum types with NotImplementedError
- TS stubs have proper interfaces/enums with throw Error("TODO")
- All function/method bodies reference source patterns
- `make scaffold-check` reports OK for all 3 packages
</success_criteria>

<output>
After completion, create `.planning/phases/02-p0-package-stubs/02-03-SUMMARY.md`
</output>
