---
phase: 01-monorepo-scaffold
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.base.json
  - biome.json
  - Makefile
  - bunfig.toml
autonomous: true
requirements: [FR-1.1, FR-1.2, FR-1.3]

must_haves:
  truths:
    - "`bun install` completes without errors"
    - "`tsc --noEmit` runs successfully against tsconfig.base.json"
    - "`make scaffold-check` target exists and reports 0/31 packages"
    - "`make type-check` and `make lint` targets exist and run"
  artifacts:
    - path: "package.json"
      provides: "Bun workspace root with @claude-patterns/ scope"
      contains: "workspaces"
    - path: "tsconfig.base.json"
      provides: "Shared TS config — strict, ES2022, Bun types"
      contains: "strict"
    - path: "biome.json"
      provides: "Biome v2 root linter config"
      contains: "linter"
    - path: "Makefile"
      provides: "Polyglot task runner with 4 targets"
      contains: "scaffold-check"
  key_links:
    - from: "Makefile:type-check"
      to: "tsconfig.base.json"
      via: "tsc --noEmit invocation"
      pattern: "tsc.*noEmit"
    - from: "package.json:workspaces"
      to: "packages/extract/*, packages/build/*, packages/translate/*"
      via: "Bun workspace resolution"
      pattern: "packages/"
---

<objective>
Create the monorepo root configuration files: package.json (Bun workspaces), tsconfig.base.json (strict/ES2022), biome.json (Biome v2), and Makefile (scaffold-check, type-check, lint, list-packages).

Purpose: Establishes the build tooling foundation that all 31 packages will build on. Nothing compiles or validates without these root configs.
Output: 4 config files at project root. `bun install` works, `tsc --noEmit` runs, `make scaffold-check` reports 0/31.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create package.json, tsconfig.base.json, and biome.json</name>
  <files>package.json, tsconfig.base.json, biome.json</files>
  <action>
Create three root config files per FR-1.1 and FR-1.2:

**package.json** — Bun workspace root:
```json
{
  "name": "claude-code-patterns",
  "private": true,
  "workspaces": [
    "packages/extract/*",
    "packages/build/*",
    "packages/translate/ivr-call-flow-validator",
    "packages/translate/prompt-cache-optimizer"
  ]
}
```
Only 28 TypeScript packages in workspaces. The 3 Python packages (consecutive-breach-tracker, cost-per-interaction, agent-skill-routing) are NOT in workspaces — they use pip. The 2 TS translate packages are listed explicitly because the translate dir also contains Python packages.

**tsconfig.base.json** — Shared compiler options all TS packages will extend:
```json
{
  "compilerOptions": {
    "strict": true,
    "target": "ES2022",
    "module": "ESNext",
    "moduleResolution": "bundler",
    "esModuleInterop": true,
    "skipLibCheck": true,
    "noEmit": true,
    "declaration": true,
    "declarationMap": true,
    "sourceMap": true,
    "forceConsistentCasingInFileNames": true,
    "resolveJsonModule": true,
    "isolatedModules": true,
    "types": ["bun-types"]
  }
}
```

**biome.json** — Biome v2 root config:
```json
{
  "$schema": "https://biomejs.dev/schemas/2.0.0/schema.json",
  "vcs": {
    "enabled": true,
    "clientKind": "git",
    "useIgnoreFile": true
  },
  "formatter": {
    "enabled": true,
    "indentStyle": "space",
    "indentWidth": 2
  },
  "linter": {
    "enabled": true
  },
  "javascript": {
    "formatter": {
      "quoteStyle": "double",
      "semicolons": "always"
    }
  },
  "files": {
    "ignore": ["node_modules", "dist", "*.d.ts"]
  }
}
```

After creating files, run `bun install` to generate lockfile and validate workspace resolution. Then run `tsc --noEmit` to confirm the tsconfig is valid (it should succeed with 0 files since no packages exist yet). Install bun-types and typescript as devDependencies: `bun add -d typescript bun-types`.

NOTE: biome.json uses Biome v2 schema. Install biome as devDependency: `bun add -d @biomejs/biome`.
  </action>
  <verify>
    <automated>cd "/Users/cpconnor/projects/Inside Claude Code" && bun install && npx tsc --noEmit -p tsconfig.base.json && echo "PASS: bun install + tsc --noEmit succeeded"</automated>
  </verify>
  <done>package.json exists with correct workspace globs, tsconfig.base.json has strict+ES2022+Bun types, biome.json has Biome v2 config, `bun install` completes, `tsc --noEmit` runs without errors</done>
</task>

<task type="auto">
  <name>Task 2: Create Makefile with all targets</name>
  <files>Makefile</files>
  <action>
Create Makefile per FR-1.3 with 4 targets. CRITICAL: Use path-based approach for type-check, NOT `bun --filter '*'` (which fails with scoped packages per known blocker in STATE.md).

```makefile
.PHONY: scaffold-check type-check lint list-packages

# All 31 package directories (used for scaffold validation)
EXTRACT_PKGS := permission-system denial-tracking cost-tracker state-store \
  streaming-tool-executor token-estimation subprocess-env-scrubbing \
  config-migration path-validation read-only-validation analytics-killswitch \
  claudemd-memory yolo-classifier auto-compact sandbox-config \
  dangerous-command-detection
BUILD_PKGS := prompt-system context-injection agent-dialogue-loop \
  skills-system multi-agent-coordinator mcp-integration vim-mode-fsm \
  keyboard-shortcuts ink-renderer cli-startup-optimization
TRANSLATE_TS_PKGS := ivr-call-flow-validator prompt-cache-optimizer
TRANSLATE_PY_PKGS := consecutive-breach-tracker cost-per-interaction \
  agent-skill-routing

ALL_EXTRACT := $(addprefix packages/extract/,$(EXTRACT_PKGS))
ALL_BUILD := $(addprefix packages/build/,$(BUILD_PKGS))
ALL_TRANSLATE_TS := $(addprefix packages/translate/,$(TRANSLATE_TS_PKGS))
ALL_TRANSLATE_PY := $(addprefix packages/translate/,$(TRANSLATE_PY_PKGS))
ALL_PKGS := $(ALL_EXTRACT) $(ALL_BUILD) $(ALL_TRANSLATE_TS) $(ALL_TRANSLATE_PY)
TS_PKGS := $(ALL_EXTRACT) $(ALL_BUILD) $(ALL_TRANSLATE_TS)

scaffold-check:
	@total=0; found=0; \
	for pkg in $(ALL_PKGS); do \
		total=$$((total + 1)); \
		name=$$(basename $$pkg); \
		if [ -d "$$pkg" ] && [ -f "$$pkg/README.md" ]; then \
			found=$$((found + 1)); \
			echo "  OK  $$pkg"; \
		else \
			echo "  MISSING  $$pkg"; \
		fi; \
	done; \
	echo ""; \
	echo "$$found/$$total packages present"; \
	if [ $$found -eq $$total ]; then exit 0; else exit 1; fi

type-check:
	@failed=0; checked=0; \
	for pkg in $(TS_PKGS); do \
		if [ -f "$$pkg/tsconfig.json" ]; then \
			checked=$$((checked + 1)); \
			echo "Checking $$pkg..."; \
			npx tsc --noEmit -p "$$pkg/tsconfig.json" || failed=$$((failed + 1)); \
		fi; \
	done; \
	echo ""; \
	echo "$$checked packages checked, $$failed failed"; \
	if [ $$failed -eq 0 ]; then exit 0; else exit 1; fi

lint:
	@echo "=== TypeScript (Biome) ==="; \
	npx @biomejs/biome check packages/extract packages/build \
		packages/translate/ivr-call-flow-validator \
		packages/translate/prompt-cache-optimizer 2>/dev/null || true; \
	echo ""; \
	echo "=== Python (Ruff) ==="; \
	ruff check packages/translate/consecutive-breach-tracker \
		packages/translate/cost-per-interaction \
		packages/translate/agent-skill-routing 2>/dev/null || true

list-packages:
	@echo "=== Extract Tier (16 TS) ==="; \
	for pkg in $(EXTRACT_PKGS); do echo "  packages/extract/$$pkg"; done; \
	echo ""; \
	echo "=== Build Tier (10 TS) ==="; \
	for pkg in $(BUILD_PKGS); do echo "  packages/build/$$pkg"; done; \
	echo ""; \
	echo "=== Translate Tier (2 TS + 3 Python) ==="; \
	for pkg in $(TRANSLATE_TS_PKGS); do echo "  packages/translate/$$pkg [TS]"; done; \
	for pkg in $(TRANSLATE_PY_PKGS); do echo "  packages/translate/$$pkg [Python]"; done; \
	echo ""; \
	echo "31 packages total (28 TS + 3 Python)"
```

Also create the empty package directories so Bun workspace resolution does not error. Create: `packages/extract/`, `packages/build/`, `packages/translate/`. These are just the tier directories — individual package dirs come in Phase 2+.
  </action>
  <verify>
    <automated>cd "/Users/cpconnor/projects/Inside Claude Code" && make scaffold-check; echo "---"; make list-packages; echo "---"; make type-check</automated>
  </verify>
  <done>`make scaffold-check` runs and reports 0/31 packages, `make type-check` runs with 0 packages checked, `make lint` runs, `make list-packages` enumerates all 31 packages with tiers</done>
</task>

</tasks>

<verification>
Run all three success criteria checks in sequence:
1. `bun install` — must complete without errors
2. `npx tsc --noEmit -p tsconfig.base.json` — must exit 0
3. `make scaffold-check` — must run and report 0/31
4. `make list-packages` — must show all 31 packages
</verification>

<success_criteria>
- package.json has correct workspace globs covering 28 TS packages
- tsconfig.base.json has strict=true, target=ES2022, types=["bun-types"]
- biome.json has Biome v2 schema and TS formatting rules
- Makefile has scaffold-check, type-check, lint, list-packages targets
- `bun install` completes without errors
- `tsc --noEmit` runs without errors
- All make targets are functional
</success_criteria>

<output>
After completion, create `.planning/phases/01-monorepo-scaffold/01-01-SUMMARY.md`
</output>
