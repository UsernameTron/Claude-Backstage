---
phase: 05-scaffold-expansion
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/build/tool-schema-cache/package.json
  - packages/build/tool-schema-cache/tsconfig.json
  - packages/build/tool-schema-cache/src/index.ts
  - packages/build/tool-schema-cache/README.md
  - packages/build/tool-registry/package.json
  - packages/build/tool-registry/tsconfig.json
  - packages/build/tool-registry/src/index.ts
  - packages/build/tool-registry/README.md
  - packages/build/dialogue-history-manager/package.json
  - packages/build/dialogue-history-manager/tsconfig.json
  - packages/build/dialogue-history-manager/src/index.ts
  - packages/build/dialogue-history-manager/README.md
  - packages/build/system-reminder-injection/package.json
  - packages/build/system-reminder-injection/tsconfig.json
  - packages/build/system-reminder-injection/src/index.ts
  - packages/build/system-reminder-injection/README.md
  - packages/build/plugin-lifecycle-manager/package.json
  - packages/build/plugin-lifecycle-manager/tsconfig.json
  - packages/build/plugin-lifecycle-manager/src/index.ts
  - packages/build/plugin-lifecycle-manager/README.md
autonomous: true
requirements: [EXP-35, EXP-36, EXP-37, EXP-38, EXP-39]

must_haves:
  truths:
    - "All 5 build-tier P2 packages compile with tsc --noEmit"
    - "Each package has 4 files: package.json, tsconfig.json, src/index.ts, README.md"
    - "All function bodies throw Error('TODO: ...')"
  artifacts:
    - path: "packages/build/tool-schema-cache/src/index.ts"
      provides: "ToolSchemaCache class, CachedToolSchema, CachePolicy"
      contains: "export class ToolSchemaCache"
    - path: "packages/build/tool-registry/src/index.ts"
      provides: "Tool, ToolPool, assembleToolPool, filterToolsByDenyRules, sortForCacheStability"
      contains: "export function assembleToolPool"
    - path: "packages/build/dialogue-history-manager/src/index.ts"
      provides: "DialogueHistoryManager class, MessageType, DialogueMessage, CompactBoundaryMessage"
      contains: "export class DialogueHistoryManager"
    - path: "packages/build/system-reminder-injection/src/index.ts"
      provides: "SystemReminderSource, SystemReminder, injectReminder, wrapInReminderTags"
      contains: "export function injectReminder"
    - path: "packages/build/plugin-lifecycle-manager/src/index.ts"
      provides: "PluginLifecycleManager class, PluginManifest, PluginState"
      contains: "export class PluginLifecycleManager"
  key_links: []
---

<objective>
Scaffold 5 build-tier P2 packages: #35 tool-schema-cache, #36 tool-registry, #37 dialogue-history-manager, #38 system-reminder-injection, #39 plugin-lifecycle-manager.

Purpose: Extend build tier from 10 to 15 packages, covering tool management, dialogue history, system reminders, and plugin lifecycle.
Output: 5 new package directories under packages/build/ with all stub files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@SCAFFOLD-EXPANSION-PROMPT.md
</context>

<interfaces>
<!-- Standard build-tier pattern from packages/build/prompt-system/ -->
package.json:
```json
{
  "name": "@claude-patterns/prompt-system",
  "version": "0.0.0",
  "private": true,
  "main": "src/index.ts",
  "types": "src/index.ts"
}
```

tsconfig.json:
```json
{
  "extends": "../../../tsconfig.base.json",
  "compilerOptions": { "rootDir": "src", "outDir": "dist" },
  "include": ["src"]
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold #35 tool-schema-cache, #36 tool-registry, #37 dialogue-history-manager</name>
  <files>
    packages/build/tool-schema-cache/package.json,
    packages/build/tool-schema-cache/tsconfig.json,
    packages/build/tool-schema-cache/src/index.ts,
    packages/build/tool-schema-cache/README.md,
    packages/build/tool-registry/package.json,
    packages/build/tool-registry/tsconfig.json,
    packages/build/tool-registry/src/index.ts,
    packages/build/tool-registry/README.md,
    packages/build/dialogue-history-manager/package.json,
    packages/build/dialogue-history-manager/tsconfig.json,
    packages/build/dialogue-history-manager/src/index.ts,
    packages/build/dialogue-history-manager/README.md
  </files>
  <read_first>
    packages/build/prompt-system/package.json,
    packages/build/prompt-system/tsconfig.json,
    packages/build/prompt-system/src/index.ts,
    packages/build/prompt-system/README.md,
    SCAFFOLD-EXPANSION-PROMPT.md
  </read_first>
  <action>
    Create 3 build-tier packages using the established 4-file pattern. Each gets package.json, tsconfig.json, src/index.ts, README.md.

    **All package.json files** follow this exact template (substitute name):
    ```json
    {
      "name": "@claude-patterns/{name}",
      "version": "0.0.0",
      "private": true,
      "main": "src/index.ts",
      "types": "src/index.ts"
    }
    ```

    **All tsconfig.json files** are identical:
    ```json
    {
      "extends": "../../../tsconfig.base.json",
      "compilerOptions": { "rootDir": "src", "outDir": "dist" },
      "include": ["src"]
    }
    ```

    **#35 tool-schema-cache src/index.ts:**
    - JSDoc header: tool schema caching pattern from Section 21.3 — freezes tool schemas at session start to prevent prompt cache invalidation from feature flag changes
    - `export type CachePolicy = "freeze_on_start" | "refresh_on_change" | "manual";`
    - `export interface CachedToolSchema`: name: string, description: string, inputSchema: Record<string, unknown>, cachedAt: number
    - `export class ToolSchemaCache`:
      - `get(toolName: string): CachedToolSchema | undefined` — throw TODO
      - `set(toolName: string, schema: CachedToolSchema): void` — throw TODO
      - `refresh(tools: Tool[]): void` — throw TODO
      - `invalidate(): void` — throw TODO
      - `getStableSchemaList(): CachedToolSchema[]` — throw TODO (returns schemas in deterministic order)
    - Need a `Tool` interface for the refresh method parameter: `export interface Tool`: name: string, description: string, inputSchema: Record<string, unknown>

    **#36 tool-registry src/index.ts:**
    - JSDoc header: three-layer tool assembly from Section 6.2-6.3 — compile-time elimination, runtime deny-rule filtering, merge built-in + external
    - `export interface Tool`: name: string, description: string, inputSchema: Record<string, unknown>, call: (...args: unknown[]) => Promise<unknown>, permissions: string[]
    - `export interface ToolPool`: tools: Tool[], getByName: (name: string) => Tool | undefined, filter: (predicate: (tool: Tool) => boolean) => Tool[]
    - `export type ToolFilterLayer = "compile" | "runtime_deny" | "assembly";`
    - `export function assembleToolPool(builtInTools: Tool[], externalTools: Tool[], denyRules: string[]): ToolPool` — throw TODO
    - `export function filterToolsByDenyRules(tools: Tool[], denyRules: string[]): Tool[]` — throw TODO
    - `export function sortForCacheStability(tools: Tool[]): Tool[]` — throw TODO
    - `export function registerTool(tool: Tool): void` — throw TODO
    - `export function getAllTools(): Tool[]` — throw TODO

    **#37 dialogue-history-manager src/index.ts:**
    - JSDoc header: dialogue history management from Section 19 — message lifecycle, JSONL persistence, crash-safe write-before-response
    - `export type MessageType = "user" | "assistant" | "attachment" | "system" | "compact_boundary" | "tool_use_summary" | "progress";`
    - `export interface DialogueMessage`: type: MessageType, content: string, timestamp: number, metadata: Record<string, unknown>
    - `export interface CompactBoundaryMessage extends DialogueMessage`: summary: string, originalMessageCount: number
    - `export interface HistoryConfig`: maxRecords: number, storePath: string, externalStorageThreshold: number
    - `export class DialogueHistoryManager`:
      - `constructor(config: HistoryConfig)` — throw TODO
      - `addMessage(msg: DialogueMessage): void` — throw TODO
      - `getMessagesAfterCompactBoundary(): DialogueMessage[]` — throw TODO
      - `persist(): void` — throw TODO (write-before-response pattern)
      - `loadFromDisk(path: string): DialogueMessage[]` — throw TODO
      - `getEffectiveMessages(): DialogueMessage[]` — throw TODO
      - `insertCompactBoundary(summary: string): void` — throw TODO

    **README.md for each** — build tier format with `# @claude-patterns/{name}`:
    - Tier: Build — TS, P2
    - Source Pattern and KB sections from spec
    - Architecture section summarizing the pattern
    - Exports list
    - Dependencies: None
    - Status: Type stubs only. All functions throw Error.
  </action>
  <verify>
    <automated>cd "/Users/cpconnor/projects/Inside Claude Code" && npx tsc --noEmit -p packages/build/tool-schema-cache/tsconfig.json && npx tsc --noEmit -p packages/build/tool-registry/tsconfig.json && npx tsc --noEmit -p packages/build/dialogue-history-manager/tsconfig.json && echo "All 3 pass"</automated>
  </verify>
  <acceptance_criteria>
    - packages/build/tool-schema-cache/package.json contains `"name": "@claude-patterns/tool-schema-cache"`
    - packages/build/tool-schema-cache/src/index.ts contains `export class ToolSchemaCache`
    - packages/build/tool-schema-cache/src/index.ts contains `export type CachePolicy`
    - packages/build/tool-schema-cache/src/index.ts contains `getStableSchemaList`
    - packages/build/tool-registry/package.json contains `"name": "@claude-patterns/tool-registry"`
    - packages/build/tool-registry/src/index.ts contains `export function assembleToolPool`
    - packages/build/tool-registry/src/index.ts contains `export function filterToolsByDenyRules`
    - packages/build/tool-registry/src/index.ts contains `export function sortForCacheStability`
    - packages/build/tool-registry/src/index.ts contains `export type ToolFilterLayer`
    - packages/build/dialogue-history-manager/package.json contains `"name": "@claude-patterns/dialogue-history-manager"`
    - packages/build/dialogue-history-manager/src/index.ts contains `export class DialogueHistoryManager`
    - packages/build/dialogue-history-manager/src/index.ts contains `export type MessageType`
    - packages/build/dialogue-history-manager/src/index.ts contains `export interface CompactBoundaryMessage`
    - packages/build/dialogue-history-manager/src/index.ts contains `insertCompactBoundary`
    - All 3 packages have README.md files
    - npx tsc --noEmit passes for all 3 packages
  </acceptance_criteria>
  <done>
    #35 tool-schema-cache, #36 tool-registry, #37 dialogue-history-manager all compile via tsc --noEmit. Each has 4 files following the established build-tier pattern.
  </done>
</task>

<task type="auto">
  <name>Task 2: Scaffold #38 system-reminder-injection and #39 plugin-lifecycle-manager</name>
  <files>
    packages/build/system-reminder-injection/package.json,
    packages/build/system-reminder-injection/tsconfig.json,
    packages/build/system-reminder-injection/src/index.ts,
    packages/build/system-reminder-injection/README.md,
    packages/build/plugin-lifecycle-manager/package.json,
    packages/build/plugin-lifecycle-manager/tsconfig.json,
    packages/build/plugin-lifecycle-manager/src/index.ts,
    packages/build/plugin-lifecycle-manager/README.md
  </files>
  <read_first>
    packages/build/prompt-system/package.json,
    packages/build/prompt-system/tsconfig.json,
    packages/build/prompt-system/src/index.ts,
    SCAFFOLD-EXPANSION-PROMPT.md
  </read_first>
  <action>
    Create 2 more build-tier packages using the same 4-file pattern.

    **#38 system-reminder-injection src/index.ts:**
    - JSDoc header: system reminder mechanism from Section 20 — `<system-reminder>` tag injection into user messages, tool results, and attachments
    - `export type SystemReminderSource = "user_context" | "tool_result" | "attachment" | "todo_write" | "deferred_tools" | "mcp_status";`
    - `export interface SystemReminder`: source: SystemReminderSource, content: string, targetMessageIndex: number
    - `export interface ReminderInjectionConfig`: wrapInTags: boolean, tagName: string
    - `export function injectReminder(message: string, reminder: SystemReminder): string` — throw TODO
    - `export function wrapInReminderTags(content: string, tagName?: string): string` — throw TODO
    - `export function extractReminders(message: string): SystemReminder[]` — throw TODO
    - `export function shouldInjectReminder(source: SystemReminderSource, turnCount: number): boolean` — throw TODO

    **#39 plugin-lifecycle-manager src/index.ts:**
    - JSDoc header: plugin system lifecycle from Section 25 — four-phase lifecycle: Discovery -> Cache -> Cleanup -> Telemetry
    - `export type PluginState = "discovered" | "cached" | "active" | "orphaned" | "error";`
    - `export interface PluginManifest`: name: string, version: string, description: string, skills: string[], mcpServers: string[], hooks: Record<string, unknown>
    - `export interface PluginLifecycleConfig`: seedDirs: string[], cacheDir: string, maxCacheAge: number
    - `export class PluginLifecycleManager`:
      - `constructor(config: PluginLifecycleConfig)` — throw TODO
      - `discover(): PluginManifest[]` — throw TODO
      - `loadFromCache(): PluginManifest[]` — throw TODO
      - `cleanupOrphaned(): Promise<number>` — throw TODO (returns count removed)
      - `logSessionPlugins(active: PluginManifest[]): void` — throw TODO
      - `getState(pluginName: string): PluginState` — throw TODO
      - `activate(pluginName: string): void` — throw TODO

    **package.json and tsconfig.json** — same template as Task 1 (substitute name).

    **README.md for each** — build tier format:
    - Title: `# @claude-patterns/{name}`
    - Tier: Build — TS, P2
    - Source Pattern and KB sections from spec
    - Architecture section
    - Exports, Dependencies (None), Status
  </action>
  <verify>
    <automated>cd "/Users/cpconnor/projects/Inside Claude Code" && npx tsc --noEmit -p packages/build/system-reminder-injection/tsconfig.json && npx tsc --noEmit -p packages/build/plugin-lifecycle-manager/tsconfig.json && echo "Both pass"</automated>
  </verify>
  <acceptance_criteria>
    - packages/build/system-reminder-injection/package.json contains `"name": "@claude-patterns/system-reminder-injection"`
    - packages/build/system-reminder-injection/src/index.ts contains `export type SystemReminderSource`
    - packages/build/system-reminder-injection/src/index.ts contains `export function injectReminder`
    - packages/build/system-reminder-injection/src/index.ts contains `export function wrapInReminderTags`
    - packages/build/system-reminder-injection/src/index.ts contains `export function extractReminders`
    - packages/build/system-reminder-injection/src/index.ts contains `export function shouldInjectReminder`
    - packages/build/plugin-lifecycle-manager/package.json contains `"name": "@claude-patterns/plugin-lifecycle-manager"`
    - packages/build/plugin-lifecycle-manager/src/index.ts contains `export class PluginLifecycleManager`
    - packages/build/plugin-lifecycle-manager/src/index.ts contains `export type PluginState`
    - packages/build/plugin-lifecycle-manager/src/index.ts contains `export interface PluginManifest`
    - packages/build/plugin-lifecycle-manager/src/index.ts contains `cleanupOrphaned`
    - packages/build/plugin-lifecycle-manager/src/index.ts contains `throw new Error("TODO:`
    - Both packages have README.md
    - npx tsc --noEmit passes for both packages
  </acceptance_criteria>
  <done>
    #38 system-reminder-injection and #39 plugin-lifecycle-manager compile via tsc --noEmit. Each has 4 files with correct stubs and READMEs.
  </done>
</task>

</tasks>

<verification>
Per D-05: Run type-check on all 5 new build packages.
```bash
cd "/Users/cpconnor/projects/Inside Claude Code"
for pkg in tool-schema-cache tool-registry dialogue-history-manager system-reminder-injection plugin-lifecycle-manager; do
  npx tsc --noEmit -p "packages/build/$pkg/tsconfig.json"
done
```
</verification>

<success_criteria>
- 5 new directories exist under packages/build/
- All 5 compile with tsc --noEmit
- Each has package.json, tsconfig.json, src/index.ts, README.md
- All function bodies throw Error("TODO: ...")
</success_criteria>

<output>
After completion, create `.planning/phases/05-scaffold-expansion-add-12-new-packages-32-43-from-gap-analysis/05-02-SUMMARY.md`
</output>
