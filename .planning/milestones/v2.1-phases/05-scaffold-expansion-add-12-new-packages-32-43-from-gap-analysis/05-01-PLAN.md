---
phase: 05-scaffold-expansion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/translate/workforce-scheduling-coordinator/pyproject.toml
  - packages/translate/workforce-scheduling-coordinator/src/workforce_scheduling_coordinator/__init__.py
  - packages/translate/workforce-scheduling-coordinator/README.md
  - packages/translate/genesys-flow-security-validator/package.json
  - packages/translate/genesys-flow-security-validator/tsconfig.json
  - packages/translate/genesys-flow-security-validator/src/index.ts
  - packages/translate/genesys-flow-security-validator/README.md
  - packages/translate/multi-step-ivr-input-validator/package.json
  - packages/translate/multi-step-ivr-input-validator/tsconfig.json
  - packages/translate/multi-step-ivr-input-validator/src/index.ts
  - packages/translate/multi-step-ivr-input-validator/README.md
autonomous: true
requirements: [EXP-32, EXP-33, EXP-34]

must_haves:
  truths:
    - "Package #32 workforce-scheduling-coordinator installs with pip install -e"
    - "Package #33 genesys-flow-security-validator compiles with tsc --noEmit"
    - "Package #34 multi-step-ivr-input-validator compiles and imports IVRCallFlow from ivr-call-flow-validator"
  artifacts:
    - path: "packages/translate/workforce-scheduling-coordinator/src/workforce_scheduling_coordinator/__init__.py"
      provides: "SchedulingCoordinator class, SchedulingJob dataclass, JobType enum, WorkerResult, CoordinatorConfig"
      contains: "class SchedulingCoordinator"
    - path: "packages/translate/genesys-flow-security-validator/src/index.ts"
      provides: "FlowValidationRule, FlowVulnerability, validateFlow, getBuiltInRules, ArchitectFlow, ArchitectNode"
      contains: "export function validateFlow"
    - path: "packages/translate/multi-step-ivr-input-validator/src/index.ts"
      provides: "DTMFSequence, DTMFStep, validateSequence, decomposeInput, generateAllSequences"
      contains: "import type"
  key_links:
    - from: "packages/translate/multi-step-ivr-input-validator/src/index.ts"
      to: "@claude-patterns/ivr-call-flow-validator"
      via: "import type { IVRCallFlow, IVRNode }"
      pattern: "import type.*IVRCallFlow.*ivr-call-flow-validator"
---

<objective>
Scaffold 3 translate-tier packages: #32 workforce-scheduling-coordinator (Python), #33 genesys-flow-security-validator (TS), #34 multi-step-ivr-input-validator (TS).

Purpose: Extend translate tier from 5 to 8 packages, covering WFM scheduling, Genesys security, and multi-step IVR validation.
Output: 3 new package directories with all stub files, READMEs, and manifests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@SCAFFOLD-EXPANSION-PROMPT.md
</context>

<interfaces>
<!-- Existing types from ivr-call-flow-validator that #34 must import -->
From packages/translate/ivr-call-flow-validator/src/index.ts:
```typescript
export interface IVRNode {
  id: string;
  type: IVRNodeType;
  label: string;
  transitions: IVRTransition[];
  defaultTransition?: IVRTransition;
  timeout?: number;
  maxRetries?: number;
}

export interface IVRCallFlow {
  id: string;
  name: string;
  entryNode: string;
  nodes: Record<string, IVRNode>;
  version: string;
}
```

Python pattern from packages/translate/consecutive-breach-tracker/pyproject.toml:
```toml
[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.build_meta"

[project]
name = "claude-patterns-consecutive-breach-tracker"
version = "0.0.0"
description = "Queue breach detection with adaptive routing escalation"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["src"]
```

TS translate pattern from packages/translate/ivr-call-flow-validator/package.json:
```json
{
  "name": "@claude-patterns/ivr-call-flow-validator",
  "version": "0.0.0",
  "private": true,
  "main": "src/index.ts",
  "types": "src/index.ts"
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold #32 workforce-scheduling-coordinator (Python) and #33 genesys-flow-security-validator (TS)</name>
  <files>
    packages/translate/workforce-scheduling-coordinator/pyproject.toml,
    packages/translate/workforce-scheduling-coordinator/src/workforce_scheduling_coordinator/__init__.py,
    packages/translate/workforce-scheduling-coordinator/README.md,
    packages/translate/genesys-flow-security-validator/package.json,
    packages/translate/genesys-flow-security-validator/tsconfig.json,
    packages/translate/genesys-flow-security-validator/src/index.ts,
    packages/translate/genesys-flow-security-validator/README.md
  </files>
  <read_first>
    packages/translate/consecutive-breach-tracker/pyproject.toml,
    packages/translate/consecutive-breach-tracker/src/consecutive_breach_tracker/__init__.py,
    packages/translate/consecutive-breach-tracker/README.md,
    packages/translate/ivr-call-flow-validator/package.json,
    packages/translate/ivr-call-flow-validator/tsconfig.json,
    packages/translate/ivr-call-flow-validator/src/index.ts,
    packages/translate/ivr-call-flow-validator/README.md,
    SCAFFOLD-EXPANSION-PROMPT.md
  </read_first>
  <action>
    Create 2 packages following exact existing conventions:

    **#32 workforce-scheduling-coordinator (Python):**

    pyproject.toml — copy consecutive-breach-tracker pattern:
    - name: "claude-patterns-workforce-scheduling-coordinator"
    - version: "0.0.0"
    - description: "Workforce scheduling coordination translated from multi-agent coordinator pattern"
    - requires-python: ">=3.11"
    - build-backend: "setuptools.build_meta"
    - [tool.setuptools.packages.find] where = ["src"]

    src/workforce_scheduling_coordinator/__init__.py:
    - Module docstring: explain translation from multi-agent coordinator (Section 24) to WFM scheduling
    - `from __future__ import annotations`
    - Imports: dataclass, field from dataclasses; Enum from enum; Optional from typing
    - `class JobType(Enum)`: FORECAST = "forecast", OPTIMIZE_SHIFTS = "optimize_shifts", CHECK_ADHERENCE = "check_adherence", GENERATE_REPORT = "generate_report", INTRADAY_REBALANCE = "intraday_rebalance"
    - `@dataclass class SchedulingJob`: job_id: str, job_type: JobType, status: str, parameters: dict, result: Optional[dict] = None
    - `@dataclass class WorkerResult`: worker_id: str, job_id: str, status: str, output: dict, duration_seconds: float
    - `@dataclass class CoordinatorConfig`: max_concurrent_workers: int = 5, job_timeout_seconds: int = 300
    - `class SchedulingCoordinator`:
      - `__init__(self, config: CoordinatorConfig) -> None` — raise NotImplementedError("TODO: translate from multi-agent coordinator pattern")
      - `dispatch_job(self, job: SchedulingJob) -> WorkerResult` — raise NotImplementedError
      - `get_active_jobs(self) -> list[SchedulingJob]` — raise NotImplementedError
      - `cancel_job(self, job_id: str) -> bool` — raise NotImplementedError
      - `get_shared_context(self) -> dict` — raise NotImplementedError

    README.md — follow translate tier format:
    - Title: `# @claude-patterns/workforce-scheduling-coordinator` (note: use @claude-patterns/ prefix per Phase 04 TS convention, but this is Python — check existing Python READMEs. Phase 04 learned rule: Python READMEs use bare names. Use `# workforce-scheduling-coordinator`)
    - Actually per D-04 and Phase 04 lesson: Python README headings use bare names. So: `# workforce-scheduling-coordinator`
    - Tier: Translate — Python, P1
    - Source Pattern: Multi-agent coordinator (Section 24, Pattern from coordinator/coordinatorMode.ts 369 LOC)
    - KB sections: Section 24, Section 43
    - Domain Translation table from spec
    - Exports list
    - Dependencies: None
    - Status: Type stubs only. All methods raise NotImplementedError.

    **#33 genesys-flow-security-validator (TS):**

    package.json:
    ```json
    {
      "name": "@claude-patterns/genesys-flow-security-validator",
      "version": "0.0.0",
      "private": true,
      "main": "src/index.ts",
      "types": "src/index.ts"
    }
    ```

    tsconfig.json:
    ```json
    {
      "extends": "../../../tsconfig.base.json",
      "compilerOptions": { "rootDir": "src", "outDir": "dist" },
      "include": ["src"]
    }
    ```

    src/index.ts:
    - JSDoc header: explains translation from security audit patterns (Sections 8-10, 38) to Genesys Architect flow validation
    - `export type ValidationSeverity = "critical" | "warning" | "info";`
    - `export interface ArchitectNode`: id: string, type: string, label: string, properties: Record<string, unknown>, outgoingEdges: string[]
    - `export interface ArchitectFlow`: id: string, name: string, type: string, nodes: ArchitectNode[], edges: Array<{ source: string; target: string }>, version: string
    - `export interface FlowValidationRule`: id: string, severity: ValidationSeverity, check: (flow: ArchitectFlow) => FlowVulnerability[], description: string
    - `export interface FlowVulnerability`: ruleId: string, severity: ValidationSeverity, nodeId: string, flowName: string, description: string, remediation: string
    - `export interface FlowValidationResult`: valid: boolean, vulnerabilities: FlowVulnerability[], checkedRules: number, flowName: string
    - `export function validateFlow(flow: ArchitectFlow): FlowValidationResult` — throw new Error("TODO: translate from security audit patterns")
    - `export function getBuiltInRules(): FlowValidationRule[]` — throw new Error("TODO: ...")

    README.md — TS translate format with @claude-patterns/ prefix:
    - Title: `# @claude-patterns/genesys-flow-security-validator`
    - Tier: Translate — TS, P1
    - Source Pattern, KB sections, Domain Translation table, Exports, Dependencies (None), Status
  </action>
  <verify>
    <automated>cd "/Users/cpconnor/projects/Inside Claude Code" && pip install -e packages/translate/workforce-scheduling-coordinator 2>&1 | tail -3 && npx tsc --noEmit -p packages/translate/genesys-flow-security-validator/tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - packages/translate/workforce-scheduling-coordinator/pyproject.toml contains `name = "claude-patterns-workforce-scheduling-coordinator"`
    - packages/translate/workforce-scheduling-coordinator/src/workforce_scheduling_coordinator/__init__.py contains `class SchedulingCoordinator`
    - packages/translate/workforce-scheduling-coordinator/src/workforce_scheduling_coordinator/__init__.py contains `class JobType(Enum)`
    - packages/translate/workforce-scheduling-coordinator/src/workforce_scheduling_coordinator/__init__.py contains `def dispatch_job`
    - packages/translate/workforce-scheduling-coordinator/src/workforce_scheduling_coordinator/__init__.py contains `raise NotImplementedError`
    - packages/translate/workforce-scheduling-coordinator/README.md contains `workforce-scheduling-coordinator`
    - packages/translate/genesys-flow-security-validator/package.json contains `"name": "@claude-patterns/genesys-flow-security-validator"`
    - packages/translate/genesys-flow-security-validator/src/index.ts contains `export function validateFlow`
    - packages/translate/genesys-flow-security-validator/src/index.ts contains `export function getBuiltInRules`
    - packages/translate/genesys-flow-security-validator/src/index.ts contains `export interface ArchitectFlow`
    - packages/translate/genesys-flow-security-validator/src/index.ts contains `export interface FlowValidationRule`
    - packages/translate/genesys-flow-security-validator/src/index.ts contains `throw new Error("TODO:`
    - packages/translate/genesys-flow-security-validator/README.md contains `@claude-patterns/genesys-flow-security-validator`
    - npx tsc --noEmit -p packages/translate/genesys-flow-security-validator/tsconfig.json exits 0
  </acceptance_criteria>
  <done>
    #32 workforce-scheduling-coordinator installs via pip install -e without errors. #33 genesys-flow-security-validator compiles via tsc --noEmit without errors. Both have README, manifest, and entry point stubs.
  </done>
</task>

<task type="auto">
  <name>Task 2: Scaffold #34 multi-step-ivr-input-validator (TS with cross-package dep)</name>
  <files>
    packages/translate/multi-step-ivr-input-validator/package.json,
    packages/translate/multi-step-ivr-input-validator/tsconfig.json,
    packages/translate/multi-step-ivr-input-validator/src/index.ts,
    packages/translate/multi-step-ivr-input-validator/README.md
  </files>
  <read_first>
    packages/translate/ivr-call-flow-validator/src/index.ts,
    packages/translate/ivr-call-flow-validator/package.json,
    packages/translate/ivr-call-flow-validator/tsconfig.json,
    packages/translate/ivr-call-flow-validator/README.md,
    SCAFFOLD-EXPANSION-PROMPT.md
  </read_first>
  <action>
    Create #34 multi-step-ivr-input-validator with a cross-package dependency on @claude-patterns/ivr-call-flow-validator (per D-04: match real package signatures).

    package.json — includes workspace dependency:
    ```json
    {
      "name": "@claude-patterns/multi-step-ivr-input-validator",
      "version": "0.0.0",
      "private": true,
      "main": "src/index.ts",
      "types": "src/index.ts",
      "dependencies": {
        "@claude-patterns/ivr-call-flow-validator": "workspace:*"
      }
    }
    ```

    tsconfig.json — standard translate-tier pattern:
    ```json
    {
      "extends": "../../../tsconfig.base.json",
      "compilerOptions": { "rootDir": "src", "outDir": "dist" },
      "include": ["src"]
    }
    ```

    src/index.ts:
    - JSDoc header: explains translation from compound command decomposition (Section 8.6, Pattern 8) to multi-step DTMF sequence validation
    - `import type { IVRCallFlow, IVRNode } from "@claude-patterns/ivr-call-flow-validator";` (use `import type` per Phase 03 convention)
    - `export type { IVRCallFlow, IVRNode } from "@claude-patterns/ivr-call-flow-validator";` (re-export for consumer convenience)
    - `export interface DTMFStep`: input: string, expectedNodeId?: string, actualNodeId?: string
    - `export interface DTMFSequence`: steps: DTMFStep[], entryNodeId: string
    - `export interface SequenceValidationResult`: valid: boolean, failedAtStep: number | null, steps: DTMFStep[], reachableEndpoint: string | null
    - `export function validateSequence(flow: IVRCallFlow, sequence: string[]): SequenceValidationResult` — throw new Error("TODO: translate from compound command decomposition pattern")
    - `export function decomposeInput(rawInput: string): string[]` — throw new Error("TODO: ...") — splits "1,3,2" or "1-3-2" into individual steps
    - `export function generateAllSequences(flow: IVRCallFlow, maxDepth?: number): DTMFSequence[]` — throw new Error("TODO: ...")
    - `export function findDeadEndSequences(flow: IVRCallFlow): DTMFSequence[]` — throw new Error("TODO: ...")

    README.md — TS translate format:
    - Title: `# @claude-patterns/multi-step-ivr-input-validator`
    - Tier: Translate — TS, P2
    - Source Pattern: Compound command decomposition (Section 8.6, Pattern 8)
    - KB sections: Section 8.6, Section 43
    - Domain Translation table from spec
    - Exports list
    - Dependencies: `@claude-patterns/ivr-call-flow-validator`
    - Status: Type stubs only.

    After creating files, run `bun install` from project root to resolve the workspace dependency.
  </action>
  <verify>
    <automated>cd "/Users/cpconnor/projects/Inside Claude Code" && bun install && npx tsc --noEmit -p packages/translate/multi-step-ivr-input-validator/tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - packages/translate/multi-step-ivr-input-validator/package.json contains `"@claude-patterns/ivr-call-flow-validator": "workspace:*"`
    - packages/translate/multi-step-ivr-input-validator/src/index.ts contains `import type { IVRCallFlow, IVRNode } from "@claude-patterns/ivr-call-flow-validator"`
    - packages/translate/multi-step-ivr-input-validator/src/index.ts contains `export interface DTMFSequence`
    - packages/translate/multi-step-ivr-input-validator/src/index.ts contains `export interface DTMFStep`
    - packages/translate/multi-step-ivr-input-validator/src/index.ts contains `export interface SequenceValidationResult`
    - packages/translate/multi-step-ivr-input-validator/src/index.ts contains `export function validateSequence`
    - packages/translate/multi-step-ivr-input-validator/src/index.ts contains `export function decomposeInput`
    - packages/translate/multi-step-ivr-input-validator/src/index.ts contains `export function generateAllSequences`
    - packages/translate/multi-step-ivr-input-validator/src/index.ts contains `export function findDeadEndSequences`
    - packages/translate/multi-step-ivr-input-validator/src/index.ts contains `throw new Error("TODO:`
    - packages/translate/multi-step-ivr-input-validator/README.md contains `@claude-patterns/multi-step-ivr-input-validator`
    - packages/translate/multi-step-ivr-input-validator/README.md contains `ivr-call-flow-validator`
    - npx tsc --noEmit -p packages/translate/multi-step-ivr-input-validator/tsconfig.json exits 0
  </acceptance_criteria>
  <done>
    #34 multi-step-ivr-input-validator compiles via tsc --noEmit. Its import of IVRCallFlow and IVRNode from @claude-patterns/ivr-call-flow-validator resolves. README documents the dependency.
  </done>
</task>

</tasks>

<verification>
Per D-05: Run `make type-check` and lint to confirm all 3 new packages compile.
```bash
cd "/Users/cpconnor/projects/Inside Claude Code"
npx tsc --noEmit -p packages/translate/genesys-flow-security-validator/tsconfig.json
npx tsc --noEmit -p packages/translate/multi-step-ivr-input-validator/tsconfig.json
pip install -e packages/translate/workforce-scheduling-coordinator
```
</verification>

<success_criteria>
- 3 new directories exist under packages/translate/
- Python package #32 installs with pip install -e
- TS packages #33, #34 compile with tsc --noEmit
- #34 successfully imports types from existing #28 ivr-call-flow-validator
- All 3 packages have README.md, manifest, and entry point stubs
</success_criteria>

<output>
After completion, create `.planning/phases/05-scaffold-expansion-add-12-new-packages-32-43-from-gap-analysis/05-01-SUMMARY.md`
</output>
