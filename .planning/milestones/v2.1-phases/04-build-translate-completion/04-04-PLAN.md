---
phase: 04-build-translate-completion
plan: 04
type: execute
wave: 3
depends_on: ["04-01", "04-02", "04-03"]
files_modified:
  - docs/DEVOPS-HANDOFF.md
  - dependency-graph.md
  - packages/build/vim-mode-fsm/README.md
  - packages/build/keyboard-shortcuts/README.md
  - packages/build/ink-renderer/README.md
  - packages/build/cli-startup-optimization/README.md
  - packages/build/mcp-integration/README.md
  - packages/build/agent-dialogue-loop/README.md
  - packages/build/skills-system/README.md
  - packages/build/multi-agent-coordinator/README.md
  - packages/translate/ivr-call-flow-validator/README.md
  - packages/translate/agent-skill-routing/README.md
  - ruff.toml
autonomous: true
requirements: [FR-1.5, FR-1.6, FR-1.7, FR-5.3, FR-5.4, NFR-5]

must_haves:
  truths:
    - "docs/DEVOPS-HANDOFF.md has environment requirements, how-to-run, security notes, known debt sections"
    - "Every package README.md has description, source paths, LOC, deps, KB section, key exports"
    - "make lint runs Biome on TS and Ruff on Python without errors"
    - "No runtime code exists in any package — all implementations are TODO throws"
    - "dependency-graph.md shows all 6 dependency chains accurately"
  artifacts:
    - path: "docs/DEVOPS-HANDOFF.md"
      provides: "DevOps delivery document"
      contains: "Environment Requirements"
    - path: "ruff.toml"
      provides: "Ruff linter config for Python packages"
      contains: "target-version"
    - path: "dependency-graph.md"
      provides: "Cross-package dependency map"
      contains: "agent-dialogue-loop"
  key_links: []
---

<objective>
Finalize documentation and linting: update docs/DEVOPS-HANDOFF.md to full spec, verify dependency-graph.md completeness, add Ruff config for Python, verify all 31 package READMEs are complete, verify no runtime code exists.

Purpose: Complete all documentation and linting requirements for project handoff.
Output: Updated DEVOPS-HANDOFF.md, ruff.toml, verified READMEs, verified no-runtime-code compliance.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ruff config and DEVOPS-HANDOFF.md update</name>
  <files>
    ruff.toml,
    docs/DEVOPS-HANDOFF.md
  </files>
  <read_first>
    docs/DEVOPS-HANDOFF.md,
    biome.json,
    Makefile,
    dependency-graph.md,
    CLAUDE.md,
    README.md
  </read_first>
  <action>
**ruff.toml** (FR-1.7): Create root-level Ruff config for Python packages:
- target-version = "py311"
- line-length = 120
- src = ["packages/translate/consecutive-breach-tracker/src", "packages/translate/cost-per-interaction/src", "packages/translate/agent-skill-routing/src"]
- select: E (pycodestyle errors), F (pyflakes), I (isort), UP (pyupgrade), B (flake8-bugbear)
- ignore: E501 (line length handled by line-length setting)

**docs/DEVOPS-HANDOFF.md** (FR-5.4): Rewrite to full DevOps handoff spec. Must include these sections:
1. **Project Summary**: Type-stub monorepo for 31 Claude Code subsystems, reference only, no runtime
2. **Environment Requirements**: Bun >= 1.0 (TypeScript), Python >= 3.11 (translate tier), Node.js >= 18 (tsc), no database/external services
3. **How to Run**: `bun install`, `make scaffold-check` (31/31), `make type-check` (28 TS packages), `make lint` (Biome + Ruff), `pip install -e packages/translate/{name}` for Python
4. **Configuration Reference**: tsconfig.base.json (strict, ES2022, Bun types), biome.json (Biome v2), ruff.toml (py311), Makefile targets
5. **Security Notes**: No secrets, no API keys, no external service connections. All packages are type stubs — no executable code. Source paths reference claude-code repo but do not vendor it
6. **Deployment Maturity**: Not deployed — local reference only. No CI/CD, no registry publishing, no Docker
7. **Known Technical Debt**: All implementations are TODO throws. No tests (nothing to test). Coverage N/A. Some high-level stubs for ink-renderer (19K LOC simplified to ~50 lines)
8. **Package Inventory**: Table of all 31 packages with tier, priority, LOC, dep count

**FR-1.5 verification**: Read dependency-graph.md and verify it includes all 6 dependency chains from REQUIREMENTS.md. If any are missing, add them. The chains are:
- permission-system <- yolo-classifier
- permission-system <- dangerous-command-detection
- token-estimation <- auto-compact
- path-validation <- sandbox-config
- path-validation <- dangerous-command-detection
- claudemd-memory <- skills-system
- mcp-integration <- multi-agent-coordinator
- streaming-tool-executor + state-store + token-estimation <- agent-dialogue-loop

**FR-1.6 verification**: Biome config already exists. Run `npx @biomejs/biome check packages/extract packages/build packages/translate/ivr-call-flow-validator packages/translate/prompt-cache-optimizer --no-errors-on-unmatched` to verify it works. If errors, fix them.

**NFR-5 verification**: Scan all src/index.ts and __init__.py files to confirm no runtime implementations — only TODO throws and NotImplementedError. Report any violations.
  </action>
  <verify>
    <automated>cd "/Users/cpconnor/projects/Inside Claude Code" && test -f ruff.toml && wc -l docs/DEVOPS-HANDOFF.md | awk '{if ($1 > 80) print "DEVOPS OK"; else print "DEVOPS TOO SHORT"}' && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - ruff.toml exists with py311 target, correct src paths for all 3 Python packages
    - docs/DEVOPS-HANDOFF.md has all 8 required sections (80+ lines)
    - dependency-graph.md includes all 8 dependency chains including new Phase 4 deps
    - Biome config verified working
    - NFR-5 verified: no runtime code in any package
  </acceptance_criteria>
  <done>Ruff config created, DEVOPS-HANDOFF.md fully rewritten, dependency-graph.md verified complete, linting verified, NFR-5 confirmed</done>
</task>

<task type="auto">
  <name>Task 2: Verify and update all package READMEs</name>
  <files>
    packages/build/vim-mode-fsm/README.md,
    packages/build/keyboard-shortcuts/README.md,
    packages/build/ink-renderer/README.md,
    packages/build/cli-startup-optimization/README.md,
    packages/build/mcp-integration/README.md,
    packages/build/agent-dialogue-loop/README.md,
    packages/build/skills-system/README.md,
    packages/build/multi-agent-coordinator/README.md,
    packages/translate/ivr-call-flow-validator/README.md,
    packages/translate/agent-skill-routing/README.md
  </files>
  <read_first>
    packages/extract/permission-system/README.md,
    packages/extract/state-store/README.md,
    packages/translate/consecutive-breach-tracker/README.md
  </read_first>
  <action>
**FR-5.3**: Verify all 31 package READMEs exist and follow consistent format. For each README, ensure it contains:
1. Package name as heading (# @claude-patterns/{name})
2. One-line description
3. **Tier**: Extract/Build/Translate + Priority
4. **Source**: Exact source file paths and LOC count
5. **KB Reference**: Knowledge Base section number
6. **Dependencies**: List of @claude-patterns/ deps or "None"
7. **Key Exports**: Bulleted list of primary exports (types, functions, classes)

Read all 31 READMEs. For the 10 new Phase 4 packages, the READMEs were created alongside the stubs — verify they follow the format above. For the 21 existing packages (Phase 2+3), spot-check 3-5 to confirm format. If any README is missing content, add it.

Focus verification on the 10 new packages created in 04-01, 04-02, 04-03. These are most likely to need updates since they were just scaffolded.

Also run `make scaffold-check` to confirm 31/31 packages present.
  </action>
  <verify>
    <automated>cd "/Users/cpconnor/projects/Inside Claude Code" && make scaffold-check 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - make scaffold-check reports 31/31 packages present
    - All 10 new package READMEs have: heading, description, tier, source, KB ref, deps, exports
    - Existing package READMEs verified consistent
  </acceptance_criteria>
  <done>All 31 package READMEs verified complete per FR-5.3, scaffold-check passes 31/31</done>
</task>

</tasks>

<verification>
cd "/Users/cpconnor/projects/Inside Claude Code" && make scaffold-check && make type-check 2>&1 | tail -3 && test -f ruff.toml && test -f docs/DEVOPS-HANDOFF.md && echo "ALL CHECKS PASS"
</verification>

<success_criteria>
- make scaffold-check: 31/31 packages present
- make type-check: 28 TS packages pass
- docs/DEVOPS-HANDOFF.md complete with all sections
- ruff.toml exists for Python linting
- dependency-graph.md includes all dependency chains
- No runtime code in any package (NFR-5)
</success_criteria>

<output>
After completion, create `.planning/phases/04-build-translate-completion/04-04-SUMMARY.md`
</output>
