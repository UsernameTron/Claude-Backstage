---
phase: 04-build-translate-completion
plan: 01
type: execute
wave: 2
depends_on: []
files_modified:
  - packages/build/mcp-integration/package.json
  - packages/build/mcp-integration/tsconfig.json
  - packages/build/mcp-integration/src/index.ts
  - packages/build/mcp-integration/README.md
  - packages/build/agent-dialogue-loop/package.json
  - packages/build/agent-dialogue-loop/tsconfig.json
  - packages/build/agent-dialogue-loop/src/index.ts
  - packages/build/agent-dialogue-loop/README.md
  - packages/build/skills-system/package.json
  - packages/build/skills-system/tsconfig.json
  - packages/build/skills-system/src/index.ts
  - packages/build/skills-system/README.md
  - packages/build/multi-agent-coordinator/package.json
  - packages/build/multi-agent-coordinator/tsconfig.json
  - packages/build/multi-agent-coordinator/src/index.ts
  - packages/build/multi-agent-coordinator/README.md
autonomous: true
requirements: [FR-3.3, FR-3.7, FR-3.8, FR-3.9, NFR-1, NFR-4, NFR-5]

must_haves:
  truths:
    - "All 4 build packages compile with tsc --noEmit"
    - "skills-system imports MemoryFile from @claude-patterns/claudemd-memory"
    - "multi-agent-coordinator imports McpServerConfig from @claude-patterns/mcp-integration"
    - "agent-dialogue-loop imports from streaming-tool-executor, state-store, and token-estimation"
    - "make scaffold-check counts all 4 as present"
  artifacts:
    - path: "packages/build/mcp-integration/src/index.ts"
      provides: "MCP server connection, config types, tool proxying"
      exports: ["connectToServer", "getMcpToolsCommandsAndResources", "McpServerConfig", "ScopedMcpServerConfig"]
    - path: "packages/build/agent-dialogue-loop/src/index.ts"
      provides: "QueryEngine class, ask() async generator"
      exports: ["QueryEngine", "QueryEngineConfig", "ask", "QueryParams"]
    - path: "packages/build/skills-system/src/index.ts"
      provides: "Skill loading, registration, token estimation"
      exports: ["getSkillDirCommands", "registerBundledSkill", "estimateSkillFrontmatterTokens", "BundledSkillDefinition"]
    - path: "packages/build/multi-agent-coordinator/src/index.ts"
      provides: "Coordinator mode detection, system prompt, user context"
      exports: ["isCoordinatorMode", "getCoordinatorSystemPrompt", "getCoordinatorUserContext"]
  key_links:
    - from: "packages/build/skills-system/src/index.ts"
      to: "@claude-patterns/claudemd-memory"
      via: "import type { MemoryFile } from '@claude-patterns/claudemd-memory'"
      pattern: "import.*from.*claudemd-memory"
    - from: "packages/build/multi-agent-coordinator/src/index.ts"
      to: "@claude-patterns/mcp-integration"
      via: "import type { McpServerConfig } from '@claude-patterns/mcp-integration'"
      pattern: "import.*from.*mcp-integration"
    - from: "packages/build/agent-dialogue-loop/src/index.ts"
      to: "@claude-patterns/streaming-tool-executor"
      via: "import type { ToolResult } from '@claude-patterns/streaming-tool-executor'"
      pattern: "import.*from.*streaming-tool-executor"
    - from: "packages/build/agent-dialogue-loop/src/index.ts"
      to: "@claude-patterns/state-store"
      via: "import type { Store } from '@claude-patterns/state-store'"
      pattern: "import.*from.*state-store"
    - from: "packages/build/agent-dialogue-loop/src/index.ts"
      to: "@claude-patterns/token-estimation"
      via: "import type { Message, TokenUsage } from '@claude-patterns/token-estimation'"
      pattern: "import.*from.*token-estimation"
---

<objective>
Scaffold 4 dependent build-tier packages: mcp-integration (standalone), agent-dialogue-loop (depends on streaming-tool-executor + state-store + token-estimation), skills-system (depends on claudemd-memory), multi-agent-coordinator (depends on mcp-integration).

Purpose: Complete the dependent build-tier packages with cross-package imports wired correctly.
Output: 16 files (4 packages x 4 files each), all compiling with tsc --noEmit, workspace deps resolving.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-extract-tier-completion/03-02-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- From @claude-patterns/claudemd-memory (packages/extract/claudemd-memory/src/index.ts): -->
<!-- export type MemoryTier = "managed" | "user" | "project" | "local"; -->
<!-- export interface MemoryFile { path: string; tier: MemoryTier; content: string; frontmatter?: MemoryFrontmatter; } -->
<!-- export const MAX_MEMORY_CHARACTER_COUNT = 40_000; -->
<!-- export function getMemoryFiles(): Promise<MemoryFile[]> -->
<!-- export function getClaudeMds(): Promise<string[]> -->

<!-- From @claude-patterns/streaming-tool-executor (packages/extract/streaming-tool-executor/src/index.ts): -->
<!-- export interface ToolResult { toolName: string; toolUseId: string; result: unknown; isError: boolean; } -->
<!-- export interface ToolDefinition { name: string; execute: (input: Record<string, unknown>) => Promise<unknown>; } -->
<!-- export class StreamingToolExecutor { addTool(tool: ToolDefinition): void; getCompletedResults(): ToolResult[]; } -->

<!-- From @claude-patterns/state-store (packages/extract/state-store/src/index.ts): -->
<!-- export type DeepImmutable<T> = ... -->
<!-- export interface Store<T> { getState(): DeepImmutable<T>; setState(updater: (draft: T) => void): void; subscribe(listener: Listener): () => void; } -->
<!-- export function createStore<T>(initial: T): Store<T> -->

<!-- From @claude-patterns/token-estimation (packages/extract/token-estimation/src/index.ts): -->
<!-- export interface Message { role: "user" | "assistant"; content: string | unknown[]; } -->
<!-- export interface TokenUsage { inputTokens: number; outputTokens: number; cacheReadInputTokens: number; cacheCreationInputTokens: number; } -->
<!-- export function countTokensWithAPI(messages: Message[]): Promise<TokenUsage> -->
<!-- export function roughTokenCountEstimation(text: string): number -->

Cross-package dependency pattern (established in Phase 03):
- package.json: "dependencies": { "@claude-patterns/dep-name": "workspace:*" }
- src/index.ts: import type { Type } from "@claude-patterns/dep-name";
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold mcp-integration and multi-agent-coordinator</name>
  <files>
    packages/build/mcp-integration/package.json,
    packages/build/mcp-integration/tsconfig.json,
    packages/build/mcp-integration/src/index.ts,
    packages/build/mcp-integration/README.md,
    packages/build/multi-agent-coordinator/package.json,
    packages/build/multi-agent-coordinator/tsconfig.json,
    packages/build/multi-agent-coordinator/src/index.ts,
    packages/build/multi-agent-coordinator/README.md
  </files>
  <read_first>
    packages/extract/yolo-classifier/package.json,
    packages/extract/yolo-classifier/tsconfig.json,
    packages/extract/yolo-classifier/src/index.ts
  </read_first>
  <action>
Follow established 4-file scaffold pattern with cross-package dependency wiring.

**mcp-integration** (Build P2, 12,310 LOC, KB Section 24, Source: services/mcp/ 23 files, NO deps):
- package.json: name @claude-patterns/mcp-integration, version 0.0.0, private true, main/types src/index.ts, no dependencies
- tsconfig.json: extends ../../../tsconfig.base.json, rootDir src, outDir dist, include [src]
- src/index.ts JSDoc header referencing services/mcp/ (23 files), 12,310 LOC, KB Pattern 10, Tier Build P2. Export types:
  - `McpTransport`: union "stdio" | "sse" | "http"
  - `StdioMcpServerConfig` interface: { type: "stdio", command: string, args: string[], env?: Record<string, string> }
  - `HttpMcpServerConfig` interface: { type: "http", url: string, headers?: Record<string, string> }
  - `SseMcpServerConfig` interface: { type: "sse", url: string }
  - `McpServerConfig`: union StdioMcpServerConfig | HttpMcpServerConfig | SseMcpServerConfig
  - `ScopedMcpServerConfig` interface: { scope: "local" | "project" | "user" | "managed", serverName: string, config: McpServerConfig }
  - `McpTool` interface: { name: string, description: string, inputSchema: Record<string, unknown> }
  - `McpResource` interface: { uri: string, name: string, description?: string, mimeType?: string }
  - `McpConnection` interface: { serverName: string, tools: McpTool[], resources: McpResource[], status: "connected" | "disconnected" | "error" }
  - `connectToServer(config: ScopedMcpServerConfig): Promise<McpConnection>` — TODO throw
  - `getMcpToolsCommandsAndResources(connections: McpConnection[]): { tools: McpTool[], resources: McpResource[] }` — TODO throw
  - `disconnectServer(connection: McpConnection): Promise<void>` — TODO throw
  - `proxyToolCall(connection: McpConnection, toolName: string, input: Record<string, unknown>): Promise<unknown>` — TODO throw
- README.md: source, LOC, deps (none), exports

**multi-agent-coordinator** (Build P1, 369 LOC, KB Section 25, Source: coordinator/coordinatorMode.ts, depends on mcp-integration):
- package.json: name @claude-patterns/multi-agent-coordinator, version 0.0.0, private true, main/types src/index.ts, dependencies: { "@claude-patterns/mcp-integration": "workspace:*" }
- tsconfig.json: same extends pattern
- src/index.ts JSDoc header referencing coordinator/coordinatorMode.ts (369 LOC), KB Section 25, Tier Build P1. Import from mcp-integration and export:
  - `import type { McpServerConfig, McpConnection } from "@claude-patterns/mcp-integration";`
  - `CoordinatorConfig` interface: { maxConcurrentAgents?: number, taskTimeout?: number, mcpServers?: McpServerConfig[] }
  - `AgentTask` interface: { id: string, description: string, status: "pending" | "running" | "complete" | "failed", result?: unknown }
  - `CoordinatorNotification` interface: { type: "task_started" | "task_complete" | "task_failed" | "xml_response", agentId: string, payload: string }
  - `isCoordinatorMode(): boolean` — TODO throw
  - `getCoordinatorSystemPrompt(config?: CoordinatorConfig): string` — TODO throw
  - `getCoordinatorUserContext(tasks: AgentTask[]): string` — TODO throw
  - `dispatchTask(task: AgentTask, connection?: McpConnection): Promise<AgentTask>` — TODO throw
- README.md: source, LOC, deps (mcp-integration), exports

Run `bun install` after creating both to wire workspace deps.
  </action>
  <verify>
    <automated>cd "/Users/cpconnor/projects/Inside Claude Code" && $HOME/.bun/bin/bun install && $HOME/.bun/bin/bun run tsc --noEmit -p packages/build/mcp-integration/tsconfig.json && $HOME/.bun/bin/bun run tsc --noEmit -p packages/build/multi-agent-coordinator/tsconfig.json && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - mcp-integration compiles, exports connectToServer, McpServerConfig, ScopedMcpServerConfig
    - multi-agent-coordinator compiles, imports McpServerConfig from mcp-integration
    - multi-agent-coordinator exports isCoordinatorMode, getCoordinatorSystemPrompt, getCoordinatorUserContext
    - workspace:* dependency resolves via bun install
  </acceptance_criteria>
  <done>mcp-integration and multi-agent-coordinator compile with cross-package dep wired (FR-3.8, FR-3.9)</done>
</task>

<task type="auto">
  <name>Task 2: Scaffold agent-dialogue-loop and skills-system</name>
  <files>
    packages/build/agent-dialogue-loop/package.json,
    packages/build/agent-dialogue-loop/tsconfig.json,
    packages/build/agent-dialogue-loop/src/index.ts,
    packages/build/agent-dialogue-loop/README.md,
    packages/build/skills-system/package.json,
    packages/build/skills-system/tsconfig.json,
    packages/build/skills-system/src/index.ts,
    packages/build/skills-system/README.md
  </files>
  <read_first>
    packages/extract/streaming-tool-executor/src/index.ts,
    packages/extract/state-store/src/index.ts,
    packages/extract/token-estimation/src/index.ts,
    packages/extract/claudemd-memory/src/index.ts
  </read_first>
  <action>
Follow established 4-file scaffold pattern with cross-package dependency wiring.

**agent-dialogue-loop** (Build P1, 3,024 LOC, KB Section 19, Source: query.ts + QueryEngine.ts, depends on streaming-tool-executor + state-store + token-estimation):
- package.json: name @claude-patterns/agent-dialogue-loop, version 0.0.0, private true, main/types src/index.ts, dependencies: { "@claude-patterns/streaming-tool-executor": "workspace:*", "@claude-patterns/state-store": "workspace:*", "@claude-patterns/token-estimation": "workspace:*" }
- tsconfig.json: extends ../../../tsconfig.base.json, rootDir src, outDir dist, include [src]
- src/index.ts JSDoc header referencing query.ts + QueryEngine.ts (3,024 LOC), KB Section 19, Tier Build P1. Import from deps and export:
  - `import type { ToolResult, ToolDefinition } from "@claude-patterns/streaming-tool-executor";`
  - `import type { Store } from "@claude-patterns/state-store";`
  - `import type { Message, TokenUsage } from "@claude-patterns/token-estimation";`
  - `QueryEngineConfig` interface: { model: string, maxTokens?: number, systemPrompt?: string, tools?: ToolDefinition[], store?: Store<unknown> }
  - `QueryParams` interface: { messages: Message[], signal?: AbortSignal, onTokenUsage?: (usage: TokenUsage) => void }
  - `StreamEvent` type: { type: "text", content: string } | { type: "tool_use", toolName: string, input: Record<string, unknown> } | { type: "tool_result", result: ToolResult } | { type: "error", error: Error } | { type: "done" }
  - `QueryResult` interface: { messages: Message[], tokenUsage: TokenUsage, toolResults: ToolResult[] }
  - `QueryEngine` class with constructor(config: QueryEngineConfig) and method ask(params: QueryParams): AsyncGenerator<StreamEvent, QueryResult> — both TODO throw
  - Re-export `ask` as standalone: `export async function* ask(config: QueryEngineConfig, params: QueryParams): AsyncGenerator<StreamEvent, QueryResult>` — TODO throw
- README.md: source, LOC, deps (streaming-tool-executor, state-store, token-estimation), exports

**skills-system** (Build P1, 4,066 LOC, KB Pattern 12, Source: skills/ 20 files, depends on claudemd-memory):
- package.json: name @claude-patterns/skills-system, version 0.0.0, private true, main/types src/index.ts, dependencies: { "@claude-patterns/claudemd-memory": "workspace:*" }
- tsconfig.json: same extends pattern
- src/index.ts JSDoc header referencing skills/ (20 files), 4,066 LOC, KB Pattern 12, Tier Build P1. Import from claudemd-memory and export:
  - `import type { MemoryFile } from "@claude-patterns/claudemd-memory";`
  - `BundledSkillDefinition` interface: { name: string, description: string, instructions: string, tools?: string[], model?: "sonnet" | "opus" | "haiku" | "inherit" }
  - `SkillFrontmatter` interface: { name: string, description: string, allowedTools?: string[], disableModelInvocation?: boolean, userInvocable?: boolean, contextFork?: boolean }
  - `SkillCommand` interface: { name: string, description: string, skillPath: string, source: "bundled" | "project" | "user" }
  - `SkillLoadResult` interface: { skill: BundledSkillDefinition, memoryFiles: MemoryFile[], tokenEstimate: number }
  - `getSkillDirCommands(skillDir: string): Promise<SkillCommand[]>` — TODO throw
  - `registerBundledSkill(definition: BundledSkillDefinition): void` — TODO throw
  - `estimateSkillFrontmatterTokens(frontmatter: SkillFrontmatter): number` — TODO throw
  - `loadSkill(skillPath: string): Promise<SkillLoadResult>` — TODO throw
  - `parseFrontmatter(content: string): SkillFrontmatter | null` — TODO throw
- README.md: source, LOC, deps (claudemd-memory), exports

Run `bun install` after creating both to wire workspace deps.
  </action>
  <verify>
    <automated>cd "/Users/cpconnor/projects/Inside Claude Code" && $HOME/.bun/bin/bun install && $HOME/.bun/bin/bun run tsc --noEmit -p packages/build/agent-dialogue-loop/tsconfig.json && $HOME/.bun/bin/bun run tsc --noEmit -p packages/build/skills-system/tsconfig.json && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - agent-dialogue-loop compiles, imports ToolResult from streaming-tool-executor, Store from state-store, Message/TokenUsage from token-estimation
    - skills-system compiles, imports MemoryFile from claudemd-memory
    - agent-dialogue-loop exports QueryEngine, QueryEngineConfig, ask, QueryParams
    - skills-system exports getSkillDirCommands, registerBundledSkill, estimateSkillFrontmatterTokens, BundledSkillDefinition
    - All workspace:* dependencies resolve via bun install
  </acceptance_criteria>
  <done>agent-dialogue-loop and skills-system compile with all cross-package deps wired (FR-3.3, FR-3.7)</done>
</task>

</tasks>

<verification>
cd "/Users/cpconnor/projects/Inside Claude Code" && $HOME/.bun/bin/bun run tsc --noEmit -p packages/build/mcp-integration/tsconfig.json && $HOME/.bun/bin/bun run tsc --noEmit -p packages/build/multi-agent-coordinator/tsconfig.json && $HOME/.bun/bin/bun run tsc --noEmit -p packages/build/agent-dialogue-loop/tsconfig.json && $HOME/.bun/bin/bun run tsc --noEmit -p packages/build/skills-system/tsconfig.json && echo "ALL 4 PASS"
</verification>

<success_criteria>
- All 4 dependent build packages compile with tsc --noEmit
- Cross-package imports resolve: skills-system -> claudemd-memory, multi-agent-coordinator -> mcp-integration, agent-dialogue-loop -> streaming-tool-executor + state-store + token-estimation
- make scaffold-check counts all 4 as present
</success_criteria>

<output>
After completion, create `.planning/phases/04-build-translate-completion/04-01-SUMMARY.md`
</output>
