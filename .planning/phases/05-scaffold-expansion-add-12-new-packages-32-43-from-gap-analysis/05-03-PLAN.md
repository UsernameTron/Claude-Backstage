---
phase: 05-scaffold-expansion
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/build/sdk-bridge/package.json
  - packages/build/sdk-bridge/tsconfig.json
  - packages/build/sdk-bridge/src/index.ts
  - packages/build/sdk-bridge/README.md
  - packages/build/voice-input-gating/package.json
  - packages/build/voice-input-gating/tsconfig.json
  - packages/build/voice-input-gating/src/index.ts
  - packages/build/voice-input-gating/README.md
  - packages/build/output-style-system/package.json
  - packages/build/output-style-system/tsconfig.json
  - packages/build/output-style-system/src/index.ts
  - packages/build/output-style-system/README.md
  - packages/build/onboarding-flow-engine/package.json
  - packages/build/onboarding-flow-engine/tsconfig.json
  - packages/build/onboarding-flow-engine/src/index.ts
  - packages/build/onboarding-flow-engine/README.md
autonomous: true
requirements: [EXP-40, EXP-41, EXP-42, EXP-43]

must_haves:
  truths:
    - "All 4 build-tier P3 packages compile with tsc --noEmit"
    - "Each package has 4 files: package.json, tsconfig.json, src/index.ts, README.md"
    - "All function bodies throw Error('TODO: ...')"
  artifacts:
    - path: "packages/build/sdk-bridge/src/index.ts"
      provides: "SDKBridge class, SDKMessage, SessionConfig, ControlRequest"
      contains: "export class SDKBridge"
    - path: "packages/build/voice-input-gating/src/index.ts"
      provides: "GateLayer, GateResult, checkVoiceGating, compositeGateCheck"
      contains: "export function checkVoiceGating"
    - path: "packages/build/output-style-system/src/index.ts"
      provides: "OutputStyle, loadOutputStyles, applyOutputStyle, isPlainText, MarkdownCache"
      contains: "export function loadOutputStyles"
    - path: "packages/build/onboarding-flow-engine/src/index.ts"
      provides: "OnboardingFlowEngine class, OnboardingStep, OnboardingConfig, OnboardingState"
      contains: "export class OnboardingFlowEngine"
  key_links: []
---

<objective>
Scaffold 4 build-tier P3 packages: #40 sdk-bridge, #41 voice-input-gating, #42 output-style-system, #43 onboarding-flow-engine.

Purpose: Complete the build tier expansion to 19 packages, covering SDK integration, voice gating, output styling, and onboarding.
Output: 4 new package directories under packages/build/ with all stub files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@SCAFFOLD-EXPANSION-PROMPT.md
</context>

<interfaces>
<!-- Standard build-tier 4-file pattern -->
package.json template:
```json
{
  "name": "@claude-patterns/{name}",
  "version": "0.0.0",
  "private": true,
  "main": "src/index.ts",
  "types": "src/index.ts"
}
```

tsconfig.json (identical for all build packages):
```json
{
  "extends": "../../../tsconfig.base.json",
  "compilerOptions": { "rootDir": "src", "outDir": "dist" },
  "include": ["src"]
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold #40 sdk-bridge and #41 voice-input-gating</name>
  <files>
    packages/build/sdk-bridge/package.json,
    packages/build/sdk-bridge/tsconfig.json,
    packages/build/sdk-bridge/src/index.ts,
    packages/build/sdk-bridge/README.md,
    packages/build/voice-input-gating/package.json,
    packages/build/voice-input-gating/tsconfig.json,
    packages/build/voice-input-gating/src/index.ts,
    packages/build/voice-input-gating/README.md
  </files>
  <read_first>
    packages/build/prompt-system/package.json,
    packages/build/prompt-system/tsconfig.json,
    packages/build/prompt-system/src/index.ts,
    packages/build/prompt-system/README.md,
    SCAFFOLD-EXPANSION-PROMPT.md
  </read_first>
  <action>
    Create 2 build-tier P3 packages.

    **#40 sdk-bridge src/index.ts:**
    - JSDoc header: WebSocket session management for SDK/Direct Connect mode from Section 26 — NDJSON message framing, SDKMessage dispatch, control_request permission prompts
    - `export interface SDKMessage`: type: string, payload: Record<string, unknown>, messageId: string
    - `export interface SessionConfig`: mode: "sdk" | "direct_connect" | "repl_bridge", connectionUrl: string, outboundOnly: boolean
    - `export interface ControlRequest`: requestId: string, action: string, description: string, requiresApproval: boolean
    - `export class SDKBridge`:
      - `connect(config: SessionConfig): Promise<void>` — throw TODO
      - `send(message: SDKMessage): void` — throw TODO
      - `onMessage(handler: (msg: SDKMessage) => void): void` — throw TODO
      - `handleControlRequest(request: ControlRequest): Promise<boolean>` — throw TODO
      - `disconnect(): void` — throw TODO

    **#41 voice-input-gating src/index.ts:**
    - JSDoc header: three-layer feature gating from Section 34 — remote flag check, authentication check, runtime composite check
    - `export type GateLayer = "remote_flag" | "authentication" | "runtime";`
    - `export interface GateResult`: allowed: boolean, deniedBy: GateLayer | null, reason: string
    - `export interface VoiceGatingConfig`: featureFlagKey: string, requiredAuthType: string, supportedPlatforms: string[]
    - `export function checkVoiceGating(config: VoiceGatingConfig): GateResult` — throw TODO
    - `export function checkRemoteFlag(flagKey: string): boolean` — throw TODO
    - `export function checkAuthentication(requiredType: string): boolean` — throw TODO
    - `export function checkRuntimeSupport(platform: string): boolean` — throw TODO
    - `export function compositeGateCheck(gates: GateLayer[]): GateResult` — throw TODO (check all layers in order, fail fast)

    **package.json and tsconfig.json** — standard template (substitute name).

    **README.md for each** — build tier format:
    - Title: `# @claude-patterns/{name}`
    - Tier: Build — TS, P3
    - Source Pattern and KB sections from spec
    - Architecture section
    - Exports, Dependencies (None), Status
  </action>
  <verify>
    <automated>cd "/Users/cpconnor/projects/Inside Claude Code" && npx tsc --noEmit -p packages/build/sdk-bridge/tsconfig.json && npx tsc --noEmit -p packages/build/voice-input-gating/tsconfig.json && echo "Both pass"</automated>
  </verify>
  <acceptance_criteria>
    - packages/build/sdk-bridge/package.json contains `"name": "@claude-patterns/sdk-bridge"`
    - packages/build/sdk-bridge/src/index.ts contains `export class SDKBridge`
    - packages/build/sdk-bridge/src/index.ts contains `export interface SDKMessage`
    - packages/build/sdk-bridge/src/index.ts contains `export interface SessionConfig`
    - packages/build/sdk-bridge/src/index.ts contains `export interface ControlRequest`
    - packages/build/sdk-bridge/src/index.ts contains `handleControlRequest`
    - packages/build/voice-input-gating/package.json contains `"name": "@claude-patterns/voice-input-gating"`
    - packages/build/voice-input-gating/src/index.ts contains `export function checkVoiceGating`
    - packages/build/voice-input-gating/src/index.ts contains `export function compositeGateCheck`
    - packages/build/voice-input-gating/src/index.ts contains `export type GateLayer`
    - packages/build/voice-input-gating/src/index.ts contains `export interface GateResult`
    - Both packages have README.md
    - npx tsc --noEmit passes for both packages
  </acceptance_criteria>
  <done>
    #40 sdk-bridge and #41 voice-input-gating compile via tsc --noEmit. Each has 4 files with correct stubs.
  </done>
</task>

<task type="auto">
  <name>Task 2: Scaffold #42 output-style-system and #43 onboarding-flow-engine</name>
  <files>
    packages/build/output-style-system/package.json,
    packages/build/output-style-system/tsconfig.json,
    packages/build/output-style-system/src/index.ts,
    packages/build/output-style-system/README.md,
    packages/build/onboarding-flow-engine/package.json,
    packages/build/onboarding-flow-engine/tsconfig.json,
    packages/build/onboarding-flow-engine/src/index.ts,
    packages/build/onboarding-flow-engine/README.md
  </files>
  <read_first>
    packages/build/prompt-system/package.json,
    packages/build/prompt-system/tsconfig.json,
    packages/build/prompt-system/src/index.ts,
    SCAFFOLD-EXPANSION-PROMPT.md
  </read_first>
  <action>
    Create 2 more build-tier P3 packages.

    **#42 output-style-system src/index.ts:**
    - JSDoc header: output styles and markdown rendering from Section 35 — custom styles from user/project dirs, LRU token cache (500 entries), plain-text fast path
    - `export interface OutputStyle`: name: string, description: string, keepCodingInstructions: boolean, forceForPlugin: boolean, content: string
    - `export interface OutputStyleConfig`: userStyleDir: string, projectStyleDir: string
    - `export interface MarkdownCacheConfig`: maxEntries: number, sampleSize: number
    - `export interface MarkdownCache`: get: (key: string) => string | undefined, set: (key: string, value: string) => void, has: (key: string) => boolean, clear: () => void, size: () => number
    - `export function loadOutputStyles(config: OutputStyleConfig): OutputStyle[]` — throw TODO
    - `export function applyOutputStyle(content: string, style: OutputStyle): string` — throw TODO
    - `export function isPlainText(content: string, sampleSize?: number): boolean` — throw TODO (fast path: check first 500 chars for markdown syntax)
    - `export function createMarkdownCache(config?: MarkdownCacheConfig): MarkdownCache` — throw TODO

    **#43 onboarding-flow-engine src/index.ts:**
    - JSDoc header: dynamic multi-step onboarding from Section 36 — conditional step assembly: preflight -> theme -> oauth -> api-key -> security -> terminal-setup
    - `export interface OnboardingStep`: id: string, label: string, execute: () => Promise<StepResult>, shouldSkip: () => boolean, dependsOn: string[]
    - `export interface OnboardingConfig`: steps: OnboardingStep[], authMethod: string, environment: string
    - `export interface OnboardingState`: currentStep: string, completedSteps: string[], skippedSteps: string[], errors: Array<{ stepId: string; error: string }>
    - `export interface StepResult`: success: boolean, nextStep: string | null, error: string | null
    - `export class OnboardingFlowEngine`:
      - `constructor(config: OnboardingConfig)` — throw TODO
      - `assembleSteps(config: OnboardingConfig): OnboardingStep[]` — throw TODO
      - `runStep(step: OnboardingStep): Promise<StepResult>` — throw TODO
      - `run(): Promise<OnboardingState>` — throw TODO (runs all assembled steps in order)
      - `getState(): OnboardingState` — throw TODO
      - `skipTo(stepId: string): void` — throw TODO

    **package.json and tsconfig.json** — standard template.

    **README.md for each** — build tier format:
    - Title: `# @claude-patterns/{name}`
    - Tier: Build — TS, P3
    - Source Pattern and KB sections
    - Architecture section
    - Exports, Dependencies (None), Status
  </action>
  <verify>
    <automated>cd "/Users/cpconnor/projects/Inside Claude Code" && npx tsc --noEmit -p packages/build/output-style-system/tsconfig.json && npx tsc --noEmit -p packages/build/onboarding-flow-engine/tsconfig.json && echo "Both pass"</automated>
  </verify>
  <acceptance_criteria>
    - packages/build/output-style-system/package.json contains `"name": "@claude-patterns/output-style-system"`
    - packages/build/output-style-system/src/index.ts contains `export interface OutputStyle`
    - packages/build/output-style-system/src/index.ts contains `export function loadOutputStyles`
    - packages/build/output-style-system/src/index.ts contains `export function applyOutputStyle`
    - packages/build/output-style-system/src/index.ts contains `export function isPlainText`
    - packages/build/output-style-system/src/index.ts contains `export function createMarkdownCache`
    - packages/build/output-style-system/src/index.ts contains `export interface MarkdownCache`
    - packages/build/onboarding-flow-engine/package.json contains `"name": "@claude-patterns/onboarding-flow-engine"`
    - packages/build/onboarding-flow-engine/src/index.ts contains `export class OnboardingFlowEngine`
    - packages/build/onboarding-flow-engine/src/index.ts contains `export interface OnboardingStep`
    - packages/build/onboarding-flow-engine/src/index.ts contains `export interface OnboardingState`
    - packages/build/onboarding-flow-engine/src/index.ts contains `export interface StepResult`
    - packages/build/onboarding-flow-engine/src/index.ts contains `assembleSteps`
    - packages/build/onboarding-flow-engine/src/index.ts contains `skipTo`
    - Both packages have README.md
    - npx tsc --noEmit passes for both packages
  </acceptance_criteria>
  <done>
    #42 output-style-system and #43 onboarding-flow-engine compile via tsc --noEmit. Each has 4 files with correct stubs.
  </done>
</task>

</tasks>

<verification>
Per D-05: Run type-check on all 4 new build packages.
```bash
cd "/Users/cpconnor/projects/Inside Claude Code"
for pkg in sdk-bridge voice-input-gating output-style-system onboarding-flow-engine; do
  npx tsc --noEmit -p "packages/build/$pkg/tsconfig.json"
done
```
</verification>

<success_criteria>
- 4 new directories exist under packages/build/
- All 4 compile with tsc --noEmit
- Each has package.json, tsconfig.json, src/index.ts, README.md
- All function bodies throw Error("TODO: ...")
</success_criteria>

<output>
After completion, create `.planning/phases/05-scaffold-expansion-add-12-new-packages-32-43-from-gap-analysis/05-03-SUMMARY.md`
</output>
