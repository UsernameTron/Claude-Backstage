---
phase: 03-extract-tier-completion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/extract/state-store/package.json
  - packages/extract/state-store/tsconfig.json
  - packages/extract/state-store/src/index.ts
  - packages/extract/state-store/README.md
  - packages/extract/streaming-tool-executor/package.json
  - packages/extract/streaming-tool-executor/tsconfig.json
  - packages/extract/streaming-tool-executor/src/index.ts
  - packages/extract/streaming-tool-executor/README.md
  - packages/extract/token-estimation/package.json
  - packages/extract/token-estimation/tsconfig.json
  - packages/extract/token-estimation/src/index.ts
  - packages/extract/token-estimation/README.md
  - packages/extract/subprocess-env-scrubbing/package.json
  - packages/extract/subprocess-env-scrubbing/tsconfig.json
  - packages/extract/subprocess-env-scrubbing/src/index.ts
  - packages/extract/subprocess-env-scrubbing/README.md
  - packages/extract/config-migration/package.json
  - packages/extract/config-migration/tsconfig.json
  - packages/extract/config-migration/src/index.ts
  - packages/extract/config-migration/README.md
  - packages/extract/path-validation/package.json
  - packages/extract/path-validation/tsconfig.json
  - packages/extract/path-validation/src/index.ts
  - packages/extract/path-validation/README.md
  - packages/extract/read-only-validation/package.json
  - packages/extract/read-only-validation/tsconfig.json
  - packages/extract/read-only-validation/src/index.ts
  - packages/extract/read-only-validation/README.md
  - packages/extract/analytics-killswitch/package.json
  - packages/extract/analytics-killswitch/tsconfig.json
  - packages/extract/analytics-killswitch/src/index.ts
  - packages/extract/analytics-killswitch/README.md
  - packages/extract/claudemd-memory/package.json
  - packages/extract/claudemd-memory/tsconfig.json
  - packages/extract/claudemd-memory/src/index.ts
  - packages/extract/claudemd-memory/README.md
autonomous: true
requirements: [FR-2.4, FR-2.5, FR-2.7, FR-2.9, FR-2.10, FR-2.12, FR-2.14, FR-2.15, FR-2.16, NFR-1, NFR-4]

must_haves:
  truths:
    - "All 9 standalone extract packages compile with tsc --noEmit"
    - "Each package has README.md with source references and LOC"
    - "make scaffold-check reports OK for all 12 extract packages (3 prior + 9 new)"
    - "No cross-package dependencies — each package compiles independently"
  artifacts:
    - path: "packages/extract/state-store/src/index.ts"
      provides: "Lightweight state store type stubs"
      exports: ["Store", "createStore", "DeepImmutable"]
    - path: "packages/extract/streaming-tool-executor/src/index.ts"
      provides: "Streaming tool executor type stubs"
      exports: ["StreamingToolExecutor", "addTool", "getCompletedResults", "getRemainingResults", "getUpdatedContext"]
    - path: "packages/extract/token-estimation/src/index.ts"
      provides: "Token estimation type stubs"
      exports: ["countTokensWithAPI", "roughTokenCountEstimation", "roughTokenCountEstimationForMessages", "bytesPerTokenForFileType"]
    - path: "packages/extract/subprocess-env-scrubbing/src/index.ts"
      provides: "Subprocess env scrubbing type stubs"
      exports: ["subprocessEnv", "GHA_SUBPROCESS_SCRUB"]
    - path: "packages/extract/config-migration/src/index.ts"
      provides: "Config migration type stubs"
      exports: ["runMigrations", "CURRENT_MIGRATION_VERSION", "Migration"]
    - path: "packages/extract/path-validation/src/index.ts"
      provides: "Path validation type stubs"
      exports: ["validatePath", "isPathAllowed", "expandTilde", "isDangerousRemovalPath", "PathCheckResult", "FileOperationType"]
    - path: "packages/extract/read-only-validation/src/index.ts"
      provides: "Read-only command validation type stubs"
      exports: ["checkReadOnlyConstraints", "GIT_READ_ONLY_COMMANDS"]
    - path: "packages/extract/analytics-killswitch/src/index.ts"
      provides: "Analytics event system + killswitch type stubs"
      exports: ["logEvent", "isKillswitchEnabled", "AnalyticsEvent"]
    - path: "packages/extract/claudemd-memory/src/index.ts"
      provides: "CLAUDE.md memory system type stubs"
      exports: ["getMemoryFiles", "getClaudeMds", "processMemoryFile", "MAX_MEMORY_CHARACTER_COUNT"]
  key_links:
    - from: "packages/extract/*/tsconfig.json"
      to: "tsconfig.base.json"
      via: "extends ../../../tsconfig.base.json"
      pattern: "extends.*\\.\\./\\.\\./\\.\\./tsconfig\\.base"
---

<objective>
Scaffold all 9 standalone (no cross-package deps) extract-tier packages: state-store, streaming-tool-executor, token-estimation, subprocess-env-scrubbing, config-migration, path-validation, read-only-validation, analytics-killswitch, claudemd-memory.

Purpose: These are Wave 1 packages with no dependencies on other @claude-patterns packages. They must be scaffolded before the dependent packages (yolo-classifier, auto-compact, sandbox-config, dangerous-command-detection) in Plan 03-02.

Output: 9 compilable TS packages in packages/extract/ with type stubs matching Claude Code source signatures.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-p0-package-stubs/02-01-SUMMARY.md

Critical references for the executor:
1. Source tree: /Users/cpconnor/projects/Inside Claude Code/claude-code/src/
2. Knowledge Base: /Users/cpconnor/projects/Inside Claude Code/claude-code-technical-knowledge-base-v2.1.md
3. Build Inventory: /Users/cpconnor/projects/Inside Claude Code/KB-v2.1-Build-Inventory.md
4. bun binary: $HOME/.bun/bin/bun (not in default PATH)
5. Package scope: @claude-patterns/{name}
6. All implementations are TODO comments only — type stubs, zero runtime code

<interfaces>
<!-- Established patterns from Phase 1-2 — use these exactly -->

Per-package tsconfig.json pattern (extract tier — 3-level nesting):
```json
{
  "extends": "../../../tsconfig.base.json",
  "compilerOptions": { "rootDir": "src", "outDir": "dist" },
  "include": ["src"]
}
```

Per-package package.json pattern:
```json
{
  "name": "@claude-patterns/{name}",
  "version": "0.0.0",
  "private": true,
  "main": "src/index.ts",
  "types": "src/index.ts"
}
```

From packages/extract/permission-system/src/index.ts (established stub pattern):
- JSDoc header with package name, source path, LOC, KB ref
- Constants as `as const` or `readonly`
- Interfaces/types with full field definitions
- Functions with full signatures, body: `throw new Error("TODO: extract from {source_path}");`
- No imports from other @claude-patterns packages (standalone packages only)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold state-store, streaming-tool-executor, token-estimation, subprocess-env-scrubbing, config-migration (FR-2.5, FR-2.4, FR-2.7, FR-2.9, FR-2.10)</name>
  <files>
    packages/extract/state-store/package.json,
    packages/extract/state-store/tsconfig.json,
    packages/extract/state-store/src/index.ts,
    packages/extract/state-store/README.md,
    packages/extract/streaming-tool-executor/package.json,
    packages/extract/streaming-tool-executor/tsconfig.json,
    packages/extract/streaming-tool-executor/src/index.ts,
    packages/extract/streaming-tool-executor/README.md,
    packages/extract/token-estimation/package.json,
    packages/extract/token-estimation/tsconfig.json,
    packages/extract/token-estimation/src/index.ts,
    packages/extract/token-estimation/README.md,
    packages/extract/subprocess-env-scrubbing/package.json,
    packages/extract/subprocess-env-scrubbing/tsconfig.json,
    packages/extract/subprocess-env-scrubbing/src/index.ts,
    packages/extract/subprocess-env-scrubbing/README.md,
    packages/extract/config-migration/package.json,
    packages/extract/config-migration/tsconfig.json,
    packages/extract/config-migration/src/index.ts,
    packages/extract/config-migration/README.md
  </files>
  <read_first>
    /Users/cpconnor/projects/Inside Claude Code/packages/extract/permission-system/src/index.ts,
    /Users/cpconnor/projects/Inside Claude Code/packages/extract/permission-system/package.json,
    /Users/cpconnor/projects/Inside Claude Code/packages/extract/permission-system/tsconfig.json,
    /Users/cpconnor/projects/Inside Claude Code/claude-code-technical-knowledge-base-v2.1.md (sections 7.1-7.2: lines 461-514, section 18: lines 1023-1054, section 12.1: lines 804-828, section 4: lines 283-287)
  </read_first>
  <action>
Create 4 files each for 5 packages. Use the EXACT tsconfig/package.json patterns from the interfaces block above. Every function body: `throw new Error("TODO: extract from {source_path}");`

**state-store** (FR-2.5, KB section 7, 603 LOC, source: `state/store.ts` + `state/AppStateStore.ts`):

src/index.ts type stubs:
```typescript
// Listener and onChange callback types
export type Listener = () => void;
export type OnChange<T> = (change: { newState: T; oldState: T }) => void;

// DeepImmutable utility type — recursively makes all fields readonly
export type DeepImmutable<T> = T extends Map<infer K, infer V>
  ? ReadonlyMap<DeepImmutable<K>, DeepImmutable<V>>
  : T extends Set<infer S>
    ? ReadonlySet<DeepImmutable<S>>
    : T extends object
      ? { readonly [K in keyof T]: DeepImmutable<T[K]> }
      : T;

// Store interface with getState/setState/subscribe
export interface Store<T> {
  getState(): T;
  setState(updater: (prev: T) => T): void;
  subscribe(listener: Listener): () => void;
}

// Factory function — Object.is reference equality prevents cascading re-renders
export function createStore<T>(initialState: T, onChange?: OnChange<T>): Store<T> {
  // TODO: extract from state/store.ts
  throw new Error("TODO: extract from state/store.ts");
}
```

README.md: Source ref `state/store.ts` + `state/AppStateStore.ts` (603 LOC), KB section 7, Extract tier P2. Key: DeepImmutable compile-time safety, Object.is reference equality, compatible with React useSyncExternalStore. No deps. Downstream: agent-dialogue-loop.

**streaming-tool-executor** (FR-2.4, KB section 6 pattern 6, 530 LOC, source: `services/tools/StreamingToolExecutor.ts`):

src/index.ts type stubs:
```typescript
export interface ToolResult {
  toolName: string;
  toolUseId: string;
  result: unknown;
  isError: boolean;
}

export interface ToolDefinition {
  name: string;
  execute: (input: Record<string, unknown>) => Promise<unknown>;
}

export class StreamingToolExecutor {
  constructor() {
    // TODO: extract from services/tools/StreamingToolExecutor.ts
  }
  addTool(toolUseId: string, toolName: string, input: Record<string, unknown>): void {
    // TODO: extract from services/tools/StreamingToolExecutor.ts
    throw new Error("TODO: extract from services/tools/StreamingToolExecutor.ts");
  }
  getCompletedResults(): ToolResult[] {
    // TODO: extract from services/tools/StreamingToolExecutor.ts
    throw new Error("TODO: extract from services/tools/StreamingToolExecutor.ts");
  }
  getRemainingResults(): Promise<ToolResult[]> {
    // TODO: extract from services/tools/StreamingToolExecutor.ts
    throw new Error("TODO: extract from services/tools/StreamingToolExecutor.ts");
  }
  getUpdatedContext(): Record<string, unknown> {
    // TODO: extract from services/tools/StreamingToolExecutor.ts
    throw new Error("TODO: extract from services/tools/StreamingToolExecutor.ts");
  }
}
```

README.md: Source ref `services/tools/StreamingToolExecutor.ts` (530 LOC), KB Pattern 6, Extract tier P1. Key: Overlaps tool execution with streaming response to reduce latency. No deps. Downstream: agent-dialogue-loop.

**token-estimation** (FR-2.7, KB section 18, 829 LOC, source: `services/tokenEstimation.ts` + `utils/tokens.ts`):

src/index.ts type stubs:
```typescript
export interface Message {
  role: "user" | "assistant";
  content: string | unknown[];
}

export interface TokenUsage {
  inputTokens: number;
  outputTokens: number;
  cacheReadInputTokens: number;
  cacheCreationInputTokens: number;
}

// Hybrid estimation: exact API count + rough estimate for new messages
export function countTokensWithAPI(messages: Message[]): number {
  // TODO: extract from services/tokenEstimation.ts
  throw new Error("TODO: extract from services/tokenEstimation.ts");
}

// Rough estimation: ~4 chars per token heuristic
export function roughTokenCountEstimation(text: string): number {
  // TODO: extract from utils/tokens.ts
  throw new Error("TODO: extract from utils/tokens.ts");
}

// Rough estimation for message arrays
export function roughTokenCountEstimationForMessages(messages: Message[]): number {
  // TODO: extract from services/tokenEstimation.ts
  throw new Error("TODO: extract from services/tokenEstimation.ts");
}

// File-type aware bytes-per-token ratio
export function bytesPerTokenForFileType(fileExtension: string): number {
  // TODO: extract from utils/tokens.ts
  throw new Error("TODO: extract from utils/tokens.ts");
}

// Hybrid: finds most recent exact count, adds rough estimate for new messages
export function tokenCountWithEstimation(messages: Message[]): number {
  // TODO: extract from services/tokenEstimation.ts
  throw new Error("TODO: extract from services/tokenEstimation.ts");
}
```

README.md: Source ref `services/tokenEstimation.ts` + `utils/tokens.ts` (829 LOC), KB section 18, Extract tier P1. Key: Hybrid exact+rough strategy avoids expensive API calls. No deps. Downstream: auto-compact.

**subprocess-env-scrubbing** (FR-2.9, KB section 12.1, 99 LOC, source: `utils/subprocessEnv.ts`):

src/index.ts type stubs:
```typescript
// Complete list of environment variables scrubbed from child processes in CI
export const GHA_SUBPROCESS_SCRUB = [
  "ANTHROPIC_API_KEY", "CLAUDE_CODE_OAUTH_TOKEN", "ANTHROPIC_AUTH_TOKEN",
  "ANTHROPIC_FOUNDRY_API_KEY", "ANTHROPIC_CUSTOM_HEADERS",
  "OTEL_EXPORTER_OTLP_HEADERS",
  "AWS_SECRET_ACCESS_KEY", "AWS_SESSION_TOKEN",
  "GOOGLE_APPLICATION_CREDENTIALS", "AZURE_CLIENT_SECRET",
  "ACTIONS_ID_TOKEN_REQUEST_TOKEN", "ACTIONS_ID_TOKEN_REQUEST_URL",
  "ACTIONS_RUNTIME_TOKEN", "ACTIONS_RUNTIME_URL",
  "ALL_INPUTS", "SSH_SIGNING_KEY",
] as const;

// Returns sanitized env vars for child process spawning.
// GITHUB_TOKEN intentionally NOT scrubbed (task-scoped, auto-expiring).
export function subprocessEnv(): Record<string, string | undefined> {
  // TODO: extract from utils/subprocessEnv.ts
  throw new Error("TODO: extract from utils/subprocessEnv.ts");
}
```

README.md: Source ref `utils/subprocessEnv.ts` (99 LOC), KB section 12.1, Extract tier P2. Key: Prevents credential leakage to child processes. GITHUB_TOKEN exception. No deps.

**config-migration** (FR-2.10, KB section 4, 603 LOC, source: `migrations/` 11 files):

src/index.ts type stubs:
```typescript
export const CURRENT_MIGRATION_VERSION = 11;

export interface Migration {
  version: number;
  name: string;
  migrate: (config: Record<string, unknown>) => Record<string, unknown>;
}

export interface MigrationResult {
  fromVersion: number;
  toVersion: number;
  migrationsRun: string[];
  config: Record<string, unknown>;
}

// Runs all pending migrations sequentially from current version to CURRENT_MIGRATION_VERSION
export function runMigrations(
  config: Record<string, unknown>,
  fromVersion: number,
): MigrationResult {
  // TODO: extract from migrations/
  throw new Error("TODO: extract from migrations/");
}

// Returns the list of all registered migrations
export function getMigrations(): Migration[] {
  // TODO: extract from migrations/
  throw new Error("TODO: extract from migrations/");
}
```

README.md: Source ref `migrations/` (11 files, 603 LOC), KB section 4, Extract tier P2. Key: Database migration pattern applied to CLI config. Migration names track model evolution (Fennec, Opus, Sonnet). No deps.

After creating all 5 packages, run `$HOME/.bun/bin/bun install` from project root then verify each compiles with `npx tsc --noEmit -p packages/extract/{name}/tsconfig.json`.
  </action>
  <verify>
    <automated>cd "/Users/cpconnor/projects/Inside Claude Code" && npx tsc --noEmit -p packages/extract/state-store/tsconfig.json && npx tsc --noEmit -p packages/extract/streaming-tool-executor/tsconfig.json && npx tsc --noEmit -p packages/extract/token-estimation/tsconfig.json && npx tsc --noEmit -p packages/extract/subprocess-env-scrubbing/tsconfig.json && npx tsc --noEmit -p packages/extract/config-migration/tsconfig.json && echo "ALL 5 PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "createStore" packages/extract/state-store/src/index.ts` matches
    - `grep "DeepImmutable" packages/extract/state-store/src/index.ts` matches
    - `grep "Store<T>" packages/extract/state-store/src/index.ts` matches
    - `grep "StreamingToolExecutor" packages/extract/streaming-tool-executor/src/index.ts` matches
    - `grep "addTool" packages/extract/streaming-tool-executor/src/index.ts` matches
    - `grep "getCompletedResults" packages/extract/streaming-tool-executor/src/index.ts` matches
    - `grep "countTokensWithAPI" packages/extract/token-estimation/src/index.ts` matches
    - `grep "roughTokenCountEstimation" packages/extract/token-estimation/src/index.ts` matches
    - `grep "bytesPerTokenForFileType" packages/extract/token-estimation/src/index.ts` matches
    - `grep "GHA_SUBPROCESS_SCRUB" packages/extract/subprocess-env-scrubbing/src/index.ts` matches
    - `grep "subprocessEnv" packages/extract/subprocess-env-scrubbing/src/index.ts` matches
    - `grep "ANTHROPIC_API_KEY" packages/extract/subprocess-env-scrubbing/src/index.ts` matches
    - `grep "CURRENT_MIGRATION_VERSION" packages/extract/config-migration/src/index.ts` matches
    - `grep "runMigrations" packages/extract/config-migration/src/index.ts` matches
    - All 5 packages have README.md files with source path references
    - `npx tsc --noEmit -p packages/extract/{name}/tsconfig.json` exits 0 for all 5
  </acceptance_criteria>
  <done>5 standalone extract packages (state-store, streaming-tool-executor, token-estimation, subprocess-env-scrubbing, config-migration) exist with type stubs, all compile cleanly, READMEs reference source paths and LOC</done>
</task>

<task type="auto">
  <name>Task 2: Scaffold path-validation, read-only-validation, analytics-killswitch, claudemd-memory (FR-2.12, FR-2.14, FR-2.15, FR-2.16)</name>
  <files>
    packages/extract/path-validation/package.json,
    packages/extract/path-validation/tsconfig.json,
    packages/extract/path-validation/src/index.ts,
    packages/extract/path-validation/README.md,
    packages/extract/read-only-validation/package.json,
    packages/extract/read-only-validation/tsconfig.json,
    packages/extract/read-only-validation/src/index.ts,
    packages/extract/read-only-validation/README.md,
    packages/extract/analytics-killswitch/package.json,
    packages/extract/analytics-killswitch/tsconfig.json,
    packages/extract/analytics-killswitch/src/index.ts,
    packages/extract/analytics-killswitch/README.md,
    packages/extract/claudemd-memory/package.json,
    packages/extract/claudemd-memory/tsconfig.json,
    packages/extract/claudemd-memory/src/index.ts,
    packages/extract/claudemd-memory/README.md
  </files>
  <read_first>
    /Users/cpconnor/projects/Inside Claude Code/packages/extract/permission-system/src/index.ts,
    /Users/cpconnor/projects/Inside Claude Code/packages/extract/permission-system/tsconfig.json,
    /Users/cpconnor/projects/Inside Claude Code/claude-code-technical-knowledge-base-v2.1.md (sections 10.2-10.5: lines 705-746, section 8.7: lines 601-613, section 14: lines 865-882, section 17: lines 983-1021)
  </read_first>
  <action>
Create 4 files each for 4 packages. Use EXACT tsconfig/package.json patterns from interfaces block.

**path-validation** (FR-2.12, KB section 10, 485 LOC, source: `utils/permissions/pathValidation.ts`):

src/index.ts type stubs:
```typescript
// File operation types that determine validation strictness
export type FileOperationType = "read" | "write" | "create" | "delete";

// Result of a path validation check
export interface PathCheckResult {
  allowed: boolean;
  reason: string;
  checks: {
    uncPath: boolean;      // UNC credential leak check
    tildeExpansion: boolean; // TOCTOU tilde expansion check
    shellExpansion: boolean; // $VAR, ${VAR}, $(cmd) check
    globPattern: boolean;   // *, ?, [ in write ops check
  };
}

// Dangerous files that are code execution or data leakage vectors
export const DANGEROUS_FILES: readonly string[] = [
  ".gitconfig", ".gitmodules", ".bashrc", ".bash_profile",
  ".zshrc", ".zprofile", ".profile", ".ripgreprc",
  ".mcp.json", ".claude.json",
];

// Dangerous directories
export const DANGEROUS_DIRECTORIES: readonly string[] = [
  ".git", ".vscode", ".idea", ".claude",
];

// Multi-layer path validation (UNC, tilde, shell expansion, glob)
export function validatePath(
  path: string,
  operation: FileOperationType,
): PathCheckResult {
  // TODO: extract from utils/permissions/pathValidation.ts
  throw new Error("TODO: extract from utils/permissions/pathValidation.ts");
}

// Check if path is allowed given permission rules and operation type
export function isPathAllowed(
  path: string,
  operation: FileOperationType,
  workingDirectory: string,
): boolean {
  // TODO: extract from utils/permissions/pathValidation.ts
  throw new Error("TODO: extract from utils/permissions/pathValidation.ts");
}

// Safe tilde expansion — only ~ and ~/ are safe, rejects ~user, ~+, ~-
export function expandTilde(path: string): string {
  // TODO: extract from utils/permissions/pathValidation.ts
  throw new Error("TODO: extract from utils/permissions/pathValidation.ts");
}

// Blocks deletion of root, Windows drive roots, user home, root children, wildcards
export function isDangerousRemovalPath(path: string): boolean {
  // TODO: extract from utils/permissions/pathValidation.ts
  throw new Error("TODO: extract from utils/permissions/pathValidation.ts");
}

// Case normalization for case-insensitive filesystem defense
export function normalizeCaseForComparison(path: string): string {
  // TODO: extract from utils/permissions/pathValidation.ts
  throw new Error("TODO: extract from utils/permissions/pathValidation.ts");
}

// Check if path is a Claude configuration file requiring confirmation
export function isClaudeConfigFilePath(path: string): boolean {
  // TODO: extract from utils/permissions/pathValidation.ts
  throw new Error("TODO: extract from utils/permissions/pathValidation.ts");
}
```

README.md: Source ref `utils/permissions/pathValidation.ts` (485 LOC), KB section 10, Extract tier P2. Key: 4-layer validation (UNC, tilde TOCTOU, shell expansion, glob). Case normalization for macOS/Windows. No deps. Downstream: sandbox-config, dangerous-command-detection.

**read-only-validation** (FR-2.14, KB section 8.7, ~500 LOC, source: `utils/shell/readOnlyCommandValidation.ts`):

src/index.ts type stubs:
```typescript
// Per-flag safety classification
export type FlagType = "none" | "number" | "string";

export interface CommandSafeFlags {
  [flag: string]: FlagType;
}

export interface ReadOnlyCommand {
  safeFlags: CommandSafeFlags;
}

// Git read-only commands with per-flag safety classification
export const GIT_READ_ONLY_COMMANDS: Readonly<Record<string, ReadOnlyCommand>> = {
  branch: { safeFlags: { "--all": "none", "--list": "none", "-v": "none" } },
  log: { safeFlags: { "--oneline": "none", "--graph": "none", "-n": "number" } },
  status: { safeFlags: { "--short": "none", "--branch": "none" } },
  diff: { safeFlags: { "--stat": "none", "--name-only": "none" } },
  show: { safeFlags: { "--stat": "none", "--name-only": "none" } },
  tag: { safeFlags: { "--list": "none", "-l": "none" } },
  remote: { safeFlags: { "-v": "none" } },
  stash: { safeFlags: { list: "none" as unknown as FlagType } },
} as const;

// gh CLI read-only commands
export const GH_READ_ONLY_COMMANDS: readonly string[] = [
  "pr list", "pr view", "pr status", "pr checks",
  "issue list", "issue view", "issue status",
  "repo view", "run list", "run view",
];

// Check if a command matches read-only constraints for plan mode
export function checkReadOnlyConstraints(command: string): boolean {
  // TODO: extract from utils/shell/readOnlyCommandValidation.ts
  throw new Error("TODO: extract from utils/shell/readOnlyCommandValidation.ts");
}

// Parse command string into executable and arguments
export function parseCommand(command: string): { executable: string; args: string[] } {
  // TODO: extract from utils/shell/readOnlyCommandValidation.ts
  throw new Error("TODO: extract from utils/shell/readOnlyCommandValidation.ts");
}
```

README.md: Source ref `utils/shell/readOnlyCommandValidation.ts` (~500 LOC), KB section 8.7, Extract tier P2. Key: Per-flag safety classification for git, gh, other tools. Used in plan mode to allow read-only operations. No deps.

**analytics-killswitch** (FR-2.15, KB section 14, 4,040 LOC, source: `services/analytics/` 9 files):

src/index.ts type stubs:
```typescript
// Analytics event with PII-safe metadata
export interface AnalyticsEvent {
  name: string;
  properties: Record<string, string | number | boolean>;
  timestamp: number;
}

// Type-governance names that force developer attestation (KB section 12.4)
export type AnalyticsMetadata_I_VERIFIED_THIS_IS_NOT_CODE_OR_FILEPATHS = never;
export type AnalyticsMetadata_I_VERIFIED_THIS_IS_PII_TAGGED = never;

// Telemetry sink for routing events to backends
export interface TelemetrySink {
  send(event: AnalyticsEvent): void;
  flush(): Promise<void>;
  isEnabled(): boolean;
}

// Backend routes: Datadog (operational), first-party (product), BigQuery (warehouse)
export type TelemetryBackend = "datadog" | "firstParty" | "bigQuery";

// Log an analytics event — events queue before sink attachment
export function logEvent(event: AnalyticsEvent): void {
  // TODO: extract from services/analytics/
  throw new Error("TODO: extract from services/analytics/");
}

// Check if killswitch is active — can disable all telemetry remotely
export function isKillswitchEnabled(): boolean {
  // TODO: extract from services/analytics/sinkKillswitch.ts
  throw new Error("TODO: extract from services/analytics/sinkKillswitch.ts");
}

// Initialize the event system — idempotent, queue drains via queueMicrotask()
export function initializeAnalytics(sinks: TelemetrySink[]): void {
  // TODO: extract from services/analytics/
  throw new Error("TODO: extract from services/analytics/");
}

// _PROTO_ prefix routes to protected BigQuery columns, stripped before Datadog
export function isProtectedField(fieldName: string): boolean {
  // TODO: extract from services/analytics/
  throw new Error("TODO: extract from services/analytics/");
}
```

README.md: Source ref `services/analytics/` (9 files, 4,040 LOC), KB section 14, Extract tier P3. Key: Remote killswitch for emergency privacy response, _PROTO_ prefix PII protection, type-name governance. No deps.

**claudemd-memory** (FR-2.16, KB section 17, 2,565 LOC, source: `utils/claudemd.ts` + `skills/loadSkillsDir.ts`):

src/index.ts type stubs:
```typescript
// Maximum characters per CLAUDE.md file
export const MAX_MEMORY_CHARACTER_COUNT = 40_000;

// Memory file tier in the 4-tier hierarchy
export type MemoryTier = "managed" | "user" | "project" | "local";

// Represents a loaded CLAUDE.md memory file
export interface MemoryFile {
  path: string;
  tier: MemoryTier;
  content: string;
  frontmatter?: MemoryFrontmatter;
}

// Frontmatter conditional rules for path-scoped CLAUDE.md
export interface MemoryFrontmatter {
  paths?: string[];  // glob patterns like src/components/**
}

// @include directive resolution result
export interface IncludeResult {
  resolvedContent: string;
  includedPaths: string[];
  circularRefs: string[];
}

// Get all memory files from the 4-tier hierarchy
// managed -> user -> project -> local (last = highest priority)
export function getMemoryFiles(workingDirectory: string): MemoryFile[] {
  // TODO: extract from utils/claudemd.ts
  throw new Error("TODO: extract from utils/claudemd.ts");
}

// Get CLAUDE.md files traversing upward from CWD to root
export function getClaudeMds(startDir: string): string[] {
  // TODO: extract from utils/claudemd.ts
  throw new Error("TODO: extract from utils/claudemd.ts");
}

// Process a single memory file: parse frontmatter, resolve @include, strip HTML comments
export function processMemoryFile(filePath: string): MemoryFile {
  // TODO: extract from utils/claudemd.ts
  throw new Error("TODO: extract from utils/claudemd.ts");
}

// Resolve @include directives: @path, @./relative, @~/home, @/absolute
// Tracks circular references. Only text file types (~80+ extensions).
export function resolveIncludes(content: string, basePath: string): IncludeResult {
  // TODO: extract from utils/claudemd.ts
  throw new Error("TODO: extract from utils/claudemd.ts");
}
```

README.md: Source ref `utils/claudemd.ts` + `skills/loadSkillsDir.ts` (2,565 LOC), KB section 17, Extract tier P1. Key: 4-tier hierarchy (managed/user/project/local), @include directive with circular ref protection, frontmatter path conditionals, 40K char limit. No deps. Downstream: skills-system (Phase 4).

After creating all 4 packages, run `$HOME/.bun/bin/bun install` then verify each compiles.
  </action>
  <verify>
    <automated>cd "/Users/cpconnor/projects/Inside Claude Code" && npx tsc --noEmit -p packages/extract/path-validation/tsconfig.json && npx tsc --noEmit -p packages/extract/read-only-validation/tsconfig.json && npx tsc --noEmit -p packages/extract/analytics-killswitch/tsconfig.json && npx tsc --noEmit -p packages/extract/claudemd-memory/tsconfig.json && echo "ALL 4 PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "validatePath" packages/extract/path-validation/src/index.ts` matches
    - `grep "isPathAllowed" packages/extract/path-validation/src/index.ts` matches
    - `grep "expandTilde" packages/extract/path-validation/src/index.ts` matches
    - `grep "isDangerousRemovalPath" packages/extract/path-validation/src/index.ts` matches
    - `grep "PathCheckResult" packages/extract/path-validation/src/index.ts` matches
    - `grep "FileOperationType" packages/extract/path-validation/src/index.ts` matches
    - `grep "checkReadOnlyConstraints" packages/extract/read-only-validation/src/index.ts` matches
    - `grep "GIT_READ_ONLY_COMMANDS" packages/extract/read-only-validation/src/index.ts` matches
    - `grep "logEvent" packages/extract/analytics-killswitch/src/index.ts` matches
    - `grep "isKillswitchEnabled" packages/extract/analytics-killswitch/src/index.ts` matches
    - `grep "AnalyticsEvent" packages/extract/analytics-killswitch/src/index.ts` matches
    - `grep "getMemoryFiles" packages/extract/claudemd-memory/src/index.ts` matches
    - `grep "getClaudeMds" packages/extract/claudemd-memory/src/index.ts` matches
    - `grep "processMemoryFile" packages/extract/claudemd-memory/src/index.ts` matches
    - `grep "MAX_MEMORY_CHARACTER_COUNT" packages/extract/claudemd-memory/src/index.ts` matches
    - All 4 packages have README.md files with source path references
    - `npx tsc --noEmit -p packages/extract/{name}/tsconfig.json` exits 0 for all 4
    - `make scaffold-check` reports at least 12 extract packages (3 prior + 9 new)
  </acceptance_criteria>
  <done>4 standalone extract packages (path-validation, read-only-validation, analytics-killswitch, claudemd-memory) exist with type stubs, all compile cleanly, READMEs reference source paths and LOC. Combined with Task 1, all 9 standalone packages are complete.</done>
</task>

</tasks>

<verification>
All 9 standalone extract packages compile and scaffold-check recognizes them:
```bash
cd "/Users/cpconnor/projects/Inside Claude Code"
for pkg in state-store streaming-tool-executor token-estimation subprocess-env-scrubbing config-migration path-validation read-only-validation analytics-killswitch claudemd-memory; do
  npx tsc --noEmit -p "packages/extract/$pkg/tsconfig.json" || echo "FAIL: $pkg"
done
make scaffold-check 2>&1 | grep -c "OK"
```
Should show 0 failures and at least 12 "OK" entries from scaffold-check.
</verification>

<success_criteria>
- 9 new standalone extract packages created with package.json, tsconfig.json, src/index.ts, README.md each
- All 9 pass `tsc --noEmit`
- No cross-package imports (all standalone)
- Type exports match KB sections (7, 6, 18, 12.1, 4, 10, 8.7, 14, 17)
- All function bodies contain TODO comments referencing source paths
- `make scaffold-check` reports OK for at least 12 extract packages
</success_criteria>

<output>
After completion, create `.planning/phases/03-extract-tier-completion/03-01-SUMMARY.md`
</output>
