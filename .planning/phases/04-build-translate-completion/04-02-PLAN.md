---
phase: 04-build-translate-completion
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/build/vim-mode-fsm/package.json
  - packages/build/vim-mode-fsm/tsconfig.json
  - packages/build/vim-mode-fsm/src/index.ts
  - packages/build/vim-mode-fsm/README.md
  - packages/build/keyboard-shortcuts/package.json
  - packages/build/keyboard-shortcuts/tsconfig.json
  - packages/build/keyboard-shortcuts/src/index.ts
  - packages/build/keyboard-shortcuts/README.md
  - packages/build/ink-renderer/package.json
  - packages/build/ink-renderer/tsconfig.json
  - packages/build/ink-renderer/src/index.ts
  - packages/build/ink-renderer/README.md
  - packages/build/cli-startup-optimization/package.json
  - packages/build/cli-startup-optimization/tsconfig.json
  - packages/build/cli-startup-optimization/src/index.ts
  - packages/build/cli-startup-optimization/README.md
autonomous: true
requirements: [FR-3.4, FR-3.5, FR-3.6, FR-3.10, NFR-1, NFR-4]

must_haves:
  truths:
    - "vim-mode-fsm, keyboard-shortcuts, ink-renderer, cli-startup-optimization all compile with tsc --noEmit"
    - "make scaffold-check counts all 4 new build packages as present"
    - "Each package has README.md with source refs, LOC, KB section"
  artifacts:
    - path: "packages/build/vim-mode-fsm/src/index.ts"
      provides: "VimState, transition(), 11-state FSM types"
      exports: ["VimState", "CommandState", "PersistentState", "transition", "createInitialVimState"]
    - path: "packages/build/keyboard-shortcuts/src/index.ts"
      provides: "Keybinding loading, resolution, parsing"
      exports: ["loadKeybindings", "resolveKey", "parseKeystroke", "ParsedBinding", "KeybindingWarning", "ResolveResult"]
    - path: "packages/build/ink-renderer/src/index.ts"
      provides: "High-level render types and component stubs"
      exports: ["render", "Ink", "Box", "Text"]
    - path: "packages/build/cli-startup-optimization/src/index.ts"
      provides: "CLI startup, lazy loading, fast path patterns"
      exports: ["main", "setup"]
  key_links: []
---

<objective>
Scaffold 4 standalone build-tier packages: vim-mode-fsm, keyboard-shortcuts, ink-renderer, cli-startup-optimization. All are P3/P2 with no cross-package dependencies.

Purpose: Complete the standalone build-tier packages so Phase 04 dependent packages (Wave 2) can proceed.
Output: 16 files (4 packages x 4 files each), all compiling with tsc --noEmit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-extract-tier-completion/03-01-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- Established 4-file scaffold pattern from Phase 02/03: -->
<!-- package.json: { name, version, private, main, types } -->
<!-- tsconfig.json: { extends: "../../../tsconfig.base.json", compilerOptions: { rootDir, outDir }, include } -->
<!-- src/index.ts: JSDoc header + type stubs + TODO throws -->
<!-- README.md: Package name, source, LOC, KB section, tier, deps, exports -->

<!-- No cross-package imports needed — all 4 packages are standalone -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold vim-mode-fsm and keyboard-shortcuts</name>
  <files>
    packages/build/vim-mode-fsm/package.json,
    packages/build/vim-mode-fsm/tsconfig.json,
    packages/build/vim-mode-fsm/src/index.ts,
    packages/build/vim-mode-fsm/README.md,
    packages/build/keyboard-shortcuts/package.json,
    packages/build/keyboard-shortcuts/tsconfig.json,
    packages/build/keyboard-shortcuts/src/index.ts,
    packages/build/keyboard-shortcuts/README.md
  </files>
  <read_first>
    packages/extract/state-store/package.json,
    packages/extract/state-store/tsconfig.json,
    packages/extract/state-store/src/index.ts,
    packages/extract/state-store/README.md
  </read_first>
  <action>
Follow the established 4-file scaffold pattern exactly. For each package create package.json, tsconfig.json, src/index.ts, README.md.

**vim-mode-fsm** (Build P3, 1,513 LOC, KB Recipe 5, Source: vim/ 5 files):
- package.json: name @claude-patterns/vim-mode-fsm, version 0.0.0, private true, main/types src/index.ts, no deps
- tsconfig.json: extends ../../../tsconfig.base.json, rootDir src, outDir dist, include [src]
- src/index.ts JSDoc header referencing vim/ (5 files), 1,513 LOC, KB Recipe 5, Tier Build P3. Export types:
  - `VimMode`: union type "normal" | "insert" | "visual" | "visual_line" | "visual_block" | "replace" | "operator_pending" | "command_line" | "search" | "ex" | "select"
  - `MotionType`: union "word" | "line" | "char" | "paragraph" | "sentence" | "search" | "mark" | "scroll"
  - `OperatorType`: union "delete" | "change" | "yank" | "indent" | "outdent" | "format" | "uppercase" | "lowercase" | "toggleCase"
  - `TextObject`: union "word" | "WORD" | "sentence" | "paragraph" | "bracket" | "quote" | "tag" | "block"
  - `VimState` interface: { mode: VimMode, count: number | null, operator: OperatorType | null, register: string, lastMotion: MotionType | null }
  - `CommandState` interface: { buffer: string, cursorPosition: number }
  - `PersistentState` interface: { registers: Record<string, string>, marks: Record<string, number>, searchHistory: string[] }
  - `VimAction` interface: { type: "motion" | "operator" | "text_object" | "mode_change" | "command", payload: Record<string, unknown> }
  - `TransitionResult` interface: { nextState: VimState, sideEffects: VimAction[] }
  - `transition(state: VimState, input: string): TransitionResult` — TODO throw
  - `createInitialVimState(): VimState` — TODO throw

**keyboard-shortcuts** (Build P3, 3,159 LOC, KB Section 21, Source: keybindings/ 14 files):
- package.json: name @claude-patterns/keyboard-shortcuts, version 0.0.0, private true, main/types src/index.ts, no deps
- tsconfig.json: same pattern
- src/index.ts JSDoc header referencing keybindings/ (14 files), 3,159 LOC, Tier Build P3. Export types:
  - `KeyContext`: union of 17 context strings: "global" | "editor" | "terminal" | "panel" | "dialog" | "menu" | "sidebar" | "statusbar" | "tab" | "search" | "debug" | "explorer" | "scm" | "output" | "problems" | "extensions" | "settings"
  - `Modifier`: union "ctrl" | "alt" | "shift" | "meta" | "cmd"
  - `ParsedBinding` interface: { key: string, modifiers: Modifier[], context: KeyContext, command: string, when?: string }
  - `KeybindingWarning` interface: { type: "conflict" | "invalid" | "shadowed", message: string, bindings: ParsedBinding[] }
  - `ResolveResult` interface: { command: string | null, binding: ParsedBinding | null, conflicts: ParsedBinding[] }
  - `KeybindingSource`: union "default" | "user" | "extension" | "platform"
  - `loadKeybindings(source: KeybindingSource): ParsedBinding[]` — TODO throw
  - `resolveKey(key: string, modifiers: Modifier[], context: KeyContext): ResolveResult` — TODO throw
  - `parseKeystroke(raw: string): ParsedBinding` — TODO throw
  - `detectConflicts(bindings: ParsedBinding[]): KeybindingWarning[]` — TODO throw

README.md for both: package name, source file paths, LOC count, KB section ref, tier, priority, no deps, key exports list.
  </action>
  <verify>
    <automated>cd "/Users/cpconnor/projects/Inside Claude Code" && $HOME/.bun/bin/bun run tsc --noEmit -p packages/build/vim-mode-fsm/tsconfig.json && $HOME/.bun/bin/bun run tsc --noEmit -p packages/build/keyboard-shortcuts/tsconfig.json && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - Both packages compile with tsc --noEmit in strict mode
    - vim-mode-fsm exports VimState, CommandState, PersistentState, transition, createInitialVimState
    - keyboard-shortcuts exports loadKeybindings, resolveKey, parseKeystroke, ParsedBinding, KeybindingWarning, ResolveResult
    - README.md present for both with source refs
  </acceptance_criteria>
  <done>vim-mode-fsm and keyboard-shortcuts compile, exports match FR-3.4 and FR-3.5</done>
</task>

<task type="auto">
  <name>Task 2: Scaffold ink-renderer and cli-startup-optimization</name>
  <files>
    packages/build/ink-renderer/package.json,
    packages/build/ink-renderer/tsconfig.json,
    packages/build/ink-renderer/src/index.ts,
    packages/build/ink-renderer/README.md,
    packages/build/cli-startup-optimization/package.json,
    packages/build/cli-startup-optimization/tsconfig.json,
    packages/build/cli-startup-optimization/src/index.ts,
    packages/build/cli-startup-optimization/README.md
  </files>
  <read_first>
    packages/build/vim-mode-fsm/package.json,
    packages/build/vim-mode-fsm/tsconfig.json
  </read_first>
  <action>
Follow the same 4-file scaffold pattern.

**ink-renderer** (Build P3, 19,848 LOC, KB Section 22, Source: ink/ 97 files):
- package.json: name @claude-patterns/ink-renderer, version 0.0.0, private true, main/types src/index.ts, no deps
- tsconfig.json: same extends pattern
- src/index.ts JSDoc header referencing ink/ (97 files), 19,848 LOC, Tier Build P3. HIGH-LEVEL types only (this is the largest package — keep stubs minimal):
  - `RenderOptions` interface: { stdout?: NodeJS.WriteStream, stdin?: NodeJS.ReadStream, exitOnCtrlC?: boolean, patchConsole?: boolean }
  - `InkInstance` interface: { rerender: (tree: unknown) => void, unmount: () => void, waitUntilExit: () => Promise<void>, clear: () => void }
  - `BoxProps` interface: { flexDirection?: "row" | "column", padding?: number, margin?: number, width?: number | string, height?: number | string, borderStyle?: string, borderColor?: string }
  - `TextProps` interface: { color?: string, backgroundColor?: string, bold?: boolean, italic?: boolean, underline?: boolean, strikethrough?: boolean, dimColor?: boolean, wrap?: "wrap" | "truncate" | "truncate-end" | "truncate-middle" | "truncate-start" }
  - `ButtonProps` interface: { label: string, onPress?: () => void, isFocused?: boolean, disabled?: boolean }
  - `FrameMetrics` interface: { fps: number, frameTime: number, dropCount: number }
  - `Ink` class stub with constructor(options?: RenderOptions) and methods: render(tree: unknown): void, unmount(): void — both TODO throw
  - `render(tree: unknown, options?: RenderOptions): InkInstance` — TODO throw
  - `Box(_props: BoxProps): unknown` — component stub, TODO throw
  - `Text(_props: TextProps): unknown` — component stub, TODO throw
  - `Button(_props: ButtonProps): unknown` — component stub, TODO throw

**cli-startup-optimization** (Build P2, ~2,000 LOC, KB Section 26/Recipe 4, Source: main.tsx + setup.ts):
- package.json: name @claude-patterns/cli-startup-optimization, version 0.0.0, private true, main/types src/index.ts, no deps
- tsconfig.json: same extends pattern
- src/index.ts JSDoc header referencing main.tsx + setup.ts (~2,000 LOC), KB Recipe 4, Tier Build P2. Export types:
  - `StartupPhase`: union "pre_init" | "config_load" | "auth_check" | "mcp_connect" | "prompt_assemble" | "ready"
  - `StartupConfig` interface: { bareMode?: boolean, nonInteractive?: boolean, printMode?: boolean, resumeConversation?: boolean, model?: string }
  - `LazyModule` interface: { name: string, load: () => Promise<unknown>, priority: "critical" | "deferred" | "idle" }
  - `StartupMetrics` interface: { totalMs: number, phases: Record<StartupPhase, number>, lazyModulesDeferred: number }
  - `main(args?: string[]): Promise<void>` — TODO throw
  - `setup(config?: StartupConfig): Promise<void>` — TODO throw
  - `registerLazyModule(module: LazyModule): void` — TODO throw
  - `getStartupMetrics(): StartupMetrics` — TODO throw

README.md for both: same pattern as Task 1.
  </action>
  <verify>
    <automated>cd "/Users/cpconnor/projects/Inside Claude Code" && $HOME/.bun/bin/bun run tsc --noEmit -p packages/build/ink-renderer/tsconfig.json && $HOME/.bun/bin/bun run tsc --noEmit -p packages/build/cli-startup-optimization/tsconfig.json && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - Both packages compile with tsc --noEmit in strict mode
    - ink-renderer exports render, Ink, Box, Text, Button, FrameMetrics
    - cli-startup-optimization exports main, setup, StartupPhase, LazyModule
    - README.md present for both with source refs
  </acceptance_criteria>
  <done>ink-renderer and cli-startup-optimization compile, exports match FR-3.6 and FR-3.10</done>
</task>

</tasks>

<verification>
cd "/Users/cpconnor/projects/Inside Claude Code" && $HOME/.bun/bin/bun run tsc --noEmit -p packages/build/vim-mode-fsm/tsconfig.json && $HOME/.bun/bin/bun run tsc --noEmit -p packages/build/keyboard-shortcuts/tsconfig.json && $HOME/.bun/bin/bun run tsc --noEmit -p packages/build/ink-renderer/tsconfig.json && $HOME/.bun/bin/bun run tsc --noEmit -p packages/build/cli-startup-optimization/tsconfig.json && make scaffold-check 2>&1 | tail -5
</verification>

<success_criteria>
- All 4 build-tier packages compile with tsc --noEmit
- make scaffold-check counts 4 new build packages as present
- Each has README.md with correct source refs
</success_criteria>

<output>
After completion, create `.planning/phases/04-build-translate-completion/04-02-SUMMARY.md`
</output>
