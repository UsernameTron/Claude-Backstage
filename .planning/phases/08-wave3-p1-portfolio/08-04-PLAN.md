---
phase: 08-wave3-p1-portfolio
plan: 04
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - packages/extract/auto-compact/src/index.ts
  - packages/extract/auto-compact/src/auto-compact.test.ts
  - packages/build/agent-dialogue-loop/src/index.ts
  - packages/build/agent-dialogue-loop/src/agent-dialogue-loop.test.ts
  - packages/build/skills-system/src/index.ts
  - packages/build/skills-system/src/skills-system.test.ts
autonomous: true
requirements: [W3-05, W3-08, W3-11, NFR-01, NFR-03, NFR-04, NFR-06]

must_haves:
  truths:
    - "auto-compact shouldAutoCompact returns true when token usage exceeds effective window threshold"
    - "auto-compact getEffectiveWindow calculates modelContextWindow - min(maxOutputTokens, 20000)"
    - "agent-dialogue-loop QueryEngine.ask yields StreamEvent sequence ending with done"
    - "skills-system parseFrontmatter extracts YAML-like key-value pairs from skill files"
    - "Cross-package imports resolve at runtime for all 3 packages"
  artifacts:
    - path: "packages/extract/auto-compact/src/index.ts"
      provides: "4 compaction functions + COMPACT_THRESHOLDS constant"
      contains: "function shouldAutoCompact"
    - path: "packages/build/agent-dialogue-loop/src/index.ts"
      provides: "QueryEngine class with async generator ask()"
      contains: "class QueryEngine"
    - path: "packages/build/skills-system/src/index.ts"
      provides: "5 skill management functions"
      contains: "function parseFrontmatter"
  key_links:
    - from: "packages/extract/auto-compact/src/index.ts"
      to: "packages/extract/token-estimation/src/index.ts"
      via: "workspace:* runtime import"
      pattern: "import.*token-estimation"
    - from: "packages/build/agent-dialogue-loop/src/index.ts"
      to: "packages/extract/streaming-tool-executor/src/index.ts"
      via: "workspace:* runtime import"
      pattern: "import.*streaming-tool-executor"
    - from: "packages/build/agent-dialogue-loop/src/index.ts"
      to: "packages/extract/state-store/src/index.ts"
      via: "workspace:* runtime import"
      pattern: "import.*state-store"
    - from: "packages/build/skills-system/src/index.ts"
      to: "packages/extract/claudemd-memory/src/index.ts"
      via: "workspace:* runtime import"
      pattern: "import.*claudemd-memory"
---

<objective>
Implement 3 packages with intra-phase dependencies: auto-compact (depends on token-estimation), agent-dialogue-loop (depends on streaming-tool-executor + state-store + token-estimation), and skills-system (depends on claudemd-memory). All dependencies are from Plan 08-01.

Purpose: Complete the dependency-chain packages that integrate Wave 1 outputs. This is the final plan to close Phase 8.
Output: 3 working packages with cross-package imports verified, full monorepo type-check and lint pass.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-wave3-p1-portfolio/08-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement auto-compact with token-estimation dependency</name>
  <files>
    packages/extract/auto-compact/src/index.ts,
    packages/extract/auto-compact/src/auto-compact.test.ts
  </files>
  <read_first>
    packages/extract/auto-compact/src/index.ts,
    packages/extract/auto-compact/package.json,
    packages/extract/token-estimation/src/index.ts
  </read_first>
  <behavior>
    - COMPACT_THRESHOLDS is an object with numeric threshold values
    - getEffectiveWindow(modelContextWindow, maxOutputTokens) returns modelContextWindow - Math.min(maxOutputTokens, 20_000)
    - shouldAutoCompact(tokenUsage, effectiveWindow) returns true when tokenUsage/effectiveWindow > COMPACT_THRESHOLDS.autoCompact
    - compactConversation(messages, options) returns CompactionResult with summary and compacted messages
    - partialCompactConversation preserves recent messages, compacts older ones
  </behavior>
  <action>
    1. Read auto-compact stub for full signatures (COMPACT_THRESHOLDS, CompactionResult, CompactBoundary, 4 functions).
    2. Read token-estimation src/index.ts to understand imported types/functions (Message, roughTokenCountEstimation).
    3. Write auto-compact.test.ts:
       - Test COMPACT_THRESHOLDS has autoCompact and warningThreshold numbers
       - Test getEffectiveWindow(200000, 8192) returns 200000 - 8192 = 191808
       - Test getEffectiveWindow(200000, 30000) caps at 20000: returns 180000
       - Test shouldAutoCompact returns true when ratio > threshold
       - Test shouldAutoCompact returns false when ratio < threshold
       - Test compactConversation returns CompactionResult with summary string
       - Test partialCompactConversation keeps recent messages intact
    4. Implement COMPACT_THRESHOLDS: { autoCompact: 0.8, warningThreshold: 0.7, minTokensToCompact: 1000 }.
    5. Implement getEffectiveWindow: return modelContextWindow - Math.min(maxOutputTokens, 20_000).
    6. Implement shouldAutoCompact: compute ratio = tokenUsage / effectiveWindow, return ratio > COMPACT_THRESHOLDS.autoCompact.
    7. Implement compactConversation: import roughTokenCountEstimation from token-estimation. Summarize message content by concatenating, truncating to boundary. Return { summary: "Compacted N messages", messages: compactedMessages, boundary: { index, tokenCount } }.
    8. Implement partialCompactConversation: split messages at boundary (keep last N messages intact), compact earlier messages.

    Preserve all type signatures. Cross-package import from @claude-patterns/token-estimation must be a value import (runtime use).
  </action>
  <verify>
    <automated>$HOME/.bun/bin/bun test packages/extract/auto-compact && npx tsc --noEmit -p packages/extract/auto-compact/tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "TODO" packages/extract/auto-compact/src/index.ts returns 0
    - auto-compact.test.ts exists with at least 7 tests
    - grep "token-estimation" packages/extract/auto-compact/src/index.ts finds import
    - bun test passes
    - tsc --noEmit passes
  </acceptance_criteria>
  <done>auto-compact implemented with token-estimation imports, all tests pass, tsc clean</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement agent-dialogue-loop and skills-system</name>
  <files>
    packages/build/agent-dialogue-loop/src/index.ts,
    packages/build/agent-dialogue-loop/src/agent-dialogue-loop.test.ts,
    packages/build/skills-system/src/index.ts,
    packages/build/skills-system/src/skills-system.test.ts
  </files>
  <read_first>
    packages/build/agent-dialogue-loop/src/index.ts,
    packages/build/agent-dialogue-loop/package.json,
    packages/build/skills-system/src/index.ts,
    packages/build/skills-system/package.json,
    packages/extract/streaming-tool-executor/src/index.ts,
    packages/extract/state-store/src/index.ts,
    packages/extract/claudemd-memory/src/index.ts
  </read_first>
  <behavior>
    agent-dialogue-loop:
    - new QueryEngine(config) creates engine with tools, state store ref
    - ask(params) returns AsyncGenerator<StreamEvent, QueryResult>
    - StreamEvent union: text | tool_use | tool_result | done
    - Yields text events, then tool_use/tool_result if tools present, then done
    - Return value is QueryResult with messages, tokenUsage, toolResults

    skills-system:
    - parseFrontmatter(content) extracts key-value pairs from YAML-like frontmatter (between --- lines)
    - loadSkill(skillPath) returns SkillLoadResult with parsed frontmatter and body
    - registerBundledSkill(definition) stores skill definition in registry
    - getSkillDirCommands(dir) returns SkillCommand[] from skill directory
    - estimateSkillFrontmatterTokens(frontmatter) returns rough token count
  </behavior>
  <action>
    agent-dialogue-loop:
    1. Read stub for QueryEngineConfig, QueryParams, StreamEvent, QueryResult, QueryEngine class signatures.
    2. Read streaming-tool-executor, state-store, token-estimation stubs for imported types.
    3. Write agent-dialogue-loop.test.ts:
       - Test QueryEngine constructor accepts config
       - Test ask() yields text StreamEvent
       - Test ask() yields done as final event
       - Test ask() with tools yields tool_use then tool_result
       - Test ask() return value is QueryResult with messages array
       - Test StreamEvent types are correctly discriminated
    4. Implement QueryEngine class:
       - Constructor: store config, create StreamingToolExecutor if tools provided, create state store.
       - async *ask(params): yield {type:"text", content:"response"} for simulated text. If tools configured, yield {type:"tool_use", toolName, input}. Use executor.getRemainingResults() for tool results. Yield {type:"tool_result", result}. Yield {type:"done"}. Return QueryResult with accumulated data.
    5. Implement standalone ask() function: create QueryEngine, delegate to engine.ask().
    Keep implementation under 150 lines. This is a pattern implementation, not production streaming.

    skills-system:
    1. Read stub for all 5 function signatures and type definitions.
    2. Read claudemd-memory stub for imported types (MemoryFile, etc.).
    3. Write skills-system.test.ts:
       - Test parseFrontmatter extracts name and description from ---\nname: test\n--- format
       - Test parseFrontmatter returns empty object for content without frontmatter
       - Test loadSkill returns SkillLoadResult with frontmatter and body
       - Test registerBundledSkill stores and retrieves skill
       - Test getSkillDirCommands returns SkillCommand array
       - Test estimateSkillFrontmatterTokens returns positive number
    4. Implement parseFrontmatter: split on "---" markers, parse key: value lines between them into SkillFrontmatter object. Handle multi-value fields (comma-separated for allowed-tools).
    5. Implement loadSkill: read skill content (simulated), call parseFrontmatter, split body from frontmatter. Return { frontmatter, body, tokenEstimate }.
    6. Implement registerBundledSkill: store in module-level Map<string, BundledSkillDefinition>. Import relevant types from claudemd-memory if needed.
    7. Implement getSkillDirCommands: scan directory contents (simulated), return SkillCommand[] with name, description from parsed frontmatters.
    8. Implement estimateSkillFrontmatterTokens: JSON.stringify(frontmatter).length / 4 (rough estimate).

    Preserve all type signatures. Only replace TODO throw bodies.
  </action>
  <verify>
    <automated>$HOME/.bun/bin/bun test packages/build/agent-dialogue-loop && $HOME/.bun/bin/bun test packages/build/skills-system && npx tsc --noEmit -p packages/build/agent-dialogue-loop/tsconfig.json && npx tsc --noEmit -p packages/build/skills-system/tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "TODO" packages/build/agent-dialogue-loop/src/index.ts returns 0
    - grep -c "TODO" packages/build/skills-system/src/index.ts returns 0
    - agent-dialogue-loop.test.ts exists with at least 6 tests
    - skills-system.test.ts exists with at least 6 tests
    - grep "streaming-tool-executor" packages/build/agent-dialogue-loop/src/index.ts finds import
    - grep "state-store" packages/build/agent-dialogue-loop/src/index.ts finds import
    - grep "claudemd-memory" packages/build/skills-system/src/index.ts finds import
    - bun test passes for both packages
    - tsc --noEmit passes for both packages
  </acceptance_criteria>
  <done>Both packages implemented with cross-package imports, all tests pass, tsc clean</done>
</task>

<task type="auto">
  <name>Task 3: Full monorepo verification</name>
  <files>None (verification only)</files>
  <read_first>None</read_first>
  <action>
    Run full monorepo verification to confirm Phase 8 is complete:
    1. make type-check — all 39 TS packages must compile
    2. make lint — Biome (TS) + Ruff (Python) must pass
    3. grep -r "TODO" across all 11 Phase 8 package src/index.ts files to confirm zero remaining
    4. Run bun test on all 11 packages to confirm all test suites pass

    If any failures, fix them before completing.
  </action>
  <verify>
    <automated>cd "/Users/cpconnor/projects/Inside Claude Code" && make type-check && make lint</automated>
  </verify>
  <acceptance_criteria>
    - make type-check reports 39/0 (all pass, 0 failures)
    - make lint exits 0
    - grep -r "TODO" across all 11 package index files returns 0 matches
  </acceptance_criteria>
  <done>Full monorepo passes type-check and lint, zero TODO throws across all 11 Phase 8 packages</done>
</task>

</tasks>

<verification>
- All 3 packages compile with cross-package imports resolving
- All tests pass for auto-compact, agent-dialogue-loop, skills-system
- make type-check 39/0 and make lint clean for entire monorepo
- Zero TODO throws across all 11 Phase 8 packages
</verification>

<success_criteria>
- auto-compact correctly imports from token-estimation
- agent-dialogue-loop integrates streaming-tool-executor + state-store + token-estimation
- skills-system imports from claudemd-memory
- Full monorepo verification passes
- Phase 8 complete: all 11 P1 packages implemented
</success_criteria>

<output>
After completion, create `.planning/phases/08-wave3-p1-portfolio/08-04-SUMMARY.md`
</output>
