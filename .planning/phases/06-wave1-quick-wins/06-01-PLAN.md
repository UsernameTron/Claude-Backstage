---
phase: 06-wave1-quick-wins
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/extract/denial-tracking/src/index.ts
  - packages/extract/cost-tracker/src/index.ts
  - packages/extract/denial-tracking/src/denial-tracking.test.ts
  - packages/extract/cost-tracker/src/cost-tracker.test.ts
autonomous: true
requirements: [W1-01, W1-02, NFR-01, NFR-03, NFR-05, NFR-06]

must_haves:
  truths:
    - "DenialTracker.recordDenial() increments counters and returns 'fallback_to_interactive' when thresholds exceeded"
    - "DenialTracker.recordApproval() resets consecutive to 0 but preserves total"
    - "DenialTracker.shouldFallback() returns true when consecutive >= 3 OR total >= 20"
    - "addToTotalSessionCost() upserts by model, accumulating token fields"
    - "getStoredSessionCosts() returns entries array and totalCostUSD sum"
    - "formatTotalCost() returns '$X.XX' formatted string"
  artifacts:
    - path: "packages/extract/denial-tracking/src/index.ts"
      provides: "DenialTracker with working implementations"
      contains: "recordDenial"
    - path: "packages/extract/cost-tracker/src/index.ts"
      provides: "Session cost tracking functions"
      contains: "addToTotalSessionCost"
    - path: "packages/extract/denial-tracking/src/denial-tracking.test.ts"
      provides: "Tests for DenialTracker"
      min_lines: 20
    - path: "packages/extract/cost-tracker/src/cost-tracker.test.ts"
      provides: "Tests for cost-tracker functions"
      min_lines: 20
  key_links:
    - from: "packages/extract/denial-tracking/src/index.ts"
      to: "DENIAL_LIMITS"
      via: "shouldFallback uses DENIAL_LIMITS thresholds"
      pattern: "DENIAL_LIMITS\\.max"
---

<objective>
Implement the two TypeScript extract-tier packages: denial-tracking (45 LOC) and cost-tracker (323 LOC). Replace all TODO throws with working code while preserving existing type signatures exactly.

Purpose: These are the first two implementations in the monorepo -- they prove the TS implementation workflow before scaling to larger packages.
Output: Two working TS packages with tests, zero TODO throws, tsc --noEmit passes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@IMPLEMENTATION-PLAYBOOK.md
@packages/extract/denial-tracking/src/index.ts
@packages/extract/cost-tracker/src/index.ts
@packages/extract/denial-tracking/README.md
@packages/extract/cost-tracker/README.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement denial-tracking</name>
  <files>packages/extract/denial-tracking/src/index.ts, packages/extract/denial-tracking/src/denial-tracking.test.ts</files>
  <behavior>
    - recordDenial(): increments consecutive and total; returns "continue" when below thresholds, "fallback_to_interactive" when consecutive >= 3 or total >= 20
    - recordApproval(): resets consecutive to 0, total unchanged
    - shouldFallback(): returns true when consecutive >= DENIAL_LIMITS.maxConsecutive OR total >= DENIAL_LIMITS.maxTotal
    - reset(): sets both consecutive and total to 0
    - getState(): returns readonly copy of {consecutive, total}
    - Edge: 3 denials then approval then 3 more denials triggers fallback (consecutive resets, total does not)
    - Edge: exactly 20 total denials triggers fallback regardless of consecutive
  </behavior>
  <action>
    Read the stub at packages/extract/denial-tracking/src/index.ts (per W1-01). The interface contract is already defined -- DenialTracker class with DenialState, DENIAL_LIMITS, DenialAction types.

    Implementation logic from KB Section 8.4:
    - recordDenial(): increment this.state.consecutive++, this.state.total++, then return this.shouldFallback() ? "fallback_to_interactive" : "continue"
    - recordApproval(): set this.state.consecutive = 0 (total stays)
    - shouldFallback(): return this.state.consecutive >= DENIAL_LIMITS.maxConsecutive || this.state.total >= DENIAL_LIMITS.maxTotal
    - reset(): set both to 0
    - getState(): return { ...this.state } (shallow copy for readonly)

    Do NOT change any exported type signatures, class name, or const values. Only replace the TODO throw bodies.

    Then create test file at packages/extract/denial-tracking/src/denial-tracking.test.ts using Bun test runner (import { describe, test, expect } from "bun:test"). Test all behaviors listed above.
  </action>
  <verify>
    <automated>cd /Users/cpconnor/projects/Inside\ Claude\ Code && npx tsc --noEmit -p packages/extract/denial-tracking/tsconfig.json && bun test packages/extract/denial-tracking/src/denial-tracking.test.ts</automated>
  </verify>
  <done>DenialTracker has 5 working methods, zero TODO throws, all tests pass, tsc --noEmit clean</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement cost-tracker</name>
  <files>packages/extract/cost-tracker/src/index.ts, packages/extract/cost-tracker/src/cost-tracker.test.ts</files>
  <behavior>
    - addToTotalSessionCost(): upserts entry by model name -- if model exists, accumulates all numeric fields; if new, adds entry
    - getStoredSessionCosts(): returns {entries: [...], totalCostUSD: sum of all entry costUSD}
    - formatTotalCost(0): returns "$0.00"
    - formatTotalCost(1.5): returns "$1.50"
    - formatTotalCost(0.001): returns "$0.00" (2 decimal places)
    - saveCurrentSessionCosts(): stores costs (in-memory, no filesystem for standalone)
    - Multiple addToTotalSessionCost() calls for same model accumulate token counts
    - Multiple addToTotalSessionCost() calls for different models create separate entries
  </behavior>
  <action>
    Read the stub at packages/extract/cost-tracker/src/index.ts (per W1-02). The interface contract defines SessionCostEntry, SessionCosts, and 4 exported functions.

    Implementation approach (standalone, no external deps):
    - Module-level Map<string, SessionCostEntry> for in-memory storage
    - addToTotalSessionCost(entry): look up by entry.model. If exists, add all numeric fields (inputTokens, outputTokens, cacheReadInputTokens, cacheCreationInputTokens, webSearchRequests, costUSD). If not, insert clone of entry.
    - getStoredSessionCosts(): build entries array from map values, sum costUSD across all entries for totalCostUSD
    - formatTotalCost(costUSD): return `$${costUSD.toFixed(2)}`
    - saveCurrentSessionCosts(costs): store into the module-level map (overwrite). This is a no-op-ish for standalone -- just update internal state from the provided SessionCosts.

    Do NOT change any exported type signatures. Only replace the TODO throw bodies.

    Then create test file at packages/extract/cost-tracker/src/cost-tracker.test.ts using Bun test runner. Test all behaviors listed above, including accumulation by model and formatting edge cases.
  </action>
  <verify>
    <automated>cd /Users/cpconnor/projects/Inside\ Claude\ Code && npx tsc --noEmit -p packages/extract/cost-tracker/tsconfig.json && bun test packages/extract/cost-tracker/src/cost-tracker.test.ts</automated>
  </verify>
  <done>cost-tracker has 4 working functions, zero TODO throws, all tests pass, tsc --noEmit clean</done>
</task>

<task type="auto">
  <name>Task 3: Monorepo-wide verification</name>
  <files></files>
  <action>
    Run make type-check and make lint across the entire monorepo to confirm neither package broke anything else. Grep both implemented files for "TODO" and "throw new Error" to confirm zero stubs remain.

    If any failures, fix them in the relevant package files without changing type signatures.
  </action>
  <verify>
    <automated>cd /Users/cpconnor/projects/Inside\ Claude\ Code && make type-check && make lint</automated>
  </verify>
  <done>make type-check passes (all TS packages compile), make lint clean, no TODO throws in denial-tracking or cost-tracker</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit -p packages/extract/denial-tracking/tsconfig.json` passes
- `npx tsc --noEmit -p packages/extract/cost-tracker/tsconfig.json` passes
- `bun test packages/extract/denial-tracking/` passes
- `bun test packages/extract/cost-tracker/` passes
- `grep -r "TODO" packages/extract/denial-tracking/src/index.ts` returns empty
- `grep -r "TODO" packages/extract/cost-tracker/src/index.ts` returns empty
- `make type-check` and `make lint` pass for entire monorepo
</verification>

<success_criteria>
1. denial-tracking DenialTracker class has 5 working methods with correct threshold logic
2. cost-tracker has 4 working functions with in-memory storage and $X.XX formatting
3. Zero TODO throws remain in either package
4. Both packages compile with tsc --noEmit strict mode
5. Tests exist and pass for both packages
6. make type-check and make lint pass monorepo-wide
</success_criteria>

<output>
After completion, create `.planning/phases/06-wave1-quick-wins/06-01-SUMMARY.md`
</output>
