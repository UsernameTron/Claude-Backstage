---
phase: 02-p0-package-stubs
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/build/prompt-system/package.json
  - packages/build/prompt-system/tsconfig.json
  - packages/build/prompt-system/src/index.ts
  - packages/build/prompt-system/README.md
  - packages/build/context-injection/package.json
  - packages/build/context-injection/tsconfig.json
  - packages/build/context-injection/src/index.ts
  - packages/build/context-injection/README.md
autonomous: true
requirements: [FR-3.1, FR-3.2, NFR-1]

must_haves:
  truths:
    - "prompt-system compiles with tsc --noEmit"
    - "context-injection compiles with tsc --noEmit"
    - "Both packages have README.md with source references"
    - "make scaffold-check reports OK for both build P0 packages"
  artifacts:
    - path: "packages/build/prompt-system/src/index.ts"
      provides: "Prompt system type stubs"
      exports: ["getSystemPrompt", "SystemPromptSection", "systemPromptSection", "SYSTEM_PROMPT_DYNAMIC_BOUNDARY", "resolveSystemPromptSections"]
    - path: "packages/build/context-injection/src/index.ts"
      provides: "Context injection type stubs"
      exports: ["getSystemContext", "getUserContext", "SystemContext", "UserContext"]
  key_links:
    - from: "packages/build/*/tsconfig.json"
      to: "tsconfig.base.json"
      via: "extends ../../tsconfig.base.json"
      pattern: "extends.*tsconfig\\.base"
---

<objective>
Scaffold the 2 build-tier P0 packages: prompt-system and context-injection. Each gets package.json, tsconfig.json, src/index.ts (type stubs with TODO comments), and README.md with source references.

Purpose: These are the foundational build-tier packages. prompt-system covers the multi-layered system prompt assembly (static/dynamic boundary, cache scoping). context-injection covers dual-position context strategy (system end + first user message).

Output: 2 compilable TS packages in packages/build/ with type stubs matching Claude Code architectural patterns.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

Critical references for the executor:
1. Source tree: /Users/cpconnor/projects/Inside Claude Code/claude-code/src/
2. Knowledge Base: /Users/cpconnor/projects/Inside Claude Code/claude-code-technical-knowledge-base-v2.1.md (sections 15, 16)
3. Build Inventory: /Users/cpconnor/projects/Inside Claude Code/KB-v2.1-Build-Inventory.md
4. bun binary: $HOME/.bun/bin/bun (not in default PATH)
5. Package scope: @claude-patterns/{name}
6. All implementations are TODO comments only -- type stubs, zero runtime code

<interfaces>
<!-- Established patterns from Phase 1 -->

Per-package tsconfig.json pattern (extract/build tier):
```json
{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": { "rootDir": "src", "outDir": "dist" },
  "include": ["src"]
}
```

Per-package package.json pattern:
```json
{
  "name": "@claude-patterns/{name}",
  "version": "0.0.0",
  "private": true,
  "main": "src/index.ts",
  "types": "src/index.ts"
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold prompt-system package (FR-3.1)</name>
  <files>
    packages/build/prompt-system/package.json,
    packages/build/prompt-system/tsconfig.json,
    packages/build/prompt-system/src/index.ts,
    packages/build/prompt-system/README.md
  </files>
  <read_first>
    tsconfig.base.json,
    package.json,
    /Users/cpconnor/projects/Inside Claude Code/claude-code-technical-knowledge-base-v2.1.md (section 15: lines 887-940)
  </read_first>
  <action>
Create 4 files for the prompt-system package (per FR-3.1):

**package.json**: `@claude-patterns/prompt-system`, version 0.0.0, private, main/types src/index.ts

**tsconfig.json**: extends ../../tsconfig.base.json, rootDir src, outDir dist, include ["src"]

**src/index.ts**: Type stubs based on KB section 15. Must export:

Types/Interfaces:
- `SystemPromptSection` — interface with `id: string`, `content: string`, `type: "static" | "dynamic"`, `cached: boolean`
- `SystemPromptConfig` — interface with `outputStyleConfig?: Record<string, unknown>`, `enabledTools?: string[]`, `cacheScope?: "global" | "org" | null`
- `DynamicSectionType` — union type: `"session_guidance" | "memory" | "env_info_simple" | "language" | "output_style" | "mcp_instructions" | "scratchpad" | "frc" | "token_budget"`

Constants:
- `SYSTEM_PROMPT_DYNAMIC_BOUNDARY` — string constant `"--- DYNAMIC CONTENT BELOW ---"` (marks cache boundary between static and dynamic sections)

Functions (all throw TODO):
- `getSystemPrompt(config?: SystemPromptConfig): string` — assembles full system prompt from static sections + boundary + dynamic sections
- `systemPromptSection(id: string, content: string, type?: "static" | "dynamic"): SystemPromptSection` — factory function for creating sections
- `resolveSystemPromptSections(sections: SystemPromptSection[]): string[]` — filters nulls, returns content strings
- `getSimpleIntroSection(outputStyleConfig?: Record<string, unknown>): SystemPromptSection | null`
- `getSimpleSystemSection(): SystemPromptSection | null`
- `getSimpleDoingTasksSection(): SystemPromptSection | null`
- `getActionsSection(): SystemPromptSection | null`
- `getUsingYourToolsSection(enabledTools?: string[]): SystemPromptSection | null`
- `getSimpleToneAndStyleSection(): SystemPromptSection | null`
- `getOutputEfficiencySection(): SystemPromptSection | null`
- `shouldUseGlobalCacheScope(): boolean`

Every function body: `throw new Error("TODO: build from constants/prompts.ts + systemPromptSections.ts");`

**README.md**: Source ref `constants/prompts.ts` + `systemPromptSections.ts` (2,368 LOC), KB section 15, Build tier P0. Notes: Static/dynamic boundary for prompt cache optimization. Static sections are globally cacheable; dynamic sections change per-turn or per-org.
  </action>
  <verify>
    <automated>cd "/Users/cpconnor/projects/Inside Claude Code" && npx tsc --noEmit -p packages/build/prompt-system/tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - `grep "getSystemPrompt" packages/build/prompt-system/src/index.ts` matches
    - `grep "SystemPromptSection" packages/build/prompt-system/src/index.ts` matches
    - `grep "SYSTEM_PROMPT_DYNAMIC_BOUNDARY" packages/build/prompt-system/src/index.ts` matches
    - `grep "resolveSystemPromptSections" packages/build/prompt-system/src/index.ts` matches
    - `grep "systemPromptSection" packages/build/prompt-system/src/index.ts` matches
    - `grep "@claude-patterns/prompt-system" packages/build/prompt-system/package.json` matches
    - `grep "2,368 LOC\|2368 LOC\|prompts.ts" packages/build/prompt-system/README.md` matches
    - `npx tsc --noEmit -p packages/build/prompt-system/tsconfig.json` exits 0
  </acceptance_criteria>
  <done>prompt-system package exists with all type stubs matching KB section 15, compiles cleanly, README references source</done>
</task>

<task type="auto">
  <name>Task 2: Scaffold context-injection package (FR-3.2)</name>
  <files>
    packages/build/context-injection/package.json,
    packages/build/context-injection/tsconfig.json,
    packages/build/context-injection/src/index.ts,
    packages/build/context-injection/README.md
  </files>
  <read_first>
    tsconfig.base.json,
    /Users/cpconnor/projects/Inside Claude Code/claude-code-technical-knowledge-base-v2.1.md (section 16: lines 941-981)
  </read_first>
  <action>
Create 4 files for the context-injection package (per FR-3.2):

**package.json**: `@claude-patterns/context-injection`, version 0.0.0, private, main/types src/index.ts

**tsconfig.json**: extends ../../tsconfig.base.json, rootDir src, outDir dist, include ["src"]

**src/index.ts**: Type stubs based on KB section 16. Must export:

Types/Interfaces:
- `SystemContext` — interface with `gitBranch: string`, `mainBranch: string`, `gitUser: string`, `gitStatus: string` (truncated to 2000 chars), `recentCommits: string[]`, `cacheBreaker?: string`
- `UserContext` — interface with `claudeMdContent: string | null`, `currentDate: string`, `additionalContext?: Record<string, string>`
- `InjectionPosition` — type: `"system_end" | "first_user_message"`
- `ContextInjection` — interface with `content: string`, `position: InjectionPosition`, `wrapInSystemReminder: boolean`

Functions (all throw TODO):
- `getSystemContext(): SystemContext` — collects git status, branch, user, commits. Memoized (computed once per session). Injected at END of system prompt for high positional attention weight.
- `getUserContext(): UserContext` — collects CLAUDE.md content and date. Injected as FIRST user message wrapped in `<system-reminder>` tags for conversational framing.
- `appendSystemContext(systemPrompt: string, context: SystemContext): string` — appends context to end of system prompt
- `prependUserContext(messages: unknown[], context: UserContext): unknown[]` — prepends context as first user message with `<system-reminder>` wrapping
- `getSystemPromptInjection(): string | null` — generates cache breaker string for forced cache invalidation
- `setCachedClaudeMdContent(content: string | null): void` — side-channel cache for YOLO classifier access (breaks circular dependency: classifier -> memory -> context -> classifier)

Every function body: `throw new Error("TODO: build from context.ts + QueryEngine.ts");`

**README.md**: Source ref `context.ts` + `QueryEngine.ts` (1,484 LOC), KB section 16, Build tier P0. Notes: Dual-position injection exploits transformer attention -- system context at prompt end gets recency bias, user context as first message sets conversational frame. Include the "Why" from KB 16.1.

After creating both packages, run `bun install` from root to resolve workspaces, then verify both compile.
  </action>
  <verify>
    <automated>cd "/Users/cpconnor/projects/Inside Claude Code" && npx tsc --noEmit -p packages/build/context-injection/tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - `grep "getSystemContext" packages/build/context-injection/src/index.ts` matches
    - `grep "getUserContext" packages/build/context-injection/src/index.ts` matches
    - `grep "SystemContext" packages/build/context-injection/src/index.ts` matches
    - `grep "UserContext" packages/build/context-injection/src/index.ts` matches
    - `grep "InjectionPosition" packages/build/context-injection/src/index.ts` matches
    - `grep "appendSystemContext" packages/build/context-injection/src/index.ts` matches
    - `grep "prependUserContext" packages/build/context-injection/src/index.ts` matches
    - `grep "system-reminder\|system_end\|first_user_message" packages/build/context-injection/src/index.ts` matches
    - `grep "@claude-patterns/context-injection" packages/build/context-injection/package.json` matches
    - `grep "1,484 LOC\|1484 LOC\|context.ts" packages/build/context-injection/README.md` matches
    - `npx tsc --noEmit -p packages/build/context-injection/tsconfig.json` exits 0
  </acceptance_criteria>
  <done>context-injection package exists with dual-position type stubs matching KB section 16, compiles cleanly, README references source and explains the attention-based injection rationale</done>
</task>

</tasks>

<verification>
Both build P0 packages compile and scaffold-check recognizes them:
```bash
cd "/Users/cpconnor/projects/Inside Claude Code"
npx tsc --noEmit -p packages/build/prompt-system/tsconfig.json
npx tsc --noEmit -p packages/build/context-injection/tsconfig.json
make scaffold-check 2>&1 | grep -E "prompt-system|context-injection"
```
Both should show "OK" in scaffold-check output.
</verification>

<success_criteria>
- 2 build P0 packages created with package.json, tsconfig.json, src/index.ts, README.md each
- Both pass `tsc --noEmit`
- prompt-system exports match KB section 15 (getSystemPrompt, SYSTEM_PROMPT_DYNAMIC_BOUNDARY, section factory)
- context-injection exports match KB section 16 (dual-position types, getSystemContext, getUserContext)
- All function bodies contain TODO comments referencing source paths
- `make scaffold-check` reports OK for both packages
</success_criteria>

<output>
After completion, create `.planning/phases/02-p0-package-stubs/02-02-SUMMARY.md`
</output>
