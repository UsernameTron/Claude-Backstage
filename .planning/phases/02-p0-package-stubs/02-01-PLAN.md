---
phase: 02-p0-package-stubs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/extract/permission-system/package.json
  - packages/extract/permission-system/tsconfig.json
  - packages/extract/permission-system/src/index.ts
  - packages/extract/permission-system/README.md
  - packages/extract/denial-tracking/package.json
  - packages/extract/denial-tracking/tsconfig.json
  - packages/extract/denial-tracking/src/index.ts
  - packages/extract/denial-tracking/README.md
  - packages/extract/cost-tracker/package.json
  - packages/extract/cost-tracker/tsconfig.json
  - packages/extract/cost-tracker/src/index.ts
  - packages/extract/cost-tracker/README.md
autonomous: true
requirements: [FR-2.1, FR-2.2, FR-2.8, NFR-1, NFR-3]

must_haves:
  truths:
    - "permission-system compiles with tsc --noEmit"
    - "denial-tracking compiles with tsc --noEmit"
    - "cost-tracker compiles with tsc --noEmit"
    - "All 3 packages have README.md with source references"
    - "make scaffold-check reports OK for all 3 extract P0 packages"
  artifacts:
    - path: "packages/extract/permission-system/src/index.ts"
      provides: "Permission system type stubs"
      exports: ["hasPermissionsToUseTool", "checkRuleBasedPermissions", "getAllowRules", "getDenyRules", "getAskRules", "PermissionMode", "PermissionRule", "PermissionResult"]
    - path: "packages/extract/denial-tracking/src/index.ts"
      provides: "Denial tracking type stubs"
      exports: ["DENIAL_LIMITS", "DenialTracker"]
    - path: "packages/extract/cost-tracker/src/index.ts"
      provides: "Cost tracker type stubs"
      exports: ["getStoredSessionCosts", "addToTotalSessionCost", "formatTotalCost", "saveCurrentSessionCosts"]
  key_links:
    - from: "packages/extract/*/tsconfig.json"
      to: "tsconfig.base.json"
      via: "extends ../../tsconfig.base.json"
      pattern: "extends.*tsconfig\\.base"
---

<objective>
Scaffold the 3 extract-tier P0 packages: permission-system, denial-tracking, and cost-tracker. Each gets package.json, tsconfig.json, src/index.ts (type stubs with TODO comments), and README.md with source references.

Purpose: These are the foundational extract-tier packages. permission-system is the most important package in the monorepo (9.4K LOC source, foundation for yolo-classifier and dangerous-command-detection). denial-tracking and cost-tracker are small standalone packages.

Output: 3 compilable TS packages in packages/extract/ with type stubs matching Claude Code source signatures.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-monorepo-scaffold/01-01-SUMMARY.md

Critical references for the executor:
1. Source tree: /Users/cpconnor/projects/Inside Claude Code/claude-code/src/
2. Knowledge Base: /Users/cpconnor/projects/Inside Claude Code/claude-code-technical-knowledge-base-v2.1.md (sections 8, 29)
3. Build Inventory: /Users/cpconnor/projects/Inside Claude Code/KB-v2.1-Build-Inventory.md
4. bun binary: $HOME/.bun/bin/bun (not in default PATH)
5. Package scope: @claude-patterns/{name}
6. All implementations are TODO comments only -- type stubs, zero runtime code

<interfaces>
<!-- Established patterns from Phase 1 -->

From tsconfig.base.json:
```json
{
  "compilerOptions": {
    "strict": true,
    "target": "ES2022",
    "module": "ESNext",
    "moduleResolution": "bundler",
    "esModuleInterop": true,
    "skipLibCheck": true,
    "noEmit": true,
    "types": ["bun-types"]
  }
}
```

From package.json (root):
```json
{
  "workspaces": [
    "packages/extract/*",
    "packages/build/*",
    "packages/translate/ivr-call-flow-validator",
    "packages/translate/prompt-cache-optimizer"
  ]
}
```

Per-package tsconfig.json pattern (extract/build tier):
```json
{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": { "rootDir": "src", "outDir": "dist" },
  "include": ["src"]
}
```

Per-package package.json pattern:
```json
{
  "name": "@claude-patterns/{name}",
  "version": "0.0.0",
  "private": true,
  "main": "src/index.ts",
  "types": "src/index.ts"
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold permission-system package (FR-2.1)</name>
  <files>
    packages/extract/permission-system/package.json,
    packages/extract/permission-system/tsconfig.json,
    packages/extract/permission-system/src/index.ts,
    packages/extract/permission-system/README.md
  </files>
  <read_first>
    tsconfig.base.json,
    package.json,
    /Users/cpconnor/projects/Inside Claude Code/claude-code-technical-knowledge-base-v2.1.md (section 8: lines 519-614)
  </read_first>
  <action>
Create 4 files for the permission-system package (per FR-2.1):

**package.json**: Standard workspace manifest:
```json
{
  "name": "@claude-patterns/permission-system",
  "version": "0.0.0",
  "private": true,
  "main": "src/index.ts",
  "types": "src/index.ts"
}
```

**tsconfig.json**: Extends base config:
```json
{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": { "rootDir": "src", "outDir": "dist" },
  "include": ["src"]
}
```

**src/index.ts**: Type stubs with TODO comments. Must export these types and functions based on KB section 8:

Types/Interfaces:
- `PermissionMode` — union type: `"default" | "plan" | "acceptEdits" | "bypassPermissions" | "dontAsk" | "auto"`
- `PermissionRule` — interface with `tool: string`, `pattern?: string`, `source: PermissionRuleSource`
- `PermissionRuleSource` — type from `PERMISSION_RULE_SOURCES`: `"policySettings" | "user" | "project" | "local" | "cliArg" | "command" | "session"`
- `PermissionResult` — interface with `allowed: boolean`, `reason: string`, `rule?: PermissionRule`

Functions (all as `export function name(...): ReturnType { /* TODO */ }` with throw):
- `hasPermissionsToUseTool(toolName: string, toolInput: Record<string, unknown>, mode: PermissionMode): PermissionResult`
- `checkRuleBasedPermissions(toolName: string, toolInput: Record<string, unknown>): PermissionResult`
- `getAllowRules(): PermissionRule[]`
- `getDenyRules(): PermissionRule[]`
- `getAskRules(): PermissionRule[]`

Constants:
- `PERMISSION_RULE_SOURCES` — readonly tuple of source strings
- `DANGEROUS_BASH_PATTERNS` — readonly string array (list from KB section 8.6)
- `isDangerousBashPermission(pattern: string): boolean`

Every function body: `throw new Error("TODO: extract from utils/permissions/");`

**README.md**: Package documentation with:
- Package name and description
- Source reference: `utils/permissions/` (24 files, 9,409 LOC)
- KB reference: Section 8 — Permission System
- Tier: Extract (P0)
- Key exports list
- Dependencies: None (foundation package)
- Downstream dependents: yolo-classifier, dangerous-command-detection
  </action>
  <verify>
    <automated>cd "/Users/cpconnor/projects/Inside Claude Code" && npx tsc --noEmit -p packages/extract/permission-system/tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export" packages/extract/permission-system/src/index.ts` returns >= 10
    - `grep "PermissionMode" packages/extract/permission-system/src/index.ts` matches
    - `grep "PermissionRule" packages/extract/permission-system/src/index.ts` matches
    - `grep "PermissionResult" packages/extract/permission-system/src/index.ts` matches
    - `grep "hasPermissionsToUseTool" packages/extract/permission-system/src/index.ts` matches
    - `grep "DANGEROUS_BASH_PATTERNS" packages/extract/permission-system/src/index.ts` matches
    - `grep "extends.*tsconfig.base" packages/extract/permission-system/tsconfig.json` matches
    - `grep "@claude-patterns/permission-system" packages/extract/permission-system/package.json` matches
    - `grep "9,409 LOC\|9409 LOC\|utils/permissions/" packages/extract/permission-system/README.md` matches
    - `npx tsc --noEmit -p packages/extract/permission-system/tsconfig.json` exits 0
  </acceptance_criteria>
  <done>permission-system package exists with all type stubs, compiles cleanly, and README references source paths</done>
</task>

<task type="auto">
  <name>Task 2: Scaffold denial-tracking and cost-tracker packages (FR-2.2, FR-2.8)</name>
  <files>
    packages/extract/denial-tracking/package.json,
    packages/extract/denial-tracking/tsconfig.json,
    packages/extract/denial-tracking/src/index.ts,
    packages/extract/denial-tracking/README.md,
    packages/extract/cost-tracker/package.json,
    packages/extract/cost-tracker/tsconfig.json,
    packages/extract/cost-tracker/src/index.ts,
    packages/extract/cost-tracker/README.md
  </files>
  <read_first>
    tsconfig.base.json,
    packages/extract/permission-system/src/index.ts,
    /Users/cpconnor/projects/Inside Claude Code/claude-code-technical-knowledge-base-v2.1.md (section 8.4: lines 551-560, section 29: lines 1328-1332)
  </read_first>
  <action>
Create 4 files each for denial-tracking and cost-tracker.

**denial-tracking** (per FR-2.2, KB section 8.4, 45 LOC source):

package.json: `@claude-patterns/denial-tracking`, version 0.0.0, private, main/types src/index.ts

tsconfig.json: extends ../../tsconfig.base.json, rootDir src, outDir dist, include ["src"]

src/index.ts type stubs:
```typescript
export const DENIAL_LIMITS = {
  maxConsecutive: 3,
  maxTotal: 20,
} as const;

export interface DenialState {
  consecutive: number;
  total: number;
}

export type DenialAction = "continue" | "fallback_to_interactive";

export class DenialTracker {
  private state: DenialState;
  constructor() {
    this.state = { consecutive: 0, total: 0 };
  }
  recordDenial(): DenialAction {
    // TODO: extract from utils/permissions/denialTracking.ts
    throw new Error("TODO: extract from utils/permissions/denialTracking.ts");
  }
  recordApproval(): void {
    // TODO: extract from utils/permissions/denialTracking.ts
    throw new Error("TODO: extract from utils/permissions/denialTracking.ts");
  }
  shouldFallback(): boolean {
    // TODO: extract from utils/permissions/denialTracking.ts
    throw new Error("TODO: extract from utils/permissions/denialTracking.ts");
  }
  reset(): void {
    // TODO: extract from utils/permissions/denialTracking.ts
    throw new Error("TODO: extract from utils/permissions/denialTracking.ts");
  }
  getState(): Readonly<DenialState> {
    // TODO: extract from utils/permissions/denialTracking.ts
    throw new Error("TODO: extract from utils/permissions/denialTracking.ts");
  }
}
```

README.md: Source ref `utils/permissions/denialTracking.ts` (45 LOC), KB section 8.4, Extract tier P0. Notes: 3 consecutive or 20 total denials trigger fallback to interactive prompting.

**cost-tracker** (per FR-2.8, KB section 29, 323 LOC source):

package.json: `@claude-patterns/cost-tracker`, version 0.0.0, private, main/types src/index.ts

tsconfig.json: extends ../../tsconfig.base.json, rootDir src, outDir dist, include ["src"]

src/index.ts type stubs:
```typescript
export interface SessionCostEntry {
  model: string;
  inputTokens: number;
  outputTokens: number;
  cacheReadInputTokens: number;
  cacheCreationInputTokens: number;
  webSearchRequests: number;
  costUSD: number;
}

export interface SessionCosts {
  entries: SessionCostEntry[];
  totalCostUSD: number;
}

export function getStoredSessionCosts(): SessionCosts {
  // TODO: extract from cost-tracker.ts
  throw new Error("TODO: extract from cost-tracker.ts");
}

export function addToTotalSessionCost(entry: SessionCostEntry): void {
  // TODO: extract from cost-tracker.ts
  throw new Error("TODO: extract from cost-tracker.ts");
}

export function formatTotalCost(costUSD: number): string {
  // TODO: extract from cost-tracker.ts
  throw new Error("TODO: extract from cost-tracker.ts");
}

export function saveCurrentSessionCosts(costs: SessionCosts): void {
  // TODO: extract from cost-tracker.ts
  throw new Error("TODO: extract from cost-tracker.ts");
}
```

README.md: Source ref `cost-tracker.ts` (323 LOC), KB section 29, Extract tier P0. Notes: Per-model cost aggregation with OpenTelemetry counter support.

After creating both packages, run `bun install` from root to resolve workspaces, then verify both compile.
  </action>
  <verify>
    <automated>cd "/Users/cpconnor/projects/Inside Claude Code" && npx tsc --noEmit -p packages/extract/denial-tracking/tsconfig.json && npx tsc --noEmit -p packages/extract/cost-tracker/tsconfig.json && echo "BOTH PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "DENIAL_LIMITS" packages/extract/denial-tracking/src/index.ts` matches
    - `grep "DenialTracker" packages/extract/denial-tracking/src/index.ts` matches
    - `grep "maxConsecutive.*3" packages/extract/denial-tracking/src/index.ts` matches
    - `grep "@claude-patterns/denial-tracking" packages/extract/denial-tracking/package.json` matches
    - `grep "45 LOC\|denialTracking.ts" packages/extract/denial-tracking/README.md` matches
    - `grep "getStoredSessionCosts" packages/extract/cost-tracker/src/index.ts` matches
    - `grep "formatTotalCost" packages/extract/cost-tracker/src/index.ts` matches
    - `grep "SessionCostEntry" packages/extract/cost-tracker/src/index.ts` matches
    - `grep "@claude-patterns/cost-tracker" packages/extract/cost-tracker/package.json` matches
    - `grep "323 LOC\|cost-tracker.ts" packages/extract/cost-tracker/README.md` matches
    - `npx tsc --noEmit -p packages/extract/denial-tracking/tsconfig.json` exits 0
    - `npx tsc --noEmit -p packages/extract/cost-tracker/tsconfig.json` exits 0
  </acceptance_criteria>
  <done>denial-tracking and cost-tracker packages exist with type stubs, both compile cleanly, READMEs reference source paths and LOC</done>
</task>

</tasks>

<verification>
All 3 extract P0 packages compile and scaffold-check recognizes them:
```bash
cd "/Users/cpconnor/projects/Inside Claude Code"
npx tsc --noEmit -p packages/extract/permission-system/tsconfig.json
npx tsc --noEmit -p packages/extract/denial-tracking/tsconfig.json
npx tsc --noEmit -p packages/extract/cost-tracker/tsconfig.json
make scaffold-check 2>&1 | grep -E "permission-system|denial-tracking|cost-tracker"
```
All 3 should show "OK" in scaffold-check output.
</verification>

<success_criteria>
- 3 extract P0 packages created with package.json, tsconfig.json, src/index.ts, README.md each
- All 3 pass `tsc --noEmit`
- Type exports match KB section 8 (permission-system, denial-tracking) and section 29 (cost-tracker)
- All function bodies contain TODO comments referencing source paths
- `make scaffold-check` reports OK for all 3 packages
</success_criteria>

<output>
After completion, create `.planning/phases/02-p0-package-stubs/02-01-SUMMARY.md`
</output>
